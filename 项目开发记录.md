# 项目开发记录
> 本文件由历史分日开发记录整合而成（2026-09-23），原分日文件已删除。
> 按时间顺序排列，v1.0.1 总览置前。后续每次实质改动直接在本文件对应日期小节追加，不再新建分日文件。

## 目录
1. 总览：企业知识库助手 v1.0.1 开发记录
2. 2026-08-11：开发记录
3. 2026-08-25：环境搭建与启动
4. 2026-08-27：联调启动 / Docker 启动修复 / 容器化打包
5. 2026-08-30：代码优化 / Docker socket 诊断与升级
6. 2026-09-02：认证与安全加固 / 前端登录打通
7. 2026-09-03：混合检索 + BGE Rerank + 评测页对比
8. 2026-09-05：GPU 化 / 评测体系扩充 / 防幻觉门禁细化
9. 2026-09-08：审计日志页 / 流式输出 / 后端代码结构重构
10. 2026-09-09：真实制度文档 / 三态门禁 / 人工复核
11. 2026-09-11：会话持久化 / 分页与归档保留 / 知识库自动路由
12. 2026-09-13：会话内短期记忆 / 证据窗口 / 前端记忆徽标
13. 2026-09-15：文档解析扩展 / 多格式与 PDF 表格结构化
14. 2026-09-17：文档入库工程化 + 前端联动
15. 2026-09-19：文档入库 P1 正确性深水区（#1/#3/#4/#5/#7）
16. 2026-09-21：Rerank 模型路径去硬编码 + 缺失优雅降级
17. 2026-09-23：Linux 部署验证 + 上线联调修复 + RERANK_ENABLED/OCR 缺陷修复

---

# 总览：企业知识库助手 v1.0.1 开发记录

> 原文件：v1.0.1-企业知识库助手开发记录.md

> 本文档由 5 份会话开发记录（2026-08-11 / 08-25 / 08-27 / 08-30 / 09-02）汇总而成，
> 用于呈现项目从立项到 v1.0.1 的全貌与当前状态。

项目目录：后端 `E:\EnterpriseKB`，前端 `E:\EnterpriseKBWeb`

---

## 一、项目概述

面向企业内部资料的 Agent，第一阶段以知识库问答为核心（RAG），并预留三类能力：

| 能力 | 主要职责 | 当前状态 |
|---|---|---|
| RAG | 企业制度、项目文档、技术规范等静态知识问答 | 已搭建并持续加固 |
| MCP | 查询工单、只读数据库、代码平台等外部实时信息 | 第二阶段接入 |
| Skills | 周报生成、制度摘要等可重复、可审计工作流 | 第三阶段接入 |

设计原则：先完成小而完整的 MVP，不一开始堆叠多 Agent 与大量工具。

核心 RAG 工作流：

```text
文档 → 文本提取 → 语义切块 → Embedding → 向量库
用户问题 → 问题 Embedding → 相似度检索 → 上下文组装 → LLM 回答 + 引用
```

---

## 二、版本演进时间线

| 日期 | 版本阶段 | 主题 |
|---|---|---|
| 2026-08-11 | 立项 | 确定 MVP 范围与设计决策，产出项目骨架 |
| 2026-08-25 | 环境落地 | 逐轮排查并跑通本地环境，数据库端口统一到 5432 |
| 2026-08-27 | 可部署 | 修复 Docker 启动 bug，完成后端/前端容器化打包 |
| 2026-08-30 | 优化 | 完成 6 项非阻塞优化，Docker 升级诊断 |
| 2026-09-02 | v1.0.1 | 认证与安全加固，打通前端登录 |

---

## 三、系统架构与模块

### 3.1 后端模块清单

| 文件 | 职责 |
|---|---|
| `app/settings.py` | 读 `config/models.yaml` + `.env`，提供 `database_url()` 与 `get_provider()` |
| `app/database.py` | SQLAlchemy `engine`/`SessionLocal`/`Base`，`init_db()` 建表与 pgvector 扩展 |
| `app/models.py` | 知识库 / 文档 / 文本块 三张表 ORM 模型 |
| `app/providers.py` | OpenAI-compatible 客户端封装（`embed()` / `chat()`） |
| `app/ingestion.py` | 读文件 → 切块 → 去重 → 向量化 → 入库 |
| `app/rag.py` | 向量检索 → 组装上下文 → LLM 回答 + 引用 |
| `app/cli.py` | 命令行入口（init-db / ingest / ask / create-user / grant / list-users） |
| `app/api.py` | Web 入口（上传/删除/列表/问答/探活 + 认证中间件） |
| `app/auth/` | JWT 安全、SSO(OIDC)、认证中间件、知识库级权限依赖、数据模型、路由 |
| `app/audit.py` | JSON 行审计日志（登录/上传/删除/问答/登出） |

### 3.2 模块依赖图

```text
cli.py / api.py
   │ 调用
   ├──> ingestion.py ──> providers.py ──> OpenAI 兼容 API（Qwen/DeepSeek）
   │        │                 ▲
   │        └──> models.py ──┤
   │                 ▲        └──> database.py ──> PostgreSQL + pgvector
   ├──> rag.py ──────┤
   │        └──> providers.py / models.py
   └──> settings.py（database_url / get_provider，依赖最底层）
```

### 3.3 一次问答请求链路（HTTP 视角）

```text
浏览器/前端  POST /chat  {"question":"年假如何计算？","knowledge_base":"hr"}
  → 认证中间件校验 Bearer Token → 挂载 request.state.user
  → api.chat() → ensure_kb_access（知识库级读权限）
  → rag.ask()
      → 查 KnowledgeBase（hr）
      → providers.embed([question])（Qwen Embedding）
      → DocumentChunk.embedding.cosine_distance 检索 Top5
      → 距离超过 retrieval_threshold → 返回「知识库中未找到相关依据。」
      → providers.chat(SYSTEM_PROMPT, 资料+问题) 生成带引用回答
  → 返回 {"answer":"...", "citations":[...]}
```

---

## 四、功能清单

| 类别 | 功能 | 状态 |
|---|---|---|
| 导入 | Markdown / TXT / 文字型 PDF | ✅ |
| 解析 | 文本清洗、语义切块（650 字 + 80 字重叠、断点扩充） | ✅ |
| 编码 | `.md/.txt` UTF-8 → GB18030 降级解码 | ✅ |
| 去重 | 按内容 SHA-256 哈希去重 | ✅ |
| 检索 | pgvector 余弦相似度 Top5 + 阈值防幻觉 | ✅ |
| 回答 | 带文件名/页码/块序号引用 | ✅ |
| 缺料 | 无依据时明确拒答、拒绝编造 | ✅ |
| 模型 | 聊天 / Embedding 模型分离，Qwen / DeepSeek 可切换 | ✅ |
| Web API | 上传/删除/列表/问答/健康检查（11+ 路由） | ✅ |
| CLI | init-db / ingest / ask / create-user / grant / list-users | ✅ |
| 认证 | JWT 登录、开发模式登录、SSO(OIDC) 预留 | ✅ |
| 授权 | 知识库级 admin/user × read/write 权限 | ✅ |
| 安全 | 上传白名单/大小限制、CORS 可配、Prompt 注入防护 | ✅ |
| 审计 | JSON 行审计日志 | ✅ |
| 前端 | 登录页、Token 自动携带、401 跳转、路由守卫、用户区 | ✅ |
| 部署 | Docker 镜像、compose 编排、Nginx 同源反代 | ✅ |

---

## 五、认证与安全（v1.0.1 新增）

### 5.1 认证

- JWT（PyJWT/HS256）签发与校验，载荷含 `sub`/`username`/`role`/`iss`/`iat`/`exp`；
- 密钥来自 `.env` 的 `AUTH_JWT_SECRET`；开发模式缺省用内置开发密钥，生产缺省直接报错；
- 每次请求用 `sub` 回库取用户并校验 `is_active`，禁用/改角色即时生效；
- SSO（OIDC）标准 Authorization Code Flow：nonce + state(JWT) → discovery → code 换 token → JWKS 验签 → 查建 User → 签发本地 Token；未配置时端点返回 501。

### 5.2 授权

- `require_kb_access` / `ensure_kb_access`：admin 全量放行，user 按 `knowledge_base_access` 表读需 `can_read`、写需 `can_write`，否则 403；
- 公开路径白名单：`/health`、`/ready`、`/docs`、`/redoc`、`/openapi.json`、`/auth/*`。

### 5.3 安全加固

- 上传：扩展名白名单 `{.md,.txt,.pdf}`、`Path(filename).name` 防目录穿越、1MB/块流式写盘、累计超 `MAX_UPLOAD_BYTES`（默认 100MB）→ 413、空文件拒绝；
- CORS：读 `CORS_ORIGINS`，开发缺省 `*`，生产缺省空（同源部署）；
- Prompt 注入防护：检索上下文用 `<资料>...</资料>` 标签隔离，SYSTEM_PROMPT 明确资料为不可信数据；
- 审计日志：JSON 行格式输出 stdout（容器采集），`AUDIT_LOG_FILE` 时同时落盘，强制 UTF-8。

---

## 六、部署

### 6.1 架构

```text
浏览器 :8080 → frontend(Nginx 静态 + 反代 /auth) → backend(FastAPI:8000) → db(pgvector:5432)
```

### 6.2 三种部署方式

- 方式 A（推荐）：目标服务器 `docker compose -f docker-compose.deploy.yml up -d --build`；
- 方式 B（离线/内网）：本地 `docker build` + `docker save`，服务器 `docker load`；
- 方式 C（镜像仓库）：`docker tag` + `docker push`，服务器改 `image:`。

### 6.3 镜像

| 镜像 | 大小 |
|---|---|
| `enterprise-kb-backend:latest` | 332 MB |
| `enterprise-kb-frontend:latest` | 93.6 MB |

---

## 七、当前环境状态

| 组件 | 状态 |
|---|---|
| Docker Desktop | 4.88.1.237512，运行中（已知上游 socket bug #531/#532 未修复） |
| pgvector 容器 | `enterprisekb-postgres-1` healthy，`5432:5432` |
| 后端 FastAPI | `http://127.0.0.1:8000`（已加载认证与安全加固） |
| 前端 Vite | `http://127.0.0.1:5173`（登录页 → 主界面） |
| 数据库 | `hr` 知识库（employee-handbook.md）完好 |
| 测试账号 | `admin`(admin)、`alice`(hr 只读)、`bob`(无授权) |
| `.env` | 已含 `QWEN_API_KEY / DEEPSEEK_API_KEY / DATABASE_URL / AUTH_JWT_SECRET / AUTH_DEV_MODE=true` |
| 依赖 | PyJWT 2.13.0 已装入 .venv |

---

## 八、注意事项

1. 日常启动使用 `scripts\start-docker.ps1`（先清理 socket 再启动），不要直接点 Docker Desktop 图标，避免触发 #531/#532 崩溃；
2. 生产上线前：`AUTH_DEV_MODE=false`、`AUTH_JWT_SECRET` 设随机长串、`SSO_PROVIDER=oidc` 并填 `OIDC_*`（OIDC 代码就绪但未对真实 IdP 端到端实测）；
3. 上传双层限制：后端 `MAX_UPLOAD_BYTES`（默认 100MB）需与 Nginx `client_max_body_size 200m` 口径一致；
4. CLI（`create-user/grant/ingest/ask`）不经过 HTTP 认证，仅限服务器本机运维；
5. 恢复本机 PostgreSQL 16 前先停 Docker 容器避免 5432 冲突；
6. 更换 Embedding 模型或向量维度时，原知识库需重新索引。

---

## 九、后续规划

1. 准备 30–50 个真实问题构成评测集，衡量检索命中、回答准确性与引用正确性；
2. 增加关键词/BM25 与向量的混合检索，再加入重排；
3. 支持 DOCX、Excel、扫描 PDF 的 OCR 与结构化解析；
4. 增加文档版本、文件去重、重建索引与处理失败重试；
5. 接入只读 MCP（项目管理系统或内部数据库查询）；涉及写/删操作必须用户确认；
6. 编写首个 Skill（如「根据知识库和项目状态生成周报草稿」），默认只生成、不自动发送；
7. 对真实 IdP 完成 SSO(OIDC) 端到端实测。

---

# 2026-08-11：开发记录

> 原文件：2026-08-11-企业知识库助手开发记录.md

日期：2026-08-11  
项目目录：`C:\Users\cheng\Documents\ChatGPT\llmops`

## 一、目标

构建一个面向企业内部资料的 Agent。第一阶段以知识库问答为核心（RAG），并预留：

- 可切换的大语言模型能力，例如千问（Qwen）和 DeepSeek；
- MCP 外部工具接入能力；
- Skills 可复用工作流能力。

## 二、确定的实施策略

先完成一个小而完整的 MVP，而不是一开始加入大量工具或多 Agent。

第一版需要支持：

1. 导入 Markdown、TXT、文字型 PDF 文档；
2. 解析、清洗、切块并生成 Embedding；
3. 将文本、元数据和向量存入 PostgreSQL + pgvector；
4. 根据用户问题检索相关文本块；
5. 基于检索结果回答，并返回文档名、页码和引用片段；
6. 缺少资料依据时，明确回答“知识库中未找到相关依据”；
7. 通过配置文件切换回答模型。

## 三、关键概念与设计决策

### RAG 工作流

```text
文档 → 文本提取 → 语义切块 → Embedding → 向量库
用户问题 → 问题 Embedding → 相似度检索 → 上下文组装 → LLM 回答 + 引用
```

RAG 的目标不是把整个文档直接发送给模型，而是先检索最相关的少量片段，再让模型严格依据这些片段回答。

### 聊天模型与 Embedding 模型分离

两者承担不同职责：

- 聊天模型：组织最终回答、理解问题、按引用格式输出；
- Embedding 模型：把文档和问题转换为向量，决定能否检索到正确资料。

因此模型配置中将两者分离。可以将回答模型由 Qwen 切换为 DeepSeek；但若更换 Embedding 模型或向量维度，原知识库应重新索引。

### 统一模型适配层

Qwen、DeepSeek 等服务可通过 OpenAI-compatible API 适配。应用代码只依赖统一 Provider 接口，供应商 URL、模型名和 API Key 环境变量只放在配置中，避免业务代码与某一家模型绑定。

### MCP 与 Skills 的后续定位

| 能力 | 主要职责 | 当前状态 |
|---|---|---|
| RAG | 企业制度、项目文档、技术规范等静态知识问答 | 已搭建 MVP |
| MCP | 查询工单、只读数据库、代码平台等外部实时信息 | 第二阶段接入 |
| Skills | 例如周报生成、制度摘要等可重复、可审计工作流 | 第三阶段接入 |

建议先接入只读 MCP；涉及发消息、创建工单、修改或删除数据的工具必须要求用户确认。

## 四、已生成的项目骨架

项目已创建以下主要内容：

```text
llmops/
├── app/
│   ├── api.py          # FastAPI 上传与问答接口
│   ├── cli.py          # init-db、ingest、ask 命令
│   ├── database.py     # SQLAlchemy 与 pgvector 初始化
│   ├── ingestion.py    # 文件读取、分块、向量化、入库
│   ├── models.py       # 知识库、文档、文本块数据模型
│   ├── providers.py    # OpenAI-compatible 模型 Provider
│   ├── rag.py          # 相似度检索、提示词、引用组装
│   └── settings.py     # YAML 与环境变量配置读取
├── config/models.yaml  # Qwen / DeepSeek 模型配置
├── examples/           # 示例员工手册
├── docker-compose.yml  # PostgreSQL + pgvector
├── requirements.txt    # Python 依赖
├── .env.example        # API Key 环境变量示例
└── README.md           # 启动和使用说明
```

## 五、已完成验证

- Python 源文件静态编译通过；
- Python 依赖已在项目 `.venv` 虚拟环境中安装；
- CLI 已确认包含 `init-db`、`ingest`、`ask` 命令；
- FastAPI 已确认包含文档上传和问答路由；
- 文本分块逻辑已通过基本测试；
- 当前执行环境没有 Docker，因此未执行 PostgreSQL + pgvector 的完整入库与问答验证。

## 六、本地启动步骤

1. 安装并启动 Docker Desktop。
2. 在项目根目录复制环境变量模板并填写 Key：

```powershell
Copy-Item .env.example .env
```

3. 编辑 `.env`，至少设置：

```text
QWEN_API_KEY=你的千问API密钥
```

4. 启动数据库并初始化：

```powershell
docker compose up -d
.\.venv\Scripts\Activate.ps1
python -m app.cli init-db
```

5. 导入示例资料并提问：

```powershell
python -m app.cli ingest .\examples\employee-handbook.md --knowledge-base hr
python -m app.cli ask "年假如何计算？" --knowledge-base hr
```

6. 若希望通过 API 使用：

```powershell
uvicorn app.api:app --reload
```

随后访问 `http://127.0.0.1:8000/docs`。

## 七、后续迭代建议

1. 准备 30–50 个真实问题构成评测集，衡量检索命中、回答准确性和引用正确性。
2. 增加关键词/BM25 与向量的混合检索，再加入重排。
3. 支持 DOCX、Excel、扫描 PDF 的 OCR 与结构化解析。
4. 增加文档版本、文件去重、重建索引和处理失败重试。
5. 增加用户、部门与知识库的权限隔离。
6. 接入一个只读 MCP，例如项目管理系统或内部数据库查询。
7. 编写第一个 Skill，例如“根据知识库和项目状态生成周报草稿”，默认只生成、不自动发送。

---

# 2026-08-25：环境搭建与启动

> 原文件：2026-08-25-企业知识库助手开发记录.md

日期：2026-08-25
项目目录：`C:\Users\cheng\Documents\ChatGPT\llmops`

## 一、本次会话目标

审查项目代码、逐项排查启动条件，并完成本地开发环境搭建，使项目真正可运行，
最终将数据库端口统一到 5432 供项目使用。

## 二、代码审查结论

### 设计亮点

- Provider 统一 OpenAI-compatible 适配层，Qwen / DeepSeek 可切换，业务代码与厂商解耦；
- 聊天模型与 Embedding 模型分离配置（DeepSeek 无 Embedding 时自动回落 Qwen）；
- 导入按内容哈希去重；每个文本块保存文件名/页码/块序号，回答带引用；
- 检索距离阈值 0.65 之上拒绝编造答案（防幻觉）；
- 知识库记录 embedding 模型与维度，更换模型时拒绝写入，防止索引错配。

### 待优化小问题（非阻塞）

- `app/api.py` 使用 FastAPI 已弃用的 `@app.on_event("startup")`，建议改用 `lifespan`；
- `app/rag.py` 只校验 `embedding_model`，未校验 `embedding_dimensions`；
- `app/ingestion.py` 对 `.md/.txt` 强制 UTF-8 解码，GBK 编码文件会报错；
- `chunk_text` 找不到断点时会硬切 650 字符，可能切断句子；
- 检索阈值 `0.65` 为魔数，不同 embedding 模型差异大，建议按评测调整；
- API 缺少 `/health` 健康检查端点。

## 三、启动条件排查过程

### 第一轮（初检）

- 代码可导入、可编译（`imports OK` / `compile OK`）；
- Python 3.10.11 + `.venv` + 全部依赖就绪；
- 阻塞项：`.env` 缺失、数据库凭据不匹配（本机 PG 无 `enterprise_kb` 角色/密码）、
  Docker 未找到、未安装 pgvector 扩展。

### 第二轮（密钥与网络验证）

- 用户补充 `QWEN_API_KEY` / `DEEPSEEK_API_KEY`，实测 DeepSeek key 有效；
- 外网可达：DashScope / DeepSeek TLS 均连通（401 为未带 key 的正常响应）；
- 尝试腾讯云服务器 `62.234.51.39` 的 Docker：22/2375/2376/80/443/3389/8080
  全部超时，ping 无响应，判定不可达（疑似安全组限制或服务器停机），未采用；
- Windows 宿主排查不到 docker.exe，WSL 仅有残留的 `docker-desktop` 发行版（无引擎）。

### 第三轮（定位并启动 Docker Desktop）

- 用户提供安装路径 `C:\Users\cheng\AppData\Local\Programs\DockerDesktop\Docker Desktop.exe`；
- 启动后引擎就绪：Docker Desktop 4.86.0 / Engine 29.7.2（linux/amd64）；
- Docker CLI：`C:\Users\cheng\AppData\Local\Programs\DockerDesktop\resources\bin\docker.exe`。

### 第四轮（数据库搭建）

- 5432 被本机 PostgreSQL 16 占用，首次采用 `5433:5432` 映射避开冲突；
- `docker compose up -d` 拉取 `pgvector/pgvector:pg16`，容器 `llmops-postgres-1` healthy；
- `.env` 配置 `DATABASE_URL` 指向 5433。

### 第五轮（端到端验证，全部通过）

- `python -m app.cli init-db` → `Database initialized.`（vector 扩展 + 数据表）；
- `ingest examples/employee-handbook.md --knowledge-base hr` → 导入 1 个块
  （Qwen `text-embedding-v4` Embedding 调用成功）；
- `ask "年假如何计算？"` → 正确回答并带引用；
- `uvicorn app.api:app` → `GET /docs` 200、`POST /chat` 返回正确回答与引用。

### 第六轮（5432 端口迁移）

- 用户要求将本地 5432 腾出供项目使用；
- 通过 UAC 提权停止并禁用本机 PG 服务 `postgresql-x64-16`（数据保留，可恢复）；
- `docker-compose.yml` 与 `.env` 端口改回 `5432:5432`；
- 重建容器，验证 healthy、5432 被容器监听、`init-db` 与问答链路正常。

## 四、当前环境状态

| 组件 | 状态 |
|---|---|
| Docker Desktop | `C:\Users\cheng\AppData\Local\Programs\DockerDesktop`，引擎运行中 |
| pgvector 容器 | `llmops-postgres-1`，healthy，映射 `5432:5432` |
| 数据库 | `enterprise_kb` @ `localhost:5432`（用户/密码 `enterprise_kb`） |
| 本机 PostgreSQL 16 | 已停止并禁用（数据保留，可恢复） |
| `.env` | `QWEN_API_KEY` / `DEEPSEEK_API_KEY` 已填；`DATABASE_URL` 指向 5432 |
| 示例数据 | `hr` 知识库（employee-handbook.md）已导入 |

## 五、日常使用

```powershell
# 启动 Docker Desktop（重启电脑后需手动启动），容器随 compose 自动运行
docker compose up -d

python -m app.cli ingest .\examples\employee-handbook.md --knowledge-base hr
python -m app.cli ask "年假如何计算？" --knowledge-base hr

# API 服务（Swagger 文档）
uvicorn app.api:app --reload   # http://127.0.0.1:8000/docs
```

## 六、注意事项

1. 重启电脑后需先启动 Docker Desktop，数据库容器才可用；
2. 恢复本机 PostgreSQL：管理员 PowerShell 执行
   `Set-Service postgresql-x64-16 -StartupType Automatic; Start-Service postgresql-x64-16`，
   恢复前先停止 Docker 容器避免 5432 冲突；
3. 腾讯云服务器 `62.234.51.39` 需在控制台放行安全组并提供 SSH 凭据后方可使用；
4. 待优化项见第二节。

---

# 2026-08-27：联调启动 / Docker 启动修复 / 容器化打包

> 原文件：2026-08-27-企业知识库助手开发记录.md

日期：2026-08-27
项目目录：后端 `E:\EnterpriseKB`，前端 `E:\EnterpriseKBWeb`

## 一、本次会话目标

1. 阅读既有开发记录，启动后端与前端，确认系统可运行；
2. 排查并修复 Docker Desktop 每次启动报错的问题；
3. 把后端与前端打包为 Docker 镜像，便于在其他服务器上部署。

## 二、环境启动与端到端联调

- 阅读了 2026-08-11、2026-08-25 两份开发记录，确认项目为「RAG 企业知识库助手」MVP。
- 依次启动：Docker Desktop → pgvector 容器（`localhost:5432`）→ 后端
  `uvicorn app.api:app`（`127.0.0.1:8000`）→ 前端 `bun run dev`（`127.0.0.1:5173`）。
- 端到端验证通过：`GET /knowledge-bases` 返回 `hr` 知识库；`POST /chat`
  提问“年假如何计算？”返回带引用的正确回答。

## 三、Docker Desktop 启动报错修复

### 现象

每次启动 Docker 都会报错：

```text
starting services: initializing Inference manager:
listening on unix://C:/Users/<user>/AppData/Local/Docker/run/dockerInference
```

随后引擎无法启动。

### 根因

这是 Docker Desktop 4.86 的已知 bug（`docker/desktop-feedback` #531 / #532）：

- `%LOCALAPPDATA%\Docker\run` 与 `%LOCALAPPDATA%\docker-secrets-engine` 中会残留
  无法删除的 AF_UNIX socket 重解析点，报错 `1920（文件无法被系统访问）`；
- 这些 socket 文件用普通删除、`Remove-Item -Force`、甚至 `fsutil reparsepoint delete`
  都删不掉，但**重命名其父目录**可行；
- 后端启动时对这些 socket 做 `remove-then-bind` 失败，导致整个启动中止。

### 处理方案（已持久化）

1. 新增 `scripts/reset-docker-sockets.ps1`：停 Docker → 尽力清理历史遗留目录 →
   把两个 socket 目录改名挪走 →（可选）启动 Docker。
2. 新增 `scripts/start-docker.ps1`：一键干净启动整套
   （清理 socket → 启动 Docker → 拉起 pgvector → init-db）。
3. 在启动文件夹放置 `clean-docker-sockets.vbs`，每次登录自动静默清理，
   重启电脑后不会再出现该报错。
4. `settings-store.json` 中关闭 `EnableDockerAI`、`EnableInference`
   （关闭未使用的 AI / Model Runner，属无害优化）。

### 验证结果

`start-docker.ps1` 全程正常：引擎约 3 秒就绪、无报错，pgvector 容器 healthy，
`init-db` 成功，后端问答链路正常。

## 四、后端 / 前端 Docker 化打包

### 架构

```text
浏览器 :8080 → frontend(Nginx 静态 + 反代) → backend(FastAPI:8000) → db(pgvector:5432)
```

前端使用相对路径请求，由 Nginx 反向代理到后端，同源部署、无需 CORS。

### 新增 / 修改文件

| 文件 | 说明 |
|---|---|
| `E:\EnterpriseKB\Dockerfile` | 后端镜像（python:3.10-slim + uvicorn） |
| `E:\EnterpriseKB\.dockerignore` | 后端构建忽略清单 |
| `E:\EnterpriseKB\docker-compose.deploy.yml` | db + backend + frontend 编排 |
| `E:\EnterpriseKB\deploy.env.example` | 部署环境变量模板 |
| `E:\EnterpriseKB\DEPLOY.md` | 详细部署说明 |
| `E:\EnterpriseKBWeb\Dockerfile` | 前端镜像（Bun 构建 → Nginx 运行） |
| `E:\EnterpriseKBWeb\nginx.conf` | 前端反代与 SPA 回退配置 |
| `E:\EnterpriseKBWeb\.dockerignore` | 前端构建忽略清单 |
| `E:\EnterpriseKBWeb\src\api.ts` | API 地址改为相对路径（默认为空） |
| `E:\EnterpriseKBWeb\vite.config.ts` | 增加本地开发代理 |

### 构建出的镜像

| 镜像 | 大小 | 状态 |
|---|---|---|
| `enterprise-kb-backend:latest` | 332 MB | 已构建并验证 |
| `enterprise-kb-frontend:latest` | 93.6 MB | 已构建并验证 |

### 验证结果

部署栈三服务启动后，通过前端端口 `8080` 完成「上传文档 → 解析入库 → 提问带引用回答」
全链路验证，全部通过（测试栈随后已清理，镜像保留）。

### 部署方式

- **方式 A（推荐）**：目标服务器上直接
  `docker compose -f docker-compose.deploy.yml up -d --build`。
- **方式 B（离线/内网）**：本地 `docker build` + `docker save` 导出 tar，
  服务器 `docker load` 导入后启动。
- **方式 C（镜像仓库）**：`docker tag` + `docker push`，服务器改 `image:` 指向仓库。

详见 `DEPLOY.md`。

## 五、当前状态

| 组件 | 状态 |
|---|---|
| 本地后端（FastAPI） | 运行中，`http://127.0.0.1:8000` |
| 本地前端（Vite/Bun） | 运行中，`http://127.0.0.1:5173` |
| 本地 pgvector 容器 | `localhost:5432`，healthy |
| 部署镜像 | `enterprise-kb-backend:latest` / `enterprise-kb-frontend:latest` 已构建 |
| 部署测试栈 | 已清理（镜像保留） |

## 六、注意事项

1. 同一会话内反复开关 Docker，请使用 `scripts/start-docker.ps1`
   （登录自动清理只覆盖重启场景，不覆盖会话内手动开/关）。
2. 部署时 `QWEN_API_KEY` 必填；前端对外端口默认 `8080`，可用 `WEB_PORT` 覆盖。
3. 后端当前为 MVP、无登录鉴权，请勿直接暴露公网。
4. 历史遗留的少数 `.stale-*` 目录因 socket 文件无法删除而被保留，体积可忽略，
   不影响使用；清理脚本每次运行会继续尝试移除。

---

# 2026-08-30：代码优化 / Docker socket 诊断与升级

> 原文件：2026-08-30-企业知识库助手开发记录.md

日期：2026-08-30
项目目录：后端 `E:\EnterpriseKB`，前端 `E:\EnterpriseKBWeb`

## 一、本次会话目标

1. 阅读既有三份开发记录，掌握项目进度与当前状态；
2. 完成 08-25 记录中列出的 6 项「非阻塞小优化」；
3. 诊断 Docker Desktop 启动报 socket 错误的原因，并尝试升级根治；
4. 加固启动脚本，稳定日常开发环境。

## 二、代码优化：6 项非阻塞小优化（全部完成并验证）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `app/api.py` | `@app.on_event("startup")` → `lifespan`；新增 `GET /health`、`GET /ready` |
| 2 | `app/rag.py` | 补 `embedding_dimensions` 校验（与导入侧一致）；检索阈值改读配置 |
| 3 | `app/ingestion.py` | `.md/.txt` 解码降级 UTF-8 → GB18030，双失败给出明确报错 |
| 4 | `app/ingestion.py` | 切块断点扩充（。！？；，、及英文标点），减少硬切 |
| 5 | `app/settings.py` | `ProviderConfig` 新增 `retrieval_threshold` 字段 |
| 6 | `config/models.yaml` | qwen 下新增 `retrieval_threshold: 0.65` |

验证：`py_compile` 通过；单元测试 8/8（切块、GB18030 降级、阈值、维度）；
路由 11 条含 `/health`、`/ready`；端到端 `init-db → ingest → ask` 与
`POST /chat` 均通过。

## 三、Docker Desktop socket 报错诊断

### 现象

启动时仍报：

```text
starting services: initializing Secrets Engine:
listening on unix://C:/Users/cheng/AppData/Local/docker-secrets-engine/engine.sock:
remove .../engine.sock: The file cannot be accessed by the system.
(listener: The filename, directory name, or volume label syntax is incorrect.)
```

### 根因

与 08-27 的 Inference manager 报错同源：Docker Desktop 已知 bug
`docker/desktop-feedback #531/#532`。AF_UNIX 域套接字在 Windows 上以
重解析点（ReparsePoint）落盘，非正常退出后成为 0 字节孤儿文件，报错
`1920 文件无法被系统访问`，Docker 启动时 remove-then-bind 失败。

### 为何上次修复未根治

- 上次的「改名父目录」是 workaround，不是根治；
- 上次靠关闭 `EnableDockerAI`/`EnableInference` 压住 Inference manager，
  但 Secrets Engine 是核心凭据组件，无法关闭；
- 因此每次启动都会再产生新的 `.stale-*` 残留目录。

## 四、升级 Docker Desktop：4.86.0 → 4.88.1

### 过程

1. `docker desktop stop` 优雅停止；
2. 从官方 CDN 下载 `Docker Desktop Installer.exe`（4.88.1.237512，
   Authenticode 签名有效，约 602MB）；
3. `install --quiet --accept-license` 静默升级；
4. 升级后 `Docker Desktop.exe` 版本 4.88.1.237512，引擎 29.7.2 不变。

### 结论：4.88.1 仍未修复

对照实验：

- 先清理 socket 再启动 → 正常（`secrets engine ready`）；
- 优雅停止后**不清理**直接重启 → 后端仍崩溃，报错从 Secrets Engine 换成
  Ingest server：`Docker\run\sailor-ingest.sock` 同样「文件无法被系统访问」，
  日志明确标注 `Docker-Desktop/4.88.1`。

即：该 bug 与组件无关（哪个 socket 先初始化就先崩哪个），最新 4.88.1
仍受影响，需继续依赖 workaround 并跟踪上游 issue。

## 五、脚本加固

| 文件 | 加固点 |
|---|---|
| `scripts/reset-docker-sockets.ps1` | 停止范围扩大到 `com.docker.build` 等全部进程，并增加强制结束兜底 |
| `scripts/start-docker.ps1` | 引擎就绪判定从「命名管道存在」改为「`docker info` 返回 0」，避免管道先出现但 daemon 未就绪导致的 compose 失败；修正 stderr 重定向 |

## 六、当前状态

| 组件 | 状态 |
|---|---|
| Docker Desktop | 4.88.1.237512，运行中 |
| pgvector 容器 | `enterprisekb-postgres-1` healthy，`5432:5432` |
| 数据库 | `hr` 知识库数据完好（employee-handbook.md） |
| 后端/前端 | 代码已优化，待启动（uvicorn / bun run dev） |
| 启动脚本 | 已加固（reset / start-docker） |

## 七、注意事项

1. 日常启动务必使用 `scripts\start-docker.ps1`（先清理再启动），不要直接
   点击 Docker Desktop 图标，否则可能触发 #531/#532 崩溃；
2. 该 bug 截至 4.88.1 仍未修复，继续跟踪 `docker/desktop-feedback #531/#532`，
   上游修复后再升级；
3. `%LOCALAPPDATA%` 下累积的 `*.stale-*` 目录是 0 字节残留，不影响使用，
   清理脚本每次运行会继续尝试移除。

## 八、程序调用过程（.py 文件执行路径）

### 8.1 模块清单与职责

| 文件 | 职责 | 被谁调用 |
|---|---|---|
| `app/settings.py` | 读 `config/models.yaml` + `.env`，提供 `database_url()` 与 `get_provider()` | database.py / cli.py / api.py / ingestion.py / rag.py |
| `app/database.py` | 创建 SQLAlchemy `engine`/`SessionLocal`、`Base`，提供 `init_db()` | cli.py / api.py / models.py |
| `app/models.py` | 三张表 ORM 模型（knowledge_bases / documents / document_chunks） | ingestion.py / rag.py / api.py |
| `app/providers.py` | OpenAI-compatible 客户端封装（`embed()` / `chat()`） | ingestion.py / rag.py |
| `app/ingestion.py` | 读文件→切块→去重→向量化→入库（`ingest_file()`） | cli.py / api.py |
| `app/rag.py` | 问题向量化→检索→组装上下文→LLM 回答+引用（`ask()`） | cli.py / api.py |
| `app/cli.py` | 命令行入口（init-db / ingest / ask） | 终端用户 |
| `app/api.py` | Web 入口（FastAPI：上传/删除/列表/问答/探活） | uvicorn / 前端 |
| `app/__init__.py` | 包说明 + 调用关系总览 | 被 Python 包导入触发 |

### 8.2 模块依赖图

```text
cli.py / api.py
   │ 调用
   ├──> ingestion.py ──> providers.py ──> OpenAI 兼容 API（Qwen/DeepSeek）
   │        │                 ▲
   │        └──> models.py ──┤
   │                 ▲        └──> database.py ──> PostgreSQL + pgvector
   ├──> rag.py ──────┤
   │        └──> providers.py / models.py
   └──> settings.py（database_url / get_provider，位于依赖最底层）
```

### 8.3 命令行入口执行路径

#### `python -m app.cli init-db`

1. Python 加载 `app` 包（`__init__.py`）→ 执行 `app/cli.py`；
2. `cli.py` 导入 `app/database.py`，触发其模块级代码：`settings.database_url()`
   → `create_engine(...)` → `SessionLocal = sessionmaker(...)`；
3. `cli.py:initialize_database()` 调用 `database.init_db()`：
   - `from app import models`（把三张表注册进 `Base.metadata`）；
   - `CREATE EXTENSION IF NOT EXISTS vector`（启用 pgvector 扩展，幂等）；
   - `Base.metadata.create_all(engine)`（创建缺失的数据表）。

#### `python -m app.cli ingest <file> --knowledge-base hr`

1. `cli.py:ingest()` → `init_db()`（确保表存在）；
2. `SessionLocal()` 打开数据库会话；
3. `settings.get_provider(for_embeddings=True)` 取得 Embedding 配置（Qwen）；
4. `ingestion.ingest_file(session, path, "hr", provider_config)`：
   - 读文件字节 → 计算 SHA-256 `content_hash`；
   - 查找/创建 `KnowledgeBase`，并校验 embedding 模型与维度一致；
   - 按 `content_hash` 去重（已存在则返回 0 跳过）；
   - `read_document()` 读文本（PDF 逐页 / md·txt UTF-8→GB18030 降级）；
   - `chunk_text()` 切块（目标 650 字 + 80 字重叠）；
   - `providers.OpenAICompatibleProvider.embed()` 批量向量化；
   - 写入 `Document` 与批量 `DocumentChunk`，`commit()` 一次提交；
5. 打印 `Imported N chunks into knowledge base 'hr'.`。

#### `python -m app.cli ask "<问题>" --knowledge-base hr`

1. `cli.py:ask_command()` → `init_db()` → `SessionLocal()`；
2. `get_provider()`（聊天模型）与 `get_provider(for_embeddings=True)`（Embedding）；
3. `rag.ask(session, question, "hr", chat_config, embedding_config)`：
   - 校验知识库存在 + embedding 模型/维度一致；
   - `providers.embed([question])` 把问题向量化；
   - `DocumentChunk.embedding.cosine_distance(vector)` 升序取最相似 5 块；
   - 最相似块距离超过 `retrieval_threshold`（0.65）→ 返回「知识库中未找到相关依据。」；
   - 组装 `Citation`（文件名/页码/块序号/摘要）与上下文；
   - `providers.chat(SYSTEM_PROMPT, 资料+问题)` 生成回答；
4. 打印回答与 `Sources` 列表。

### 8.4 Web 入口执行路径

启动：`uvicorn app.api:app`（`127.0.0.1:8000`，Swagger 在 `/docs`）。

1. uvicorn 加载 `app.api:app`，导入链触发 `database.py`/`settings.py` 等模块级代码；
2. FastAPI `lifespan` 启动时调用 `init_db()`；
3. 各端点：
   - `GET  /knowledge-bases` → `list_knowledge_bases()`：查 `KnowledgeBase` + 文档数；
   - `GET  /knowledge-bases/{kb}/documents` → `list_documents()`：查 `Document` + chunk 数；
   - `POST /knowledge-bases/{kb}/documents` → `upload_document()`：临时文件 → `ingestion.ingest_file()`；
   - `DELETE /knowledge-bases/{kb}/documents/{id}` → `delete_document()`：删 `Document`（级联 chunks）；
   - `POST /chat` → `chat()` → `rag.ask()`；
   - `GET /health` → 存活探针；`GET /ready` → `SELECT 1` 数据库就绪探针。

### 8.5 一次问答请求的完整链路（HTTP 视角）

```text
浏览器/前端  POST /chat  {"question":"年假如何计算？","knowledge_base":"hr"}
  → api.chat()                                    # app/api.py
  → rag.ask()                                     # app/rag.py
      → 查 KnowledgeBase（hr）                    # app/models.py → database.py → pgvector
      → settings.get_provider(for_embeddings=True)# app/settings.py → qwen 配置
      → providers.embed([question])               # app/providers.py → DashScope Embedding
      → DocumentChunk.embedding.cosine_distance   # pgvector 向量检索，取 Top5
      → settings.get_provider()                   # 聊天模型配置
      → providers.chat(system, 资料+问题)          # app/providers.py → 聊天模型
  → 返回 {"answer":"...", "citations":[...]}
```

---

# 2026-09-02：认证与安全加固 / 前端登录打通

> 原文件：2026-09-02-企业知识库助手开发记录.md

日期：2026-09-02
项目目录：后端 `E:\EnterpriseKB`，前端 `E:\EnterpriseKBWeb`

## 一、本次会话目标

在 MVP 基础上补齐「公司正式使用」所需的安全底座，并打通前端登录：

1. 预留并实现 SSO（OIDC）登录，建立统一身份入口；
2. 实现认证中间件（JWT Bearer Token）与知识库级权限（admin/user，read/write）；
3. 对上传、CORS、Prompt 注入等做安全加固，并加入审计日志；
4. 打通前端登录页 → 获取 Token → 自动携带 Token → 401 跳登录的完整链路；
5. 补齐生产部署配置（Nginx / compose / 环境变量）与开发、部署文档。

## 二、完成项概览

| 类别 | 内容 | 状态 |
|---|---|---|
| 认证 | JWT（PyJWT/HS256）签发与校验、开发模式登录、SSO(OIDC) 预留 | ✅ |
| 授权 | 认证中间件 + 知识库级权限（`require_kb_access` / `ensure_kb_access`） | ✅ |
| 安全 | 上传白名单/大小限制/流式写盘、CORS 可配、Prompt 注入防护 | ✅ |
| 审计 | JSON 行审计日志（登录/上传/删除/问答/登出） | ✅ |
| 前端 | 登录页、Token 存储与自动携带、401 跳转、路由守卫、用户区/登出 | ✅ |
| 部署 | nginx `/auth` 反代、compose 透传 AUTH_*/SSO_*、离线编排同步 | ✅ |
| 文档 | README 重写、DEPLOY 更新、认证时序说明 | ✅ |
| 依赖 | requirements.txt 新增 `PyJWT`；.venv 安装 PyJWT 2.13.0 | ✅ |

## 三、认证与权限实现路径

### 3.1 数据模型（新增 `app/auth/models.py`）

两张表，随 `init_db()` 一并创建：

| 表 | 字段要点 | 用途 |
|---|---|---|
| `users` | username（唯一）、display_name、email、role（admin/user）、is_active | 登录用户 |
| `knowledge_base_access` | user_id、knowledge_base_id、can_read、can_write，`(user_id, kb_id)` 唯一 | 知识库级授权 |

`app/database.py` 的 `init_db()` 增加 `from app.auth import models`，使 `Base.metadata.create_all` 一并建表。

### 3.2 JWT（新增 `app/auth/security.py`）

- `create_access_token(user)`：签发 HS256 Token，载荷含 `sub`（用户 id）、`username`、`role`、`iss`、`iat`、`exp`；
- `decode_access_token(token)`：PyJWT 验签并校验算法 / issuer / 过期；
- `create_login_state(nonce)` / `verify_login_state(state)`：SSO 登录用带 nonce 的短效 state（防 CSRF/重放）。

密钥来自 `.env` 的 `AUTH_JWT_SECRET`；开发模式缺省用内置开发密钥，生产缺省直接报错。

### 3.3 认证中间件（新增 `app/auth/middleware.py`）

`api.py` 通过 `app.middleware("http")(auth_middleware)` 全局注册。每个请求：

1. 记录 `request.state.client_ip`（审计用）；
2. 路径在 `PUBLIC_PATHS`（`/health`、`/ready`、`/docs`、`/redoc`、`/openapi.json`、`/auth/login`、`/auth/sso/login`、`/auth/sso/callback`、`/auth/logout`）→ 直接放行；
3. 其余路径校验 `Authorization: Bearer <token>`，验签后按 `sub` 回库取 `User` 并校验 `is_active`；
4. 通过后把用户挂到 `request.state.user`；任一步失败返回 401。

> 设计要点：Token 无状态、不存会话，但**每次请求都用 `sub` 回库取一次用户**，
> 因此禁用用户 / 改角色立即生效，无需等 token 过期。

### 3.4 依赖与知识库权限（新增 `app/auth/dependencies.py`）

- `get_current_user`：优先读 `request.state.user`（中间件已写入），缺失时兜底自行解析 Bearer 头；
- `require_kb_access(write=False)`：路由级依赖，从 `request.path_params["knowledge_base"]` 取库名鉴权；
- `ensure_kb_access(user, kb_name, write=False)`：供 `/chat` 这类库名在请求体里的端点调用。

规则：`admin` 全量放行；`user` 查 `knowledge_base_access` 表，读需 `can_read`、写需 `can_write`，否则 403。

### 3.5 SSO（OIDC）实现（`app/auth/sso.py` + `routes.py`）

标准 Authorization Code Flow：

```text
/auth/sso/login   生成 nonce + state(JWT) → discovery → 302 跳转 IdP 授权页
/auth/sso/callback
   校验 state → code 换 token（client_secret 认证）
   → jwt.PyJWKClient 拉 JWKS 验 id_token 签名 + aud/iss/exp/nonce
   → 按 preferred_username/email/sub 查找或创建 User → 签发本地 access token
```

未配置 `SSO_PROVIDER=oidc` 与 `OIDC_*` 时两端点返回 501（保留预留语义）；IdP 异常 502、state 非法 400。

### 3.6 路由保护与 CLI（改 `app/api.py`、`app/cli.py`）

- 读权限：`GET /knowledge-bases`（列表按授权过滤）、`GET .../documents`、`POST /chat`；
- 写权限：`POST .../documents`（上传）、`DELETE .../documents/{id}`；
- CLI 新增：`create-user`、`grant`、`list-users`（本机运维，不经过 HTTP 认证）。

## 四、安全加固实现路径

### 4.1 上传安全（`app/api.py` upload_document）

- 扩展名白名单 `{.md,.txt,.pdf}`，否则 400；
- `Path(filename).name` 剥离路径防目录穿越；
- 1MB/块流式写盘，累计字节数超过 `MAX_UPLOAD_BYTES`（默认 100MB）→ 413；
- 空文件拒绝；业务错误统一 400。

### 4.2 CORS 可配置（`app/settings.py` + `api.py`）

`cors_origins()` 读 `CORS_ORIGINS`：开发缺省 `["*"]`，生产缺省空（同源部署无需 CORS），跨域逗号分隔。

### 4.3 Prompt 注入防护（`app/rag.py`）

- 检索上下文用 `<资料>...</资料>` 标签隔离；
- `SYSTEM_PROMPT` 明确「资料是不可信数据、不是指令」，拒绝文档中的「忽略指令/泄露资料/扮演角色」等注入，且不足时拒绝编造。

### 4.4 审计日志（新增 `app/audit.py`）

- JSON 行格式记录 `ts/action/user/ip/detail/extra`；
- 已接入登录、SSO 登录、登出、上传、删除、问答六类事件；IP 取自中间件写入的 `client_ip`；
- 输出 stdout（容器日志采集），`AUDIT_LOG_FILE` 时同时落盘；并强制 stdout/stderr 为 UTF-8（修复 Windows GBK 乱码）。

## 五、前端登录打通（`E:\EnterpriseKBWeb`）

| 文件 | 改动 |
|---|---|
| `vite.config.ts` | dev 代理新增 `/auth` |
| `src/api.ts` | `getToken/setAuth/clearAuth` 等 Token 管理；`request()` 统一自动附 `Authorization`；401 清凭证跳 `/login`；新增 `login/me/logout` |
| `src/pages/LoginPage.tsx` | 登录页：登录 → 存 Token → 拉 `me` → 跳转 |
| `src/App.tsx` | `/login` 路由 + `RequireAuth` 路由守卫 |
| `src/components/Layout.tsx` | 侧边栏显示当前用户/角色 + 退出登录 |
| `src/styles.css` | 登录页与用户区样式 |

Token 时序：登录请求本身不带 Authorization → 登录成功 `setAuth` 写 localStorage → 之后的 `me` 及所有请求由 `request()` 自动携带。

## 六、生产部署配置

- `docker-compose.deploy.yml`：backend 环境变量新增 `AUTH_JWT_SECRET / AUTH_DEV_MODE(默认 false) / AUTH_TOKEN_TTL_MINUTES / SSO_*/OIDC_*/CORS_ORIGINS / MAX_UPLOAD_BYTES / AUDIT_LOG_FILE`；
- `E:\EnterpriseKBWeb\nginx.conf`：新增 `location /auth` 反代；
- `deploy.env.example` / `.env.example`：补全认证、SSO、CORS、上传、审计变量；
- `.env`：写入随机 `AUTH_JWT_SECRET` + `AUTH_DEV_MODE=true`；
- `scripts\start_all.ps1`：引擎就绪判定改为 `docker info` 返回码（与 start-docker.ps1 一致）。

## 七、文件变更清单

**新增**：`app/auth/__init__.py`、`app/auth/models.py`、`app/auth/security.py`、`app/auth/dependencies.py`、
`app/auth/middleware.py`、`app/auth/sso.py`、`app/auth/routes.py`、`app/audit.py`、
`E:\EnterpriseKBWeb\src\pages\LoginPage.tsx`

**修改**：`app/api.py`、`app/cli.py`、`app/database.py`、`app/settings.py`、`app/rag.py`、`requirements.txt`、
`docker-compose.deploy.yml`、`.env`、`.env.example`、`deploy.env.example`、`scripts\start_all.ps1`、
`README.md`、`DEPLOY.md`、
`E:\EnterpriseKBWeb\vite.config.ts`、`E:\EnterpriseKBWeb\src\api.ts`、`E:\EnterpriseKBWeb\src\App.tsx`、
`E:\EnterpriseKBWeb\src\components\Layout.tsx`、`E:\EnterpriseKBWeb\src\styles.css`、`E:\EnterpriseKBWeb\nginx.conf`

## 八、验证结果

- 后端全部 `py_compile` 通过；前端 `bun run typecheck` 通过；
- 认证：无 token/坏 token → 401；`/auth/login` 签发 token、`/auth/me` 正常；`/auth/sso/login` 未配置 → 501、`/auth/sso/callback` 非法 state → 400；
- 权限：admin 全量；alice（hr 只读）可问答/列文档、上传/删除 → 403；bob（无授权）问答 → 403、知识库列表为空；
- 上传：`.exe` → 400 白名单拦截、空文件 → 400；正常 `.md` 上传成功并可删除清理；
- CORS：开发环境响应 `Access-Control-Allow-Origin: *`；
- 审计：登录/问答/上传/删除事件均以 JSON 行输出，中文无乱码；
- 前端代理：`/auth/login` → 带 token 访问 `/knowledge-bases`、`/auth/me` 全链路 200。

## 九、当前状态

| 组件 | 状态 |
|---|---|
| pgvector 容器 | `enterprisekb-postgres-1` healthy，`5432:5432` |
| 后端 FastAPI | 运行中，`http://127.0.0.1:8000`（已加载认证与安全加固） |
| 前端 Vite | 运行中，`http://127.0.0.1:5173`（登录页 → 主界面） |
| 测试账号 | `admin`(admin)、`alice`(hr 只读)、`bob`(无授权) |
| `.env` | 已含 `QWEN_API_KEY / DEEPSEEK_API_KEY / DATABASE_URL / AUTH_JWT_SECRET / AUTH_DEV_MODE=true` |
| 依赖 | PyJWT 2.13.0 已装入 .venv |

## 十、注意事项与后续

1. 生产上线前：`AUTH_DEV_MODE=false`、`AUTH_JWT_SECRET` 设随机长串、`SSO_PROVIDER=oidc` 并填 `OIDC_*`（当前 OIDC 代码已就绪但未对真实 IdP 端到端实测）；
2. `CORS_ORIGINS` 生产缺省为空（同源部署）；跨域时再显式配置；
3. 上传双层限制：后端 `MAX_UPLOAD_BYTES`（默认 100MB）需与 Nginx `client_max_body_size 200m` 保持一致口径；
4. CLI（`create-user/grant/ingest/ask`）不经过 HTTP 认证，仅限服务器本机运维使用；
5. 后续建议：评测集与混合检索、DOCX/Excel/OCR、文档版本与增量同步、只读 MCP、首个 Skill。

---

# 2026-09-03：混合检索 + BGE Rerank + 评测页对比

> 原文件：2026-09-03-企业知识库助手开发记录.md

日期：2026-09-03
项目目录：后端 `E:\EnterpriseKB`，前端 `E:\EnterpriseKBWeb`

## 一、本次会话目标

1. 在纯向量检索基础上引入**混合检索**（BM25 + RRF 融合）提升检索命中；
2. 在 RRF 之后引入 **BGE 交叉编码器 Rerank** 精排，进一步提升 top-k 精度；
3. 把「向量 / 混合 / Rerank」三种检索方式接到**前端评测页做一键对比**；
4. 盘点遗留问题与后续待办。

## 二、完成项概览

| 类别 | 内容 | 状态 |
|---|---|---|
| 混合检索 | 稠密(pgvector 余弦) + BM25(中文二元组分词) + RRF 融合 | 完成 |
| Rerank | BGE `bge-reranker-v2-m3` 交叉编码器精排（FlagEmbedding） | 完成 |
| 检索方式切换 | `dense` / `hybrid` / `rerank` 三模式可切换 | 完成 |
| 评测对比 | 前端评测页一键跑三种方式并对比指标 | 完成 |
| 依赖与模型 | 安装 FlagEmbedding；从 ModelScope 下载模型到本地 | 完成 |

## 三、关键实现路径

### 3.1 混合检索（新增 `app/retrieval.py`）

- `tokenize()`：中文按字符二元组分词（`年假如何计算` → `年假/假如/如何/何计/计算`），英文/数字按整词；
- `Bm25`：标准 Okapi BM25（k1=1.5, b=0.75），按知识库 id 进程内缓存；
- `hybrid_search(..., mode)`：稠密 Top20 + 稀疏 Top20 → RRF（1/(k+rank)）融合 → 按 `mode` 决定最终排序（`dense` 纯向量 / `hybrid` 融合 / `rerank` 再叠加交叉编码器）。

### 3.2 Rerank（新增 `app/rerank.py`）

- `_BgeReranker` 封装 `FlagEmbedding.FlagReranker`，`get_reranker()` 按模型名懒加载缓存；
- `_resolve_fp16("auto")`：自动检测 CUDA（有则半精度，无则全精度）；
- 模型默认 `BAAI/bge-reranker-v2-m3`（多语言、中文强，约 2.27GB fp32）。

### 3.3 检索方式透传

`app/rag.py` 的 `ask()` 新增 `retrieval_mode` 参数，缺省按配置（`rerank.enabled=true` 则 rerank，否则 hybrid）；`app/eval.py` 的 `run_eval()` 同样透传，使评测可按不同方式分别跑。

### 3.4 评测对比（`app/eval_api.py` + 前端）

- `POST /eval/runs` 请求体新增 `modes`（默认 `["dense","hybrid","rerank"]`）；
- job 的 `summary`/`results` 改为按 mode 分组的字典，`total = 题数 × 方式数`；
- 前端评测页：对比表（指标 × 方式）+ 方式 tab 切换逐题结果。

## 四、文件变更清单

**新增**：`app/retrieval.py`、`app/rerank.py`、`E:\EnterpriseKBWeb\src\pages\EvalPage.tsx`（评测页重写为对比版）

**修改**：`app/rag.py`、`app/eval.py`、`app/eval_api.py`、`app/settings.py`、`app/ingestion.py`、`app/api.py`、`app/database.py`、`config/models.yaml`、`requirements.txt`、`.gitignore`、`README.md`、`E:\EnterpriseKBWeb\src\api.ts`、`E:\EnterpriseKBWeb\src\styles.css`、`E:\EnterpriseKBWeb\README.md`

**新增目录/模型**：`models/bge-reranker-v2-m3`（约 2.27GB，已加入 `.gitignore`）

## 五、验证结果

- 后端 `py_compile` 通过；前端 `bun run typecheck` / `bun run build` 通过；
- BM25 单测：`请假流程→请假块`、`出差申请→出差块`、`年休假→年假块` 正确命中；
- Rerank 打分：`年假如何计算` vs 年假块 **0.1698**、vs 出差块 **0.0029**（区分明显）；
- 端到端评测 6/6 无回归；三方式对比 18/18 完成，三种方式 summary 均正确返回。

## 六、当前状态

| 组件 | 状态 |
|---|---|
| GPU | NVIDIA GeForce RTX 4060 Ti，8GB，驱动 591.86 |
| torch | **2.13.0+cpu，`cuda_available=False`（Rerank 当前走 CPU）** |
| Rerank 模型 | `models/bge-reranker-v2-m3` 本地已就绪 |
| pgvector 容器 | healthy，`5432:5432` |
| 后端 FastAPI | 运行中，`http://127.0.0.1:8000` |
| 前端 Vite | 未启动（`cd E:\EnterpriseKBWeb; bun run dev`） |
| 评测集 | `eval/hr-eval.yaml`（6 题） |

## 七、遗留问题与待办

### 7.1【P0】Rerank 是否使用 GPU —— 当前未启用，走 CPU

- 现状：`torch 2.13.0+cpu`，`cuda_available=False`，BGE 重排全在 CPU 上跑（模型首次加载约 7s，每次重排数秒）。
- 解决：安装 CUDA 版 torch（4060 Ti 8GB 支持 fp16）：`pip install torch --index-url https://download.pytorch.org/whl/cu121`；`retrieval.rerank.fp16` 保持 `auto`，升级后会自动切半精度。
- 注意：升级 torch 后需重装/校验 `FlagEmbedding`、`transformers` 的兼容性；建议后端启动时打印一次「rerank 运行设备」，便于确认已切到 cuda。

### 7.2【P0】评测集过少，无法体现差别

- 现状：`hr-eval.yaml` 仅 6 题（4 事实 + 2 拒答），三种方式检索命中率均为 4/4，无区分度。
- 解决：按 `eval/README.md` 扩充到 30–50 题：覆盖各文档章节、难度梯度（L1 原文 / L2 改写 / L3 推理）、**专有名词 / 编号 / 精确词面类**（最能体现 BM25 与 Rerank 价值）、多跳与负面案例；扩充后重跑三方式对比，量化提升。

### 7.3 其他待办

| # | 待办 | 说明 |
|---|---|---|
| 1 | 评测指标细化 | 当前只统计「来源文件是否进 Top5」，建议加 hit@1/@3/@5、MRR、首块命中率，更精确衡量 Rerank 收益 |
| 2 | 评测结果持久化/历史对比 | job 目前仅内存保留最近 50 个；建议 summary 落库/落盘，画命中率趋势图 |
| 3 | BM25 分词升级 | 二元组 → jieba 分词（或自定义词典），提升专有名词/长词召回 |
| 4 | 大规模检索 | BM25 内存索引全量加载在超大知识库下吃力，可评估 PG 原生 `pg_trgm`/全文检索 |
| 5 | 防幻觉门禁细化 | 当前只按稠密距离拒答；BM25/Rerank 强命中但稠密弱时会被误拒，可加可配置的「稀疏/重排分数救援」 |
| 6 | 文档处理扩展 | DOCX / Excel / 扫描 PDF OCR（历史遗留） |
| 7 | 文档生命周期 | 版本、增量同步、重建索引、失败重试（历史遗留） |
| 8 | SSO(OIDC) 实测 | 代码就绪但未对真实 IdP 端到端实测（历史遗留） |
| 9 | 只读 MCP + 首个 Skill | 周报草稿等可审计工作流（历史遗留） |
| 10 | 部署镜像 | Dockerfile 需纳入 FlagEmbedding 与模型分发（镜像将显著变大）；`models/` 已 gitignore，部署需单独分发权重 |
| 11 | 自动化测试 | 仓库无 `tests/`，建议补 pytest（tokenize / BM25 / RRF / rerank / 评测指标 / 权限） |
| 12 | 评测页增强 | 在线编辑评测集（增删题目写回 YAML） |
| 13 | 检索回归对比脚本 | 纯向量 vs 混合 vs Rerank 的 hit@k/MRR，接入 CI 门禁 |
| 14 | Docker socket bug | 继续跟踪 `docker/desktop-feedback #531/#532` 上游修复（历史遗留） |

---

# 2026-09-05：GPU 化 / 评测体系扩充 / 防幻觉门禁细化

> 原文件：2026-09-05-企业知识库助手开发记录.md

日期：2026-09-05
项目目录：后端 `E:\EnterpriseKB`，前端 `E:\EnterpriseKBWeb`

## 一、本次会话目标

1. 阅读既有开发记录，掌握项目进度与已实现功能；
2. 解决 torch 为 CPU 版的问题，使 BGE Rerank 真正跑在 GPU 上；
3. 扩充评测体系：生成多类型制度文档、把评测集扩到 50 题、升级块级检索指标；
4. 用数据驱动的方式细化防幻觉门禁（解决“拒答完全靠 LLM 兜底、稠密距离门禁形同虚设”）；
5. 产出两份技术文档，便于随时查阅学习。

## 二、完成项概览

| 类别 | 内容 | 状态 |
|---|---|---|
| torch GPU | 2.13.0+cpu → 2.13.0+cu126（CUDA 12.6），Rerank 上 GPU | 完成 |
| 示例文档 | 新增财务/差旅/IT 三份制度，覆盖 Markdown/TXT/PDF 三类型 | 完成 |
| 评测集 | `enterprise-eval.yaml` 扩至 50 题（43 事实 + 7 无依据，L1/L2/L3） | 完成 |
| 评测指标 | 新增块级 Recall@5 / Hit@1 / Hit@3 / MRR、难度标签、单题知识库 | 完成 |
| 门禁细化 | `should_refuse`：稠密距离 + Rerank 分数组合判定，阈值数据驱动标定 | 完成 |
| 技术文档 | 《向量化与检索技术说明》《技术全景与学习手册》两份 | 完成 |

## 三、关键实现路径

### 3.1 torch GPU 化（Rerank 提速）

- 现状：`torch 2.13.0+cpu`，`cuda_available=False`，BGE 重排全在 CPU；
- 下载：官方 `download.pytorch.org` 直连仅 ~100KB/s；阿里云镜像有该 wheel 但单连接仍慢；
  改用 **32 路分段并行下载**（curl `-r` 范围请求，聚合约 2–3 MB/s）→ **SHA-256 校验
  通过**（`349ce4cc…1052` 与官方一致）→ `pip install --no-deps` 本地安装；
- 版本选择：torch 2.13.0 的 Windows/Python3.10 稳定 CUDA 构建有 cu126 / cu130，
  选更成熟的 **cu126**；驱动 591.86（CUDA 13.1）完全兼容；
- 验证：`torch.cuda.is_available()=True`，设备 RTX 4060 Ti；Rerank 首次调用把模型
  搬到 `cuda:0` + `float16`（显存约 1.1GB），之后单次 **29ms**（此前 CPU 每次数秒）。

### 3.2 评测体系扩充

- 文档：扩充 `employee-handbook.md`，新增 `finance-policy.md`（Markdown）、
  `travel-policy.txt`（TXT）、`it-policy.pdf`（reportlab 生成，3 页，中文 CID 字体），
  导入 `enterprise` 知识库共 **9 个文本块**；
- `app/rag.py`：`ask()` 增加 `return_chunks`，把完整检索块透出给评测；
- `app/eval.py`：`EvalCase` 新增 `knowledge_base / expected_chunk / difficulty`；
  用 `expected_chunk` 计算**块级** Recall@5 / Hit@1 / Hit@3 / MRR（替代只能看文件名的
  失真指标）；报告按难度分层；
- 新建 `eval/enterprise-eval.yaml`（50 题：跨文档近似内容、精确词面、档位边界、多跳、
  负面拒答）；更新 `eval/README.md` 说明新字段与新指标。

### 3.3 防幻觉门禁细化（数据驱动）

- `app/retrieval.py`：新增 `RetrievalSignals`（稠密距离 / BM25 最高分 / Rerank 最高分），
  `hybrid_search` 改为返回信号；
- `app/rag.py`：新增 `should_refuse()` —— 稠密距离为主、Rerank 分为辅：
  稠密通过但重排分 < floor 则拒答（收紧）；稠密超阈值但重排分 ≥ rescue 则救援放行；
- `app/settings.py` + `config/models.yaml`：新增 `retrieval.gate` 段；
- **阈值标定**：采集 50 题信号分布（`eval/gate-signals.tsv`）发现——
  稠密距离 0.65 门禁**从未触发**（全部 < 0.58，之前拒答 100% 靠 LLM）；
  Rerank 分才是有效信号（负面 ≤0.081、事实 ≥0.062，小重叠）→ 取
  `rerank_floor=0.05`、`rerank_rescue=0.5`。

### 3.4 技术文档

- `向量化与检索技术说明`：入库与检索两环节的处理方式、参数速查；
- `技术全景与学习手册`：全技术栈 28 项技术、每项解决什么问题、关键概念讲解
  （RAG/Embedding/BM25/RRF/交叉编码器/JWT/OIDC/pgvector）、代码地图、学习路线。

## 四、文件变更清单

**新增**：`examples/finance-policy.md`、`examples/travel-policy.txt`、`examples/it-policy.pdf`、
`eval/enterprise-eval.yaml`、`eval/gate-signals.tsv`、
`向量化与检索技术说明`、`技术全景与学习手册`

**修改**：`examples/employee-handbook.md`、`app/rag.py`、`app/retrieval.py`、
`app/settings.py`、`app/eval.py`、`config/models.yaml`、`eval/README.md`、`README.md`

**生成物（评测输出）**：`eval/enterprise-eval-result.json`、`eval/enterprise-eval-report.md`

## 五、验证结果

- 后端 `py_compile` 通过；配置加载正常；
- **50 题评测（默认 rerank 模式，GPU）**：

| 指标 | 数值 |
|---|---|
| 检索命中率（文件名级） | 43/43 = 1.0 |
| 事实覆盖率 | 43/43 = 1.0 |
| 拒答正确率 | 7/7 = 1.0 |
| 块级 Recall@5 | 1.0 |
| 块级 Hit@1 / Hit@3 | 0.9535 / 1.0 |
| 块级 MRR | 0.9767 |
| 难度分层 Recall@5 | L1 21/21 · L2 15/15 · L3 7/7 |

- **门禁细化验证**：改后 `neg-002 ~ neg-007` 的拒答不再携带引用（门禁确定性拦截，
  空引用返回），仅 `neg-001`（重排分 0.081，处模糊带）仍由 LLM 兜底；事实题无回归。

## 六、当前状态

| 组件 | 状态 |
|---|---|
| GPU | RTX 4060 Ti 8GB，驱动 591.86 |
| torch | 2.13.0+cu126（CUDA 12.6），`cuda_available=True` |
| Rerank | `bge-reranker-v2-m3` 本地就绪，GPU + fp16，单次约 29ms |
| pgvector 容器 | healthy，`5432:5432` |
| `enterprise` 知识库 | 4 份文档、3 类型、9 个文本块 |
| 评测集 | `enterprise-eval.yaml` 50 题 |
| 门禁参数 | `rerank_floor=0.05`、`rerank_rescue=0.5` |
| 技术文档 | `docs/` 两份 + README 已链接 |

## 七、遗留问题与待办

| # | 待办 | 说明 |
|---|---|---|
| 1 | 门禁模糊带 | `neg-001`(0.081) 与弱事实题(0.062) 重排分重叠，单阈值无法 100% 分离，最后仍需 LLM 兜底 |
| 2 | 稠密阈值偏松 | 0.65 对当前 Embedding 模型从不触发；后续可下调并配合 `rerank_rescue`，需重新标定 |
| 3 | 阈值重标定机制 | `rerank_floor/rescue` 随 Embedding/重排模型变化，换模型须重跑 `gate-signals` |
| 4 | 三方式对比 CLI | `app.eval` 尚未暴露 `--modes`，前端评测页可跑三方式但 CLI 不行 |
| 5 | CI 门禁 | 块级 hit@k/MRR 尚未接入持续集成回归 |
| 6 | 评测集真实化 | 当前 50 题为自编，建议替换/补充真实用户问题 |
| 7 | 跨文档近重复内容 | finance 与 travel 的“600 元住宿”造成 2 题 Hit@2（属语料冗余，收益有限，暂缓） |
| 8 | 历史遗留 | DOCX/Excel/扫描PDF OCR、文档版本与增量同步、SSO 真实 IdP 实测、只读 MCP 与首个 Skill、部署镜像纳入模型分发、补 pytest |

## 八、本次小结

本次围绕“让检索/拒答可量化、可解释”推进：GPU 化让 Rerank 从“每次数秒”降到 29ms；
评测体系从“6 题无区分度”升级到“50 题 + 块级指标”，首次拿到有区分度的真实数字；
门禁从“形同虚设、全靠 LLM”变成“数据驱动的确定性拦截”。下一步优先：三方式对比
CLI + CI 门禁，以及把评测集替换为真实问题。

---

# 2026-09-08：审计日志页 / 流式输出 / 后端代码结构重构

> 原文件：2026-09-08-企业知识库助手开发记录.md

日期：2026-09-08
项目目录：后端 `E:\EnterpriseKB`，前端 `E:\EnterpriseKBWeb`

## 一、本次会话目标

1. 在前端补充「审计日志」查看功能，便于审计追溯；
2. 启动整套项目并排查修复提问时的 500 错误；
3. 把问答从「一次性输出」优化为「流式输出」，提升体验；
4. 重构后端代码结构：统一用 APIRouter + Depends 注册路由，并按职责分层，避免代码平铺在 `app/` 下。

## 二、完成项概览

| 类别 | 内容 | 状态 |
|---|---|---|
| 审计日志 | 后端落库 `audit_logs` 表 + `/audit/actions`、`/audit/logs` 查询 API（admin） | ✅ |
| 审计日志页 | 前端新增 `/audit` 页面（过滤/分页），侧边栏入口仅 admin 可见 | ✅ |
| 启动与排障 | Docker + pgvector + 前后端启动；修复后端在沙箱内出站网络被禁导致的 500 | ✅ |
| 流式输出 | `POST /chat/stream`（SSE）+ 前端增量渲染 | ✅ |
| 代码结构 | 分层为 core / models / schemas / services / routers | ✅ |

## 三、审计日志查看

### 3.1 背景

此前审计日志仅以 JSON 行输出到 stdout 或 `AUDIT_LOG_FILE` 文件，无法在页面查询。
本次补齐「落库 + 查询 + 前端展示」闭环。

### 3.2 后端

- `app/models/audit.py`：新增 `AuditLog` 模型（`audit_logs` 表：ts/action/user/ip/detail/extra(JSONB)）；
- `app/services/audit.py`：`log_event()` 在写 stdout/文件之外，调用 `_persist_to_db()` 落库；
  落库为「尽力而为」，DB 不可用时仅记录错误、不中断主流程；
- `app/routers/audit.py`：`GET /audit/actions`（操作类型+计数）、`GET /audit/logs`
  （`q/action/user` 过滤 + `limit/offset` 分页），均 `require_admin`。

### 3.3 前端

- `src/api.ts`：新增 `AuditLogEntry/AuditLogsResponse/AuditActionStat` 类型与 `listAuditActions/listAuditLogs`；
- `src/pages/AuditLogPage.tsx`：过滤栏（关键词/操作类型/用户）+ 表格 + 分页；
- `src/App.tsx` 增加 `/audit` 路由；`src/components/Layout.tsx` 侧边栏新增「审计日志」（仅 admin）；
- `vite.config.ts` / `nginx.conf` 增加 `/audit` 代理与反代。

> 注意：改动前的历史审计日志只存在于 stdout/文件，不会自动回填到新表；新表从改动后产生的事件开始记录。

## 四、启动与 500 排障

### 4.1 启动

按 `scripts/start-docker.ps1` 拉起 Docker Desktop + pgvector（5432）→ `init-db` →
后端 `uvicorn app.api:app`（8000）→ 前端 `bun run dev`（5173）。

### 4.2 提问 500 根因与修复

- 现象：`POST /chat` 返回 500，后端日志报
  `httpcore.ConnectError: [WinError 10013] 以一种访问权限不允许的方式做了一个访问套接字的尝试`
  → `openai.APIConnectionError`，发生在 Qwen Embedding 调用；
- 根因：后端进程在**沙箱内**启动，出站网络被禁止（本机回环正常，外部模型 API 被拦）；
- 修复：改用非沙箱命令启动后端（项目内已有对应授权前缀）：

```powershell
C:\Users\cheng\.cache\codex-runtimes\codex-primary-runtime\dependencies\native\powershell\pwsh.exe -Command "cd E:\EnterpriseKB; .\.venv\Scripts\python.exe -m uvicorn app.api:app --host 127.0.0.1 --port 8000"
```

> 经验：本地起后端务必用非沙箱方式，否则外部模型调用会被沙箱拦截。

## 五、流式输出

### 5.1 后端

- `app/services/providers.py`：新增 `chat_stream()`（`stream=True`，逐块 yield `delta.content`）；
- `app/services/rag.py`：抽出 `retrieve_context()`（检索 + 门禁，供一次性/流式共用）与
  `build_user_prompt()`；原 `ask()` 行为不变（CLI/评测不受影响）；
- `app/routers/chat.py`：新增 `POST /chat/stream`（SSE），事件
  `citations → delta×N → done`，异常走 `error`；保留原 `POST /chat` 兼容。

### 5.2 前端

- `src/api.ts`：新增 `chatStream()`（`fetch` + `ReadableStream` 解析 SSE，逐块回调）；
- `src/pages/ChatPage.tsx`：先插入空的助手消息，`onDelta` 逐字追加、`onCitations` 到达即显示引用，
  打字指示器仅在未出字时显示；
- `nginx.conf`：`location = /chat` → `location /chat`（覆盖 `/chat/stream`），并加
  `proxy_buffering off / proxy_read_timeout 300s` 等 SSE 必需项。

## 六、后端代码结构重构

### 6.1 目标

统一用 APIRouter + Depends 注册路由，避免代码引用混乱；按职责分层，避免都堆在 `app/` 根目录。

### 6.2 新结构

```text
app/
├── api.py            # 应用装配（中间件 + 路由聚合），启动入口 uvicorn app.api:app
├── cli.py / eval.py  # 命令行入口；eval.py 为 python -m app.eval 兼容入口
├── core/             # config / database / security
├── models/           # knowledge / auth / audit
├── schemas/          # ChatRequest / LoginRequest / RunEvalRequest
├── services/         # providers/ingestion/retrieval/rerank/rag/audit/eval/sso
└── routers/          # deps/middleware + health/auth/knowledge/chat/eval/audit
```

### 6.3 关键迁移

- 原 `app/settings.py → app/core/config.py`、`app/database.py → app/core/database.py`、
  `app/auth/security.py → app/core/security.py`；
- 原 `app/models.py → app/models/knowledge.py` + 拆分出 `app/models/audit.py`；
  `app/auth/models.py → app/models/auth.py`；
- 原 `app/providers.py / ingestion.py / retrieval.py / rerank.py / rag.py / audit.py` →
  `app/services/`；`app/auth/sso.py → app/services/sso.py`；`app/eval.py → app/services/eval.py`；
- 原 `app/auth/dependencies.py → app/routers/deps.py`、`app/auth/middleware.py → app/routers/middleware.py`、
  `app/auth/routes.py → app/routers/auth.py`、`app/eval_api.py → app/routers/eval.py`、
  `app/audit_api.py → app/routers/audit.py`；
- 知识库/文档、问答、健康检查路由从 `app/api.py` 拆出为 `app/routers/knowledge.py`、
  `app/routers/chat.py`、`app/routers/health.py`；
- `app/api.py` 只保留应用装配，`app/routers/__init__.py` 的 `api_router` 聚合全部子路由。

### 6.4 兼容性

`uvicorn app.api:app`、`python -m app.cli ...`、`python -m app.eval` 与全部 HTTP 路径/字段均保持不变，
前端与 `scripts/*.ps1` 无需改动。

## 七、验证结果

- 后端 `py_compile` 全部 31 个文件通过；关键模块导入通过；
- 路由清单核对一致：health/ready、auth（login/me/sso/logout）、knowledge-bases + documents、
  chat + chat/stream、eval（sets/runs）、audit（actions/logs）；
- 冒烟测试：`/health`=ok、`/ready`=ready、`/knowledge-bases` 返回 hr/enterprise、
  `/audit/actions` 正常、`/chat/stream` 流式返回 5 条引用 + 增量文本；
- 前端 `bun run typecheck`、`vite build` 通过；
- 审计：登录/问答事件已落库并可经管理端 API 查询。

## 八、当前状态

| 组件 | 状态 |
|---|---|
| pgvector 容器 | `enterprisekb-postgres-1` healthy，`5432:5432` |
| 后端 FastAPI | 运行中（非沙箱），`http://127.0.0.1:8000` |
| 前端 Vite | 运行中，`http://127.0.0.1:5173` |
| 数据库 | `hr`（2 文档）、`enterprise`（4 文档）知识库完好 |
| 审计 | `audit_logs` 表已启用，登录/问答事件持续落库 |
| 代码结构 | 已重构为 core/models/schemas/services/routers 分层 |

## 九、遗留问题与待办（本次未处理，下次继续）

| # | 待办 | 说明 |
|---|---|---|
| 1 | 代码未提交 | 自 09-03 起的多轮改动均未 git commit，当前 master 仅停在 09-02 认证提交 |
| 2 | 门禁模糊带 | `neg-001`(0.081) 与弱事实题(0.062) 重排分重叠，单阈值无法 100% 分离 |
| 3 | 三方式对比 CLI | `app.eval` 尚未暴露 `--modes`，只能走前端评测页对比 |
| 4 | CI 门禁 / pytest | 块级 hit@k/MRR 未接入 CI；仓库无 `tests/` |
| 5 | 评测集真实化 | 50 题为自编，建议替换/补充真实用户问题 |
| 6 | 文档处理扩展 | DOCX / Excel / 扫描 PDF OCR |
| 7 | 文档生命周期 | 版本、增量同步、重建索引、失败重试 |
| 8 | SSO 实测 | OIDC 代码就绪但未对真实 IdP 端到端实测 |
| 9 | MCP + Skill | 只读 MCP 接入与首个 Skill（周报草稿等） |
| 10 | 部署镜像 | Dockerfile 未纳入 FlagEmbedding 与 2.3GB 重排模型分发 |
| 11 | Docker socket bug | 继续跟踪 `docker/desktop-feedback #531/#532` 上游修复 |
| 12 | BM25 分词升级 | 二元组 → jieba/自定义词典，提升专有名词召回 |

## 十、本次小结

本次聚焦「可用性与可维护性」：补齐了审计日志的前端查询闭环，把问答改成流式输出改善体验，
并将后端从单文件堆叠重构为分层结构，为后续继续迭代扫清代码组织上的障碍。
检索质量相关的遗留问题（门禁模糊带、三方式 CLI、CI 回归、评测集真实化等）未在本次处理，留待下次。

---

# 2026-09-09：真实制度文档 / 三态门禁 / 人工复核

> 原文件：2026-09-09-企业知识库助手开发记录.md

日期：2026-09-09 ~ 2026-09-10（连续工作，两日合并为一份）
项目目录：后端 `E:\EnterpriseKB`，前端 `E:\EnterpriseKBWeb`

## 一、工作总览

本次跨两日连续推进，分两个阶段：

- **09-09（实现）**：导入 5 篇真实制度文档并重建信号标定 → 三态防幻觉门禁 →
  人工复核流程 → 引用摘要修复；
- **09-10（规划）**：梳理三个待解决问题（问答记录持久化、知识库自动路由、
  文档类型与多模态解析）并给出方案，只记录、暂不实现。

## 二、真实文档与入库

用户提供 5 篇真实制度（`examples/` 下）：

| 文档 | 类型 | 文号 | 入库块数 |
|---|---|---|---|
| XX公司无形资产管理办法 | DOCX | 京咨发〔2026〕2号 | 7 |
| XX公司业务合同管理规定 | DOCX | 京咨发〔2026〕62号 | 13 |
| XX公司员工对外授课管理办法（试行） | PDF | 京咨发〔2025〕109号 | 5 |
| XX公司员工奖惩管理办法 | PDF | 京咨发〔2026〕19号 | 6 |
| XX公司员工招聘、录用管理办法 | PDF | 京咨发〔2026〕42号 | 6 |

入库知识库 `real`，共 5 文档 / 37 文本块（Embedding 用 Qwen text-embedding-v4，1024 维）。

## 三、管线修复（为真实文档扫清障碍）

### 3.1 DOCX 支持（`app/services/ingestion.py` + `app/routers/knowledge.py`）

- 新增 `read_docx()`：不引入 python-docx，直接解包 `word/document.xml`，
  按 `<w:p>` 段落提取 `<w:t>` 文本（表格单元格内段落一并捕获）；
- `read_document()` 与上传白名单 `ALLOWED_SUFFIXES` 增加 `.docx`。

### 3.2 PDF 文本规范化（`app/services/ingestion.py`）

- 新增 `normalize_text()`：NFKC 折叠康煕部首（`⼯→工`、`⾏→行` 等）+ 补充
  CJK Radicals Supplement 映射（`⻔→门`、`⻅→见`、`⺠→民` 等）+ 清理 `\x01` 等 C0 控制符；
- 效果：3 篇 PDF 提取文本残留偏旁/控制字符从 375+ 处归零，检索词面与向量质量恢复。

### 3.3 Embedding 批大小修复（`app/services/providers.py`）

- Qwen text-embedding-v4 单次批上限 10；原 `embed()` 一次发全部文本，
  13 块的大文档触发 400（"batch size should not be larger than 10"）；
- 改为按 `_EMBED_BATCH_SIZE=10` 分批请求再按原序拼接，调用方无感知。

## 四、真实评测集与信号分布

### 4.1 评测集（`eval/real-eval.yaml`）

37 题：29 事实（覆盖 5 篇文档、L1/L2/L3 难度）+ 8 无依据（年假/宿舍/补充医疗等
真实用户可能问、但 5 篇制度均未覆盖的主题）。

### 4.2 信号采集与二维分析

- 新增 `eval/collect_gate_signals.py`（采集 dense_dist/dense_sim/bm25/rerank 信号）；
- `eval/gate_2d_analysis.py` 泛化为可指定 TSV，并修复统计分母与 UTF-8 输出。

### 4.3 关键结论：真实数据上模糊带消失

| 指标 | 合成 50 题（此前） | 真实 37 题（本次） |
|---|---|---|
| 负面最高 rerank 分 | 0.0805（neg-001） | **0.0101**（neg-002） |
| 事实最低 rerank 分 | 0.0622（hr-103） | **0.4061**（ia-004） |
| 分差 | 0.0037（贴边） | **≈0.396（40 倍）** |
| 线性可分（dense+rerank） | 0.96 | **1.00** |

结论：此前“模糊带”是合成文档 + 自编负面带来的假象；在真实制度 + 真实无依据主题下，
Rerank 分数对“有依据/无依据”的区分干净利落。单阈值 `rerank_floor=0.05` 即可
100% 分离（8/8 负面拦截、0 误拦事实）。

## 五、三态防幻觉门禁（refuse / review / answer）

### 5.1 实现（`app/services/rag.py`）

- `should_refuse()`（bool）→ `gate_decision()`（返回 `refuse/review/answer`）；
- 分档规则（`config/models.yaml` 的 `retrieval.gate`）：
  - `rerank_floor=0.05`：重排分低于此 → **refuse**；
  - `rerank_review=0.3`：`[floor, review)` → **review**（作答但标记人工复核）；
  - `rerank_rescue=0.5`：重排分达到此 → **answer**（含“稠密弱但重排强”的救援）；
  - 介于 review 与 rescue 之间：稠密命中则 answer，否则 review；
  - 无 rerank（dense/hybrid 模式）→ 退化为稠密距离单阈值。
- `retrieve_context()` 返回 `(chunks, citations, context, decision)`；
  `ask()` 新增 `return_decision`，`return_chunks` 行为不变（评测不受影响）。

### 5.2 接口（`app/routers/chat.py`）

- `POST /chat` 响应新增 `decision` 字段；
- `POST /chat/stream` 的 `citations` / `done` 事件携带 `decision`，
  `refuse` 时不下发引用、只下发固定拒答文案。

### 5.3 配置（`app/core/config.py`）

- `RetrievalConfig` 与 `retrieval_config()` 新增 `rerank_review` 字段。

## 六、评测结果（真实知识库 real）

| 指标 | 数值 |
|---|---|
| 检索命中率（Citation Recall） | 29/29 = 1.0 |
| 事实覆盖率（Fact Accuracy） | 29/29 = 1.0 |
| 拒答正确率（No-Answer Acc） | 8/8 = 1.0 |
| 块级 Recall@5 / Hit@1 / Hit@3 | 1.0 / 0.9655 / 1.0 |
| 块级 MRR | 0.9828 |
| 难度分层 Recall@5 | L1 12/12 · L2 9/9 · L3 8/8 |

- 三态门禁在 36 题上全对：8 refuse / 0 review / 29 answer，无误判；
- CLI 冒烟：事实题正确作答带引用、无依据题正确拒答。

## 七、人工复核流程与前端

在“三态门禁”基础上补齐 `review` 状态的人工兜底闭环：

### 7.1 后端

- 新增 `app/models/review.py`：`review_items` 表（question/answer/knowledge_base/
  citations(JSONB)/decision/status(pending·approved·rejected)/asked_by/reviewer/
  review_note/created_at/reviewed_at）；
- 新增 `app/services/review.py`：`enqueue_review()`（尽力而为，不阻断问答）；
- 新增 `app/routers/review.py`：`GET /review/queue`（分页 + status 过滤）、
  `POST /review/{id}/resolve`（approve/reject，记录复核人/意见/时间，审计 `review_resolve`）；
- `app/routers/chat.py`：`/chat` 与 `/chat/stream` 在 `decision == "review"` 时自动入队
  （流式在结束后写入完整回答）。

### 7.2 前端

- `api.ts`：`ChatResponse.decision`、`chatStream` 的 `onDecision` 回调、
  `ReviewItem/ReviewQueueResponse` 类型与 `listReviewItems/resolveReviewItem`；
- `ChatPage.tsx`：`review` 回答显示“低置信度 · 已提交人工复核”徽标；
- 新增 `ReviewPage.tsx`（仅 admin）：待复核/已通过/已驳回页签、逐条通过/驳回 + 意见；
- `Layout.tsx`/`App.tsx`：侧边栏“人工复核”入口与 `/review` 路由（adminOnly）；
- `vite.config.ts`/`nginx.conf`：新增 `/review` 代理与反代；`styles.css` 增补样式。

### 7.3 验证

- 后端全部 `py_compile` 通过；前端 `bun run typecheck`、`bun run build` 通过；
- TestClient 端到端：review 带问答 → 自动入队 → 管理员查询/通过 → 普通用户 403，全部通过。

### 7.4 触发 review 的真实样例（Rerank 分落在 [0.05, 0.3)）

| 问题 | rerank 分 |
|---|---|
| 公司如何防范合同风险？ | 0.1678 |
| 员工报销的流程是什么？ | 0.1140 |
| 员工体检有哪些规定？ | 0.2276 |

## 八、引用摘要修复

- 问题：引用摘要固定取块的前 240 字，答案落在块中后部时，摘要与问题对不上
  （如问“200 万审批”，摘要却显示“示范文本”）；
- 修复：`app/services/rag.py` 新增 `_excerpt()`，改为**滑动窗口**取与问题词面最相关的
  一段（280 字，用问题分词给窗口打分，非整块时加省略号）；
  `retrieve_context()` 组装引用时改用 `_excerpt(c.content, question)`。

## 九、文件变更清单

**新增（后端）**：`app/models/review.py`、`app/services/review.py`、`app/routers/review.py`

**新增（评测/工具）**：`eval/real-eval.yaml`、`eval/collect_gate_signals.py`、
`eval/gate_2d_analysis.py`、`eval/real-gate-signals.tsv`、`eval/real-eval-result.json`、
`eval/real-eval-report.md`

**新增（文档）**：`待解决问题与解决方案`

**新增数据**：`examples/` 下 5 篇真实制度文档

**修改（后端）**：`app/core/config.py`、`app/core/database.py`、`app/models/__init__.py`、
`app/routers/__init__.py`、`app/routers/chat.py`、`app/routers/knowledge.py`、
`app/services/eval.py`、`app/services/ingestion.py`、`app/services/providers.py`、
`app/services/rag.py`、`config/models.yaml`

**修改（前端）**：`src/api.ts`、`src/App.tsx`、`src/components/Layout.tsx`、
`src/pages/ChatPage.tsx`、`src/styles.css`、`vite.config.ts`、`nginx.conf`，
新增 `src/pages/ReviewPage.tsx`

**修改（文档）**：`技术全景与学习手册`、`向量化与检索技术说明`

## 十、注意事项与遗留

1. `review` 状态当前仍“作答 + 标记”，人工审阅界面已就绪（`/review` 页），但前端
   对话页尚未做“复核进度”深度联动。
2. 阈值（0.05/0.3/0.5）由 37 题真实样本标定；后续样本增多或换 rerank 模型时，
   用 `eval/collect_gate_signals.py` 重采 `real-gate-signals.tsv` 复核。
3. 负面样本仅 8 条，偏“主题级无依据”；生产可能出现“近义改写、擦边”负面，
   落在 `[0.05, 0.3)` review 带，需观察是否需要人工复核兜底。
4. 文档仍不含 DOCX 页内图片/表格结构（仅正文段落文本）；扫描 PDF OCR 仍未支持。
5. 前后端两仓库改动均未 git commit。

## 十一、后续待解决问题与规划（09-10 梳理，暂不实现）

### 11.1 问答记录未持久化（切页即失）

- 现象：前端仅用 `useState` 保存消息，路由切换即丢；
- 方案：后端新增 `conversations` / `conversation_messages` 两表 + 会话 API，
  前端加「历史会话列表 + 新建会话」；
- 补充：用户所提“记忆模块”应拆两层——**会话持久化**（记录不丢、可审计）与
  **多轮记忆**（历史轮次喂回模型、支持追问），当前痛点属第一层，第二层单独立项。

### 11.2 提问前必须选知识库（体验差）

- 现象：`/chat` 的 `knowledge_base` 必填，员工不知道制度归哪个库；
- 方案：`knowledge_base` 改为**可选**，为空时**自动路由**——只在当前用户有读权限的
  库中检索，跨库聚合 top-k，引用标注来源库；前端默认「自动」，折叠「高级」手动入口；
- 补充：库少时「全库聚合检索」，库多时「先轻量选库 → 再精检」两阶段路由。

### 11.3 文档类型识别 + 图片/图表/表格解析

- 现状：已支持 `.md/.txt/.docx/文字型 PDF`；不支持 `.xlsx/.pptx/扫描 PDF`，
  图片/图表/表格未结构化；
- 方案：分层推进——先「格式扩展（xlsx/pptx）+ 表格转 Markdown 文本」，
  再「图片/扫描 OCR + 版面分析 + VLM 描述入库」，最后按需「视觉向量多路检索」；
  块类型扩展 `content_type`（text/table/image），图片块存描述 + 原图位置，引用可回看原图。

### 11.4 节奏建议

1. 优先并并行：11.1（会话持久化）与 11.2（自动路由），均无外部依赖、直接解决高频痛点；
2. 11.3 拆小迭代：先表格与格式扩展，再图片/扫描 OCR，视觉向量检索按需。

> 三问题详细设计（数据模型 / API / 技术路线 / 优先级）见 `待解决问题与解决方案`。

---

# 2026-09-11：会话持久化 / 分页与归档保留 / 知识库自动路由

> 原文件：2026-09-11-企业知识库助手开发记录.md

日期：2026-09-11
项目目录：后端 `E:\EnterpriseKB`，前端 `E:\EnterpriseKBWeb`

## 一、本次会话目标

1. 阅读既有开发记录，掌握项目进度与已实现能力；
2. 解决「问答记录切页即失」——实现会话持久化；
3. 为 Agent 规模变大做准备——落地消息分页、归档与保留策略；
4. 解决「提问前必须先选知识库」——实现知识库自动路由（跨库检索）；
5. 记录遗留问题并产出规模化演进技术文档。

## 二、完成项概览

| 类别 | 内容 | 状态 |
|---|---|---|
| 会话持久化 | `conversations` / `conversation_messages` 两表 + 会话 CRUD + 会话内问答（非流式/流式）落库 | ✅ |
| 前端会话 | 历史会话侧栏、新建/切换/删除、流式回答自动保存 | ✅ |
| 消息分页 | 游标分页（`before_id` + `limit`，返回 `total/has_more`）+ 前端「加载更早」 | ✅ |
| 归档/恢复 | `status`/`archived_at` + 归档/取消归档接口 + 列表过滤 + 前端「进行中/已归档」页签 | ✅ |
| 保留策略 | 自动归档 + 超期永久删除（连同消息）+ 启动/管理接口/CLI 三入口 | ✅ |
| 自动路由 | `knowledge_base` 可选 → 跨库检索 + 引用标注来源库 + 前端去选库固定自动 | ✅ |
| 技术文档 | 《会话持久化与规模化演进方案》 | ✅ |
| 遗留记录 | 保留策略定时任务（待部署 Linux 后处理） | ✅ |

## 三、会话持久化

### 3.1 数据模型（新增 `app/models/conversation.py`）

| 表 | 字段要点 | 用途 |
|---|---|---|
| `conversations` | user_id、title（首问截断）、knowledge_base（可空）、status（active/archived）、archived_at、created_at、updated_at | 会话元数据与生命周期 |
| `conversation_messages` | conversation_id、role（user/assistant）、content、citations(JSONB)、decision、created_at | 单轮消息（完整文本 + 引用 + 门禁判定） |

删除会话经 ORM 级联删除消息；归档为软状态，保留消息可恢复。

### 3.2 API（新增 `app/routers/conversations.py`）

- `GET /conversations`（列表，按 status 过滤）、`POST /conversations`（新建）；
- `GET /conversations/{id}`（详情 + 消息分页）、`DELETE /conversations/{id}`；
- `POST /conversations/{id}/archive`、`/unarchive`；
- `POST /conversations/{id}/chat`（非流式落库）、`POST /conversations/{id}/chat/stream`（流式落库）；
- `POST /conversations/retention/run`（仅 admin，执行保留策略）。

权限：普通用户仅能访问自己的会话（越权返回 404），管理员可查全部；问答仍走知识库读权限校验。

### 3.3 代码复用

- `app/services/rag.py` 抽出 `iter_answer_events()` 流式事件生成器，`/chat/stream` 与
  会话流式问答共用，消除重复；`/chat` 非流式回归通过。

## 四、消息分页与归档/保留

### 4.1 消息游标分页

- `GET /conversations/{id}?limit=&before_id=` 返回 `total / has_more / messages`（最新在前）；
- 采用 **keyset（游标）分页**而非 OFFSET：`before_id` 指向更早消息，对「实时追加新消息」免疫，
  避免 OFFSET 在新增消息后发生漂移；
- 前端 `ChatPage` 顶部「↑ 加载更早消息」按游标向前翻页。

### 4.2 归档

- 软归档：`status=archived` + `archived_at`，消息保留、可随时恢复；
- 列表按 `status=active|archived|all` 过滤，前端「进行中 / 已归档」页签切换；
- 归档会话再发消息时自动恢复为 `active`（服务端在问答落库前处理）。

### 4.3 保留策略（新增 `app/services/conversation.py`）

- `apply_retention()` 一次执行两阶段：
  1. `CONVERSATION_ARCHIVE_DAYS`：活跃会话超期未更新 → 自动归档；
  2. `CONVERSATION_RETENTION_DAYS`：归档会话超期 → 永久删除（先删消息再删会话，保证级联）；
- 两变量未配置（或 ≤0）时对应步骤跳过；
- 触发入口：后端启动钩子（`app/api.py` lifespan，仅配置后执行）、管理接口
  `POST /conversations/retention/run`、命令行 `python -m app.cli retention`；
- 新增入口脚本 `scripts/run-retention.ps1` 供系统定时任务调用。

### 4.4 轻量迁移

- `app/core/database.py` 的 `init_db()` 在 `create_all` 后执行幂等
  `ALTER TABLE conversations ADD COLUMN IF NOT EXISTS status/archived_at` 与索引，
  旧库无需重建、新库自动建列。

## 五、知识库自动路由（跨库检索）

### 5.1 后端

- `app/routers/deps.py` 新增：
  - `readable_knowledge_bases(user)`：当前用户有读权限的全部库（admin=全部）；
  - `resolve_kb_names(user, requested, fallback)`：解析为「单库」或「自动路由候选列表」，
    权限校验不变（无读权限库不会进入候选）。
- `app/services/retrieval.py` 新增 `multi_hybrid_search()`：跨库检索——
  稠密全局召回 + 每库 BM25 召回，**rerank 全局精排**（交叉编码器跨库可比）；
  无 rerank 时退化为余弦距离全局排序（距离是绝对度量）。
- `app/services/rag.py`：`Citation` 增加 `knowledge_base` 字段；`retrieve_context` 支持
  `kb_names` 走跨库检索；`ask` / `iter_answer_events` 透传。
- `app/schemas/__init__.py`：`ChatRequest.knowledge_base` 改为可选（空 / `"auto"` = 自动路由）；
- `app/routers/chat.py` 与 `app/routers/conversations.py`：接入 `resolve_kb_names`，
  自动路由时会话不绑定具体库（`knowledge_base=None`），引用/审计标注 `auto`。

### 5.2 前端

- `ChatPage` 去掉知识库选择器，**固定自动路由**；新建会话 `knowledge_base=null`、问答传 `auto`；
- 引用来源显示 `[库名] 文件名…`；会话列表与页头标注「自动路由 / 自动跨库检索」。

## 六、技术文档与遗留记录

- 新增 `会话持久化与规模化演进`：按规模阈值（10万/100万/1000万）给出分级演进方案
  （时间分区、引用去重、冷热分层、消息体外置、分区/分库分表、异步落库、独立检索、长期记忆治理）。
- 更新 `待解决问题与解决方案`：
  - 「问题二 选库」标注已实现；
  - 新增「问题四：会话保留策略定时任务（遗留，待部署 Linux 后处理）」。

## 七、文件变更清单

**新增（后端）**：`app/models/conversation.py`、`app/routers/conversations.py`、
`app/services/conversation.py`、`scripts/run-retention.ps1`

**新增（文档）**：`会话持久化与规模化演进`

**修改（后端）**：`app/api.py`、`app/cli.py`、`app/core/config.py`、`app/core/database.py`、
`app/models/__init__.py`、`app/routers/__init__.py`、`app/routers/chat.py`、
`app/routers/deps.py`、`app/schemas/__init__.py`、`app/services/rag.py`、
`app/services/retrieval.py`、`.env.example`

**修改（文档）**：`待解决问题与解决方案`

**修改（前端）**：`src/api.ts`、`src/pages/ChatPage.tsx`、`src/styles.css`、`vite.config.ts`、`nginx.conf`

## 八、验证结果

- 后端 `compileall`、前端 `tsc --noEmit`、`vite build` 均通过；
- 建表/迁移：`conversations` / `conversation_messages` 创建成功，旧库 `status/archived_at` 列补齐；
- TestClient：
  - 会话 CRUD、跨用户隔离（404）、管理员可见——通过；
  - 消息游标分页（`total/has_more/before_id` 翻页）——通过；
  - 归档/恢复/列表过滤——通过；
  - 保留策略：过期活跃会话→归档、过期归档会话→连同消息永久删除——通过；
  - 自动路由：admin 自动=3 库、alice 自动仅 hr、alice 指定无权限库=403、bob 无库=400；
- 端到端（真实模型）：
  - `/chat` 不传库 → 跨库命中 `real`，返回正确文号「京咨发〔2026〕2号」，引用带 `knowledge_base`；
  - 会话内流式自动路由 → 回答落库、会话 `knowledge_base=None`、引用标注来源库。

## 九、当前状态

| 组件 | 状态 |
|---|---|
| pgvector 容器 | `enterprisekb-postgres-1` healthy，`5432:5432` |
| 后端 FastAPI | 运行中（非沙箱），`http://127.0.0.1:8000` |
| 前端 Vite | 运行中，`http://127.0.0.1:5173` |
| 知识库 | `hr` / `enterprise` / `real`（5 文档 / 37 块） |
| 会话 | 已启用持久化 + 分页 + 归档 + 保留策略（保留阈值未配置，默认关闭） |
| 自动路由 | 已启用，前端默认自动、跨库检索 |

## 十、遗留与待办

| # | 待办 | 说明 |
|---|---|---|
| 1 | 代码未提交 | 本次及 09-03 起多轮改动均未 git commit |
| 2 | 保留策略定时任务 | 部署 Linux 后挂 crontab，或引入 APScheduler（详见问题四） |
| 3 | 多轮记忆（第二层） | 会话持久化已做，跨轮追问/长期记忆未做 |
| 4 | 文档解析扩展 | xlsx/pptx/扫描 PDF/图片图表（问题三，分阶段） |
| 5 | 历史遗留 | SSO 真实 IdP 实测、BM25 换 jieba、pytest/CI、部署镜像纳入模型分发、Docker #531/#532 跟踪 |

---

# 2026-09-13：会话内短期记忆 / 证据窗口 / 前端记忆徽标

> 原文件：2026-09-13-企业知识库助手开发记录.md

日期：2026-09-13
项目目录：后端 `E:\EnterpriseKB`，前端 `E:\EnterpriseKBWeb`

## 一、本次会话目标

1. 阅读既有开发记录，掌握项目进度与已实现能力；
2. 梳理「记忆」的分层与各场景方案，沉淀技术文档，供后续查阅与选型；
3. 实现**会话内短期记忆**（方案 D：查询改写 + 短窗口历史 + 引用复用）；
4. 前端增加**记忆徽标**，让多轮记忆行为可观察；
5. 实现方案 A **证据窗口**（reuse 时合并最近 N 轮检索块，而非只复用上一轮）；
6. 启动整套项目供本机验证，并修复验证中暴露的问题。

## 二、完成项概览

| 类别 | 内容 | 状态 |
|---|---|---|
| 记忆策略文档 | 《记忆策略与方案》：分层总览 + 四个场景方案矩阵 + 方案 D 判定规则 + 升级方案 A–F | ✅ |
| 会话内短期记忆 | 查询改写（new/followup/reuse）+ 短窗口历史 + 引用复用 + 三条确定性兜底 | ✅ |
| 引用复用判定 | 「是否引入新证据需求」为核心判据 + 兜底降级 | ✅ |
| 证据窗口 | reuse 合并最近 N 轮检索块（按块去重，默认 3 轮） | ✅ |
| 前端优化 | 记忆徽标（追问·已改写检索 / 复用上文资料）+ 落库持久化 | ✅ |
| 问题修复 | reuse 缺历史导致「展开讲讲刚才那条」无法消解指代 → 回答 prompt 补对话历史 | ✅ |
| 项目启动 | Docker + pgvector + 后端（非沙箱）+ 前端，本机可验证 | ✅ |
| 技术文档 | 《记忆策略与方案》+《待解决问题与解决方案》状态更新 | ✅ |

## 三、记忆分层与选型（详见 记忆策略与方案）

### 3.1 分层总览

| 层次 | 记住多久 | 现状 |
|---|---|---|
| 会话持久化 | 永久（落库） | ✅（09-11） |
| 会话内短期记忆 | 单次会话内 | ✅（本次，方案 D + 方案 A） |
| 跨会话长期记忆 | 永久、跨会话 | ❌ 待选型 |
| 知识沉淀 | 永久、进知识库 | ❌ 待选型 |

### 3.2 方案 D 判定规则（何时复用引用）

改写 LLM 每次输出 `{"type":"new|followup|reuse","query":"..."}` 三分类：

- `new`：与历史无关 / 首问 → 原问题重检，引用全新；
- `followup`：指代前文 + 引入新对象/金额/条件/时间 → 改写为完整问题重检，引用全新；
- `reuse`：仅对上一轮回答加工（展开/举例/总结/翻译等），无新证据需求 → 复用证据与引用。

**核心判据**：本轮是否引入了「新的证据需求」。三条确定性兜底：

1. 无历史（首条）→ 强制 `new`；
2. 判 `reuse` 但最近一轮无证据（`context_chunks` 为空，如上一轮拒答）→ 降级 `followup`；
3. LLM 输出解析失败 / 类型非法 → 降级 `new`（原问题直检，问答永不因改写而中断）。

### 3.3 升级方案清单（用量上来后再评估）

| 方案 | 做法 | 状态 |
|---|---|---|
| A. 证据窗口 | 最近 N 轮检索块去重合并 | ✅ 本次已实现 |
| B. 结构化指代消解 | 改写输出 `ref_rounds/hops`，按轮次聚焦证据 | 待选型 |
| C. 滚动摘要 + 关键事实条 | 长对话压缩 + 关键数字/条款结构化保留 | 待选型 |
| D. 结构化对话状态（槽位） | 话题槽位，追问先填槽再检索 | 待选型 |
| E. 跨会话长期记忆 | 结构化画像 / 向量记忆库 / 知识沉淀 | 待选型 |
| F. 通用工程做法 | 记忆即检索 / 自动压缩 / Graph | 参考 |

## 四、会话内短期记忆实现（方案 D）

### 4.1 新增 `app/services/memory.py`

- `build_turn_plan(session, conversation_id, question, chat_config, before_id)`：取最近
  `max_history_messages` 条历史 → 改写 LLM 判定三分类 → 生成 `TurnPlan`（含改写后 query、
  reuse 证据、意图类型、历史）；
- `_rewrite()` / `_parse_rewrite()`：调用聊天模型做改写判定，从返回文本中稳健解析 JSON；
  解析失败返回空 dict（由兜底降级）；
- 改写提示词明确「reuse 唯一标准 = 只用上一轮已检索资料即可回答；引入新事实即 followup」。

### 4.2 数据模型变更

`conversation_messages` 新增两列（`init_db()` 幂等 `ALTER TABLE` 补齐旧库）：

| 列 | 用途 |
|---|---|
| `context_chunks`（JSONB） | 本轮检索到的完整文本块（含 knowledge_base/filename/page_number/chunk_index/content/excerpt），供 reuse 复用原文 |
| `memory`（VARCHAR 20） | 本轮记忆意图（new/followup/reuse），前端据此展示徽标 |

### 4.3 rag.py 增改

- `serialize_chunks()` / `context_from_stored_chunks()` / `citations_from_stored_chunks()`：
  检索块与引用的落库序列化与复用重建；
- `answer_from_stored()` / `iter_answer_reuse_events()`：reuse 场景基于已有证据作答（流式/非流式）；
- `build_reuse_user_prompt()`：reuse 提示词 = 资料 + 对话历史（仅用于理解所指）+ 当前问题；
- `iter_answer_events()`：新增内部 `chunks` 事件（供会话落库，路由层消费、不下发前端）。

### 4.4 路由接入（conversations.py / chat.py）

- 会话内问答（非流式/流式）在保存用户消息后调用 `build_turn_plan` 生成计划；
- 按计划分支：`new/followup` 走 `ask`/`iter_answer_events`（用改写后 query 检索并落库 chunks）；
  `reuse` 走 `answer_from_stored`/`iter_answer_reuse_events`（复用证据，落库沿用引用与 decision）；
- 非流式响应与审计日志新增 `memory` 字段；`/chat/stream`（非会话）忽略内部 `chunks` 事件。

## 五、方案 A：证据窗口

- 新配置 `memory.evidence_window_rounds`（默认 3）；
- `memory.py` 新增 `merge_chunk_lists()`（去重键 `(knowledge_base, filename, chunk_index)`，
  最新一轮在前）与 `_recent_evidence_windows()`；
- `reuse` 分支由「复用最近一条 assistant 的 context_chunks」改为「合并最近 N 轮并去重」，
  支持「对比刚才两条 / 综合上面几轮」类追问。

## 六、前端优化

- `src/api.ts`：`ChatStreamCallbacks` 新增 `onMemory`；`postSse` 解析 SSE `citations` 事件中的
  `memory` 字段；`ConversationMessage` 新增 `memory` 字段；
- `src/pages/ChatPage.tsx`：消息体渲染记忆徽标——`↻ 追问 · 已改写检索`（followup）与
  `📎 复用上文资料`（reuse）；`toMessage` / 流式回调接入 `memory`；
- `src/styles.css`：新增 `.memory-badge` 样式（followup/reuse 两色）。

徽标随消息落库持久化，切页/刷新后仍保留。

## 七、验证中发现并修复的问题

- 现象：输入「展开讲讲刚才的那条」，徽标正确显示「复用上文资料」，但模型反问“请指明是哪一条”。
- 根因：reuse 分支只把上一轮「检索原文块」喂给模型，未把「上一轮对话（问/答）」一并带入，
  模型无法消解“刚才的那条”的指代。
- 修复：新增 `build_reuse_user_prompt()`，reuse 回答 prompt 带入最近 `max_history_messages` 条
  对话历史，并加约束“历史仅用于理解当前问题所指，回答仍须以资料为准”。

## 八、文件变更清单

**新增（后端）**：`app/services/memory.py`

**新增（文档）**：`记忆策略与方案`、`2026-09-13-企业知识库助手开发记录.md`（本文件）

**修改（后端）**：`app/core/config.py`、`app/core/database.py`、`app/models/conversation.py`、
`app/routers/chat.py`、`app/routers/conversations.py`、`app/services/rag.py`、`config/models.yaml`

**修改（文档）**：`待解决问题与解决方案`（问题一状态更新）

**修改（前端，今日）**：`src/api.ts`、`src/pages/ChatPage.tsx`、`src/styles.css`

> 前端另有 09-09~09-11 起未提交的历史改动：`README.md`、`nginx.conf`、`src/App.tsx`、
> `src/components/Layout.tsx`、`vite.config.ts`，及未跟踪新增 `src/pages/AuditLogPage.tsx`、
> `EvalPage.tsx`、`ReviewPage.tsx`。

## 九、验证结果

- 后端 `compileall` 通过；`memory/rag/routers` 导入正常；`memory_config()` 读取正常；
- 纯函数冒烟：`citations_from_stored_chunks` / `context_from_stored_chunks` / `_parse_rewrite`
  （含代码块包裹、非法 JSON 兜底）/ `merge_chunk_lists`（去重）通过；
- 数据库：`conversation_messages` 已补 `context_chunks`、`memory` 两列（`init_db` 幂等 ALTER）；
- 前端 `bun run typecheck` 通过；`vite build` 通过（本机沙箱下 `bun run build` 偶发 EPERM，
  改用 `node_modules\.bin\vite.exe build` 验证，属环境问题非代码问题）；
- 端到端（本机用户实测）：`new` / `followup` / `reuse` 三分类在审计日志中均正确记录；
  修复后 reuse 可正确消解“刚才那条”指代并展开。

## 十、当前状态

| 组件 | 状态 |
|---|---|
| pgvector 容器 | `enterprisekb-postgres-1` healthy，`5432:5432` |
| 后端 FastAPI | 运行中（非沙箱），`http://127.0.0.1:8000` |
| 前端 Vite | 运行中，`http://127.0.0.1:5173` |
| 会话内记忆 | 方案 D + 方案 A 已启用（`memory.enabled=true`，窗口 6 消息 / 证据 3 轮） |
| 记忆徽标 | 前端已展示（followup / reuse） |

## 十一、遗留与待办

| # | 待办 | 说明 |
|---|---|---|
| 1 | 代码未提交 | 今日后端/前端改动均未 git commit；前端另有 09-09 起历史未提交改动 |
| 2 | 方案 B 结构化指代消解 | 改写输出 `ref_rounds/hops`，让证据窗口按轮次聚焦（待用量上来后评估） |
| 3 | 证据窗口相关性 | 当前按「轮数」而非「语义相关性」合并，无关轮次可能被带入（与 B 关联） |
| 4 | 跨会话长期记忆 | 结构化画像 / 向量记忆库（方案 E） |
| 5 | 知识沉淀 | 对话 → 知识库（人工标记→审核→入库） |
| 6 | 历史遗留 | 保留策略定时任务、SSO 真实 IdP 实测、pytest/CI、BM25 换 jieba、文档解析扩展（xlsx/pptx/扫描 PDF）、部署镜像纳入模型分发、Docker #531/#532 跟踪 |

---

# 2026-09-15：文档解析扩展 / 多格式与 PDF 表格结构化

> 原文件：2026-09-15-企业知识库助手开发记录.md

日期：2026-09-15
项目目录：后端 `E:\EnterpriseKB`，前端 `E:\EnterpriseKBWeb`

## 一、本次会话目标

在既有 `.md / .txt / .docx / 文字型 PDF` 解析基础上，按 `待解决问题与解决方案`
「问题三」的分层方案，落地**第一层：格式扩展 + 第二层：表格结构化（含 PDF 内嵌表格）**，
并把**扫描 PDF OCR** 做成可选能力（装即用、不强制重依赖）、补齐**混合 PDF 扫描页提示**。

## 二、完成项概览

| 类别 | 内容 | 状态 |
|---|---|---|
| XLSX | openpyxl 逐 sheet 转 Markdown 表格（带 sheet 名标题，过滤空行） | ✅ |
| PPTX | python-pptx 逐页提取标题/正文/表格（表格转 Markdown） | ✅ |
| HTML / EPUB | 标准库 HTMLParser 提取正文 + 表格，EPUB 按 OPF spine 顺序 | ✅ |
| DOCX 表格 | 解析重写为 XML 树：按文档顺序识别段落与 `<w:tbl>`，表格转 Markdown，不再拍平 | ✅ |
| PDF 表格结构化 | PyMuPDF：有边框（lines）+ 无边框（词坐标列对齐，≥3 列），表格内文字不重复入正文 | ✅ |
| 合并单元格 | DOCX `gridSpan`/`vMerge` 展开填充；XLSX `merged_cells` 回填 | ✅ |
| 块类型 | `document_chunks.content_type`（text / table），旧库幂等 `ALTER TABLE` 补齐 | ✅ |
| 表格切块 | `chunk_table_text()`：超长表格按完整行切分，保持行列结构 | ✅ |
| 扫描 PDF OCR | 可选能力：延迟导入 `rapidocr-onnxruntime`（渲染走 PyMuPDF），装即自动识别 | ✅ |
| 混合 PDF 提示 | 部分扫描页未识别时返回 warning（CLI / 前端展示），不再静默跳过 | ✅ |
| 上传白名单 | 后端 `ALLOWED_SUFFIXES` 与前端文件选择器同步扩展 | ✅ |
| 文档 | README、`向量化与检索技术说明`、`待解决问题与解决方案` 更新 | ✅ |

## 三、关键实现路径

### 3.1 统一解析结果：`ParsedBlock`

`app/services/ingestion.py` 新增 `ParsedBlock(text, page_number, content_type)`，
所有 `read_*` 函数统一返回 `list[ParsedBlock]`，替代原先的 `list[tuple[str, int|None]]`。
`content_type` 区分 `text` / `table`，供切块与入库区分处理。

### 3.2 各格式 reader

- `read_xlsx()`：`openpyxl.load_workbook(read_only=True, data_only=True)`，逐 sheet 读有效行，
  `_table_to_markdown(rows, caption=sheet 名)`；空行自动过滤；
- `read_pptx()`：`Presentation()`，逐 slide 遍历 shape——先判 `has_table`（表格转 Markdown），
  再判 `has_text_frame`（提取段落 runs），每页一个块（`page_number=页码`）；
- `read_docx()`：改用 `xml.etree.ElementTree` 解析 `word/document.xml`，按 body 子元素顺序
  遍历 `<w:p>`（段落）与 `<w:tbl>`（表格），两者顺序与原文一致；
- `read_html()` / `read_epub()`：`_HTMLTextExtractor`（标准库 `HTMLParser`）跳过
  script/style/noscript，保留标题、段落与 `<table>`；EPUB 先解 container.xml → OPF，
  解析 manifest（id→href）与 spine（itemref 顺序），按 spine 读各 XHTML 章节，
  未在 spine 的章节按文件名排序追加；
- `read_pdf()`：优先 PyMuPDF——有边框表格走 `find_tables()`（lines），无边框表格走
  词坐标列对齐（`_detect_borderless_tables`），再 `get_text("blocks")` 提取正文
  （排除表格内文本避免重复），正文与表格按页面坐标交错保持阅读顺序；
  扫描页尝试 `_ocr_page()`（可选）；PyMuPDF 不可用时降级 pypdf 纯文本提取。

### 3.3 表格转 Markdown 与切块

- `_table_to_markdown()`：第一行视为表头，处理不等长行（补空）、过滤全空行，输出
  `| A | B |` + `|---|---|` 结构；可选 caption（xlsx 的 sheet 名）；
- `chunk_table_text()`：表格块不参与普通标点切块，超长时**按完整行**累加切分，
  避免拆散行列；单行超长（超宽单元格）时才硬切兜底。

### 3.4 `content_type` 落库

- `app/models/knowledge.py`：`DocumentChunk` 新增 `content_type`（String(20)，默认 "text"）；
- `app/core/database.py`：`init_db()` 幂等 `ALTER TABLE document_chunks
  ADD COLUMN IF NOT EXISTS content_type VARCHAR(20) NOT NULL DEFAULT 'text'`，旧库自动补齐；
- `ingest_file()`：按 `ParsedBlock.content_type` 分派切块，写入 `content_type` 字段。

### 3.5 扫描 PDF OCR（可选能力）与混合 PDF 提示

- `_ocr_page(page)`：接收已打开的 PyMuPDF 页面，渲染位图后延迟导入 `rapidocr_onnxruntime`
  做识别；首次懒加载 `RapidOCR()` 并模块级缓存；依赖未安装/识别异常返回 `None`，不阻断主流程；
- 整篇无可提取文字时抛出明确提示（建议 `pip install rapidocr-onnxruntime`）；
  混合 PDF 的部分扫描页通过 `warnings_out` 返回警告（含页码），CLI 打印、前端展示；
- `_find_tables()`：吞掉 PyMuPDF 首次 `find_tables()` 打到 stdout 的一次性提示，
  避免污染审计日志 JSON 行。

### 3.6 警告传递链路

`read_document(path, warnings_out)` → `read_pdf(path, warnings_out)` 收集非致命警告，
`ingest_file(..., warnings_out=...)` 透传；CLI `ingest` 打印 `WARNING:`，上传接口在
响应中返回 `warnings` 数组，前端上传结果里以 ⚠️ 展示。

### 3.7 无边框表格检测（自研，`_detect_borderless_tables`）

- `get_text("words")` 取词坐标 → 按 y 聚类成行 → 按「相对水平间隙」切单元格
  （以行内间隙中位数做阈值，区分列间空隙与单元格内空格，避免「1000 元/晚」被拆列）
  → 连续多列行分组 → 跨行列锚点聚类（`_cluster_1d`）→ 按锚点填充（允许空单元格）；
- **门槛 ≥3 列**：编号列表（「1. 内容」）与文档标题等 2 列形态极易误判，宁漏勿错；
  2 列表格文字仍会作为正文入库；
- 实测：examples/ 中「XX公司员工招聘.pdf」的编号列表曾被误判 25 个表格，加门槛后归零。

### 3.8 合并单元格处理

- DOCX：解析 `<w:gridSpan>`（水平跨列，复制展开）与 `<w:vMerge>`（垂直合并，
  restart 值向下填充到 continue），展平成规则二维表；
- XLSX：改用非 read_only 模式（read_only 无 `merged_cells`），对每个合并区域把
  左上角值回填到区域内所有单元格；
- PDF / PPTX / HTML 的合并单元格暂为保守处理（见「注意事项」），原因见技术文档 3.4。

## 四、文件变更清单

**修改（后端）**：`app/services/ingestion.py`（重写：多格式解析 + PDF 表格结构化 + 可选 OCR + 警告）、
`app/models/knowledge.py`（content_type）、`app/core/database.py`（幂等迁移）、
`app/routers/knowledge.py`（白名单 + 上传返回 warnings）、`app/cli.py`（ingest 打印警告）、
`requirements.txt`（openpyxl / python-pptx / pymupdf，rapidocr 注释）

**修改（前端）**：`E:\EnterpriseKBWeb\src\pages\AdminPage.tsx`（accept + 提示语 + 警告展示）、
`E:\EnterpriseKBWeb\src\api.ts`（UploadResult.warnings）

**修改（文档）**：`README.md`、`向量化与检索技术说明`、`待解决问题与解决方案`、
新增 `AGENTS.md`（工程化约定）、`文档解析与入库技术说明`（链路技术文档）

## 五、验证结果

- 后端 `compileall` 全部通过；关键模块导入正常，`DocumentChunk.content_type` 列定义正确；
- 解析单测（临时脚本，验证后已清理）：`_table_to_markdown` / xlsx / pptx / docx（含表格）/
  html / epub / `chunk_table_text`（超长表格按行切块）全部通过；
- `_split_blocks` 验证：text/table 块正确分派、content_type 正确标注；
- PDF 表格结构化验证（临时脚本，验证后清理）：自造「正文 + 中文表格 + 空白页」PDF，
  表格正确转 Markdown、表格文字不重复入正文、正文/表格按阅读顺序交错、第 3 空白页触发警告；
- 真实 PDF 回归：examples/ 下 4 篇 PDF（it-policy + 3 篇真实制度）全部正常解析、无表格误检；
- 无边框表格：自造 3 列无边框表格（含空格单元格、空单元格、正文干扰）正确检测，
  且表格文字不重复入正文；
- 合并单元格：DOCX（gridSpan + vMerge）与 XLSX（水平 + 垂直合并）均正确展开填充；
- PyMuPDF 首次 `find_tables()` 的 stdout 提示已被抑制（不影响审计日志 JSON 行）；
- 前端 `bun run typecheck` 通过；
- 依赖 `openpyxl 3.1.5`、`python-pptx 1.0.2`、`pymupdf 1.28.2` 已装入 `.venv`。

## 六、注意事项与遗留

1. **数据库迁移**：`content_type` 列的幂等 `ALTER TABLE` 会在下次 `init_db()`（后端启动 /
   `python -m app.cli init-db`）时自动补齐旧库；本次会话环境数据库未运行，未做真实库的
   端到端入库验证，建议下次启动项目时用一份 xlsx/docx 实测确认。
2. **OCR 只差 rapidocr**：pymupdf 已装（渲染 + 表格），`rapidocr-onnxruntime` 未装，
   扫描 PDF 当前仍只会提示；装好后即可自动识别（onnxruntime 与现有 numpy/torch 的
   兼容性需在安装时确认）。
3. **无边框表格已自研解决**：词坐标列对齐 + ≥3 列门槛，规整对齐表格可检测；2 列
   无边框表格与 PDF/PPTX/HTML 合并单元格仍待后续（见 `文档解析与入库技术说明`）；
4. **图片/图表结构化、视觉向量检索**：仍属问题三第三层，未实现；
5. **历史遗留**：本次及 09-13 起多轮改动仍均未 git commit。

---

# 2026-09-17：文档入库工程化 + 前端联动

> 原文件：2026-09-17-企业知识库助手开发记录.md

日期：2026-09-17
项目目录：后端 `E:\EnterpriseKB`（本次不涉及前端）

## 一、本次会话目标

依据 `文档入库工程化技术指标` 的优先级分级，落地文档入库的 **P0 止血项**：

1. #9 超长表格切块后丢表头；
2. #12 上传仅扩展名白名单、不校验内容；
3. #15 解析缺结构化指标；
4. #20 `content_type` 迁移未在真实库验证；
5. #21 缺 `reingest` CLI（旧库按新策略重导）；
6. #22 缺 `rebuild` CLI（重建索引 / 换 Embedding）。

## 二、完成项概览

| # | 内容 | 状态 |
|---|---|---|
| 9 | `chunk_table_text` 超长表格按完整行切块，后续块重复「表头 + 分隔行」 | ✅ |
| 12 | `sniff_format` / `validate_file_type`：magic bytes + 内部结构嗅探，扩展名交叉校验 | ✅ |
| 15 | `IngestStats` / `ParseStats`：`format/text_chunks/table_chunks/skipped_pages/ocr_pages/parse_ms` 进审计 extra | ✅ |
| 20 | 真实库端到端验证：`content_type` 列幂等迁移 + 含表格文档 `table` 块正确入库 | ✅ |
| 21 | `python -m app.cli reingest <dir> -k <kb>`：按文件名替换同名文档重导 | ✅ |
| 22 | `python -m app.cli rebuild <dir> -k <kb>`：清空库按当前配置重建（可换 Embedding） | ✅ |

## 三、关键实现

### 3.1 表格补表头（#9）

`app/services/ingestion.py` 的 `chunk_table_text()` 重写：定位表头行（首个以 `|` 开头的行）
与分隔行（仅含 `| - : `），超长切块时从第二块起前置重复表头 + 分隔行；首块保留 caption
（`【sheet 名】`）等前缀。非标准表格退回 `_chunk_lines()` 普通按行切块兜底。

### 3.2 内容嗅探（#12）

新增 `sniff_format()` / `_sniff_zip()` / `_sniff_text_markup()` / `validate_file_type()`：
- PDF 魔数 `%PDF-`；ZIP 容器按内部结构区分 docx/xlsx/pptx/epub；
- HTML 严格匹配 `<!doctype html` / `<html` 前缀，避免误伤含代码示例的 md；
- `validate_file_type()` 在 `ingest_file_detailed()` 入口执行，对「明确签名与扩展名不符」的文件
  抛 `ValueError` 拒绝；纯文本类（md/txt）无可靠签名，放行由 `read_*` 处理。

### 3.3 结构化指标（#15）

- 新增 `ParseStats`（skipped_pages / ocr_pages）贯穿 `read_document → read_pdf`，解析层直接计数，
  不再靠解析警告字符串反推；
- 新增 `IngestStats`，`ingest_file_detailed()` 返回 `(chunks, stats)`，`ingest_file()` 保留原签名
  仅返回 chunks（向后兼容）；
- 上传审计 `extra` 与上传响应体新增 `format/text_chunks/table_chunks/skipped_pages/ocr_pages/parse_ms`；
- CLI `ingest` 打印 `[text=.., table=.., parse=..ms, ...]`，并对 `ValueError/RuntimeError` 友好报错。

### 3.4 reingest / rebuild（#21 / #22）

新增 `rebuild_knowledge_base()` 与 CLI 命令：
- `reingest`（`replace_embedding=False`）：保持库的 Embedding 配置，按文件名替换同名文档；
  Embedding 配置不一致时报错引导改用 `rebuild`；
- `rebuild`（`replace_embedding=True`）：ORM 逐条删除清空库（触发 relationship 级联删除文本块，
  因表无 `ON DELETE CASCADE`），更新 embedding 模型/维度后按目录全量重建；
- 文件级事务：先标记删除同名旧文档、再入库，同一事务提交；单文件失败 `session.rollback()`
  回滚、旧文档不变，错误记入 `RebuildResult.errors`，不阻断其余文件。

## 四、文件变更清单

**修改（后端）**：`app/services/ingestion.py`（表格补表头 + 内容嗅探 + 结构化指标 +
`ingest_file_detailed` / `rebuild_knowledge_base`）、`app/routers/knowledge.py`
（复用 `ALLOWED_SUFFIXES`、审计 extra 指标）、`app/cli.py`（ingest 指标输出 + reingest/rebuild 命令）

**新增（文档）**：`文档入库工程化技术指标`、`2026-09-17-企业知识库助手开发记录.md`（本文件）

**修改（文档）**：`文档解析与入库技术说明`（#9/#12/#15/#20/#21/#22 标记已解决）、
`待解决问题与解决方案`（问题三状态更新）

## 五、验证结果

- 后端 `compileall` 全部通过；`app.services.ingestion` / `app.cli` / `app.routers.knowledge` 导入正常；
- 纯函数冒烟：`chunk_table_text` 超长表格（40 行）切 14 块且每块含表头；`sniff_format` 识别
  PDF 魔数、`validate_file_type` 拒绝「PDF 伪装 .txt」；`_split_blocks` 正确分派 text/table；
- 真实库端到端（Docker pgvector，容器 `enterprisekb-postgres-1` healthy）：
  - `init_db` 幂等迁移后 `document_chunks.content_type` 为 `varchar`、默认 `'text'`、非空；
  - 旧库 56 个块均为 `text`（符合「旧库无法区分表格」的遗留描述）；
  - 含表格 xlsx 入库 `text=0, table=1, parse=164ms`，库内 `content_type='table'` 正确；
  - `reingest`（2 文件替换+新增）与 `rebuild`（清空重建）均成功，验证后清理测试库。

## 六、P1（第一批）实现

本批聚焦高价值、风险可控的正确性项；PDF 版面类（#1/#3/#4/#5）与大文件内存（#13/#26）
及 #7/#25 留待后续批次。

| # | 内容 | 状态 |
|---|---|---|
| 24 | 编码探测：UTF-8 → charset-normalizer → GB18030，覆盖 GBK/Big5/UTF-16 | ✅ |
| 2 | PPTX 合并单元格（DrawingML `gridSpan/rowSpan`）+ HTML 合并单元格（`rowspan/colspan`） | ✅ |
| 11 | 表格引用摘要「表头 + 命中行」，按命中分数排序 | ✅ |
| 14 | 上传失败审计 + `ProviderError` 统一封装模型调用异常 | ✅ |
| 10 | 表格 overlap 策略：明确「按完整行切分不重叠，靠重复表头保语义」 | ✅ |

### 关键实现

- `_decode_text_file()`：UTF-8 严格解码 → charset-normalizer 智能探测 → GB18030 兜底；
  `read_html` 与 `read_document` 的 md/txt 分支统一走该函数（解决 GB18030 先“吞掉”Big5 的问题）；
- `_pptx_table_markdown()`：解析 DrawingML `a:tbl`，`gridSpan` 横向复制、`rowSpan` 经 occupied
  网格向下回填；`read_pptx` 顺带修复「表格被标为 text」问题，按 text/table 分块并保持顺序；
- `_expand_html_table()` + `_HTMLTextExtractor`：`td/th` 读取 `colspan/rowspan`，展平成规则网格；
- `rag.py` `_excerpt()` 表格分支：`_looks_like_table()` 识别 Markdown 表格，`_excerpt_table()`
  保留「表头 + 分隔行」，按问题词面命中分数降序选行，避免通用词（如“部门”）掩盖精确命中行；
- `providers.py` 新增 `ProviderError(RuntimeError)`，embed/chat/chat_stream 包装 `OpenAIError`；
  `knowledge.py` 上传失败写 `document_upload_failed` 审计（ProviderError→502，其余→400）。

### 文件变更清单

**修改**：`app/services/ingestion.py`、`app/services/rag.py`、`app/services/providers.py`、
`app/routers/knowledge.py`、`requirements.txt`（新增 `charset-normalizer`）

### 验证结果

- `compileall` 通过；
- 纯函数冒烟全部通过：HTML `colspan/rowspan` 展平、PPTX 垂直合并回填、表格引用摘要命中行排序、
  Big5/GBK/UTF-8/UTF-16 编码探测、表格补表头回归。

## 七、P1（第二批）实现

| # | 内容 | 状态 |
|---|---|---|
| 25 | 表格语义增强：`_table_to_markdown` 新增 `enrich_cells`，非空数据单元格合入列名（`列名：值`） | ✅ |
| 13 | 大 XML 内存保护：`MAX_PARSE_BYTES` 限制读入大小 + ZIP 解压总体积（防 zip bomb） | ✅ |
| 26 | XLSX 大文件内存：解压后 >100MB 改用 openpyxl `read_only` 流式读取（牺牲合并单元格） | ✅ |

### 关键实现

- `_table_to_markdown(rows, caption=None, *, enrich_cells=True)`：默认把表头合入每个非空
  数据单元格（`部门：研发`），使单元格自包含，提升向量 / BM25 对「列名 ↔ 值」跨列关系的召回；
  表头行与分隔行保持不变，引用摘要 `_excerpt_table` 兼容；
- 新增 `config.max_parse_bytes()`（环境变量 `MAX_PARSE_BYTES`，默认 512MB）：
  - `ingest_file_detailed` 入口检查读入文件大小；
  - `_check_zip_expansion()` / `_zip_total_size()` 对 docx/xlsx/pptx/epub 检查解压后总体积；
- `read_xlsx` 解压后 >100MB 时切换 `read_only=True` 流式读取（read_only 无 `merged_cells`，
  大文件合并单元格暂不展开，已在 docstring 注明）；
- 同步 `.env.example`、`deploy.env.example`、`docker-compose.deploy.yml`、`DEPLOY.md`。

### 文件变更清单

**修改**：`app/services/ingestion.py`、`app/core/config.py`、`.env.example`、
`deploy.env.example`、`docker-compose.deploy.yml`、`DEPLOY.md`、`README.md`

### 验证结果

- `compileall` 通过；
- 冒烟：语义增强后 `| 部门：研发 | 年假天数：10天 |`；长表格切块仍补表头；引用摘要仍命中
  `部门：部门20`；`_check_zip_expansion` 小 limit 正确拒绝；HTML/PPTX/xlsx 合并单元格 +
  enrich、Big5 编码、文本摘要回归全部通过。

## 八、遗留与待办

| # | 待办 | 说明 |
|---|---|---|
| 1 | 代码已提交 | 后端按「功能代码 + 文档」拆分提交，前端独立提交（见 git log） |
| 2 | P1 剩余项 | #1 2 列无边框表、#3/#4 PDF 合并/跨页表格、#5 多栏、#7 各格式细节 |
| 3 | P2 底座项 | #17 pytest、#18 CI、#19 依赖锁定、#16 OCR 三态、#23 老格式、#27 降级链、#28 模型入镜像、#29 增量同步、#30 表格质量基线 |
| 4 | CLI 网络异常友好化 | ✅ 已解决：`ProviderError(RuntimeError)` 统一封装，CLI 已按运行时错误友好报错 |
| 5 | SSO 登录前端入口 | 后端 `/auth/sso/login`、`/auth/sso/callback` 代码就绪但未实测真实 IdP；前端登录页暂无 SSO 入口，待后端实测后补 |

## 九、前端联动（reingest/rebuild + 解析指标 + 保留策略入口）

本轮对照后端能力补齐前端缺口：

| 后端能力 | 前端原来 | 本轮前端改动 |
|---|---|---|
| `POST /knowledge-bases/{kb}/reingest`（新增 HTTP API） | 无 UI（仅 CLI） | AdminPage 新增「索引运维」卡片：选库 + 填源目录 + 重导按钮 |
| `POST /knowledge-bases/{kb}/rebuild`（新增 HTTP API） | 无 UI（仅 CLI） | 同上，重建按钮带二次确认 |
| 上传响应 `text_chunks/table_chunks/skipped_pages/ocr_pages/parse_ms` | 仅显示 chunks 数 | `UploadResult` 扩展 + 上传结果展示解析指标 |
| `POST /conversations/retention/run`（仅 admin） | api 有 `runRetention()` 但无 UI | ChatPage 会话面板新增「保留策略」按钮（admin only） |

### 后端新增

- `app/schemas/__init__.py`：`KnowledgeBaseRebuildRequest(source_dir)`；
- `app/routers/knowledge.py`：`POST /{kb}/reingest` 与 `POST /{kb}/rebuild`（`require_admin`），
  复用 `rebuild_knowledge_base`，写审计 `kb_reingest/kb_rebuild(_failed)`，区分
  ProviderError(502) / ValueError/RuntimeError(400)。

### 前端新增/修改

- `src/api.ts`：`UploadResult` 扩展解析指标字段；新增 `RebuildResult` 与
  `reingestKnowledgeBase` / `rebuildKnowledgeBase`；
- `src/pages/AdminPage.tsx`：上传结果展示「正文/表格/跳过扫描页/OCR/解析耗时」；新增
  admin-only「索引运维」卡片（重导/重建）；
- `src/pages/ChatPage.tsx`：admin-only「保留策略」按钮；
- `src/styles.css`：`.ops-result/.ops-ok/.ops-warn/.ops-errors`、`.conv-retention`。

### 验证结果

- 后端 `compileall` 通过；`/openapi.json` 出现 `reingest`/`rebuild` 两个路径；
- 前端 `tsc --noEmit` 通过、`vite build` 通过（42 模块，475ms）；
- 后端非沙箱重启后 HTTP 端到端验证：上传建库 → `reingest`（9 files/338 chunks）→
  错误路径 400 → 查询 9 文档，全部通过，演示库已清理。

---

# 2026-09-19：文档入库 P1 正确性深水区（#1/#3/#4/#5/#7）

> 原文件：2026-09-19-企业知识库助手开发记录.md

日期：2026-09-19
项目目录：后端 `E:\EnterpriseKB`（本次不涉及前端）

## 一、本次会话目标

依据 `文档入库工程化技术指标` 的 P1 分级，落地文档入库「正确性深水区」剩余项：

- #1 2 列无边框表格不检测；
- #3 PDF 合并单元格无法可靠还原；
- #4 PDF 跨页表格被切开、续页丢表头；
- #5 多栏 PDF 版面乱序；
- #7 各格式细节缺口（PPTX 备注/SmartArt/图表、XLSX 公式与 sheet 内多表、
  DOCX 页眉页脚/文本框、EPUB 图片 alt）。

## 二、完成项概览

| # | 内容 | 状态 |
|---|---|---|
| 1 | 无边框表格门槛 ≥3 列 → ≥2 列，加「编号/项目符号」负向过滤 + 长单元格长度过滤 | ✅ |
| 3 | PDF `extract()` 的 `None`（合并延续位）先横后纵回填主格值，真空格 `""` 不动 | ✅ |
| 4 | 跨页有边框表格续接：连续页同列数 + 贴页底/顶 + 首行不同 → 合并并补表头 | ✅ |
| 5 | 多栏阅读顺序：x 空隙分栏（先左后右、栏内自上而下）+ 通栏块按 y 插入 | ✅ |
| 7 | PPTX 备注/图表/SmartArt、XLSX 公式回退 + sheet 多表、DOCX 页眉页脚/文本框、HTML/EPUB 图片 alt | ✅ |

## 三、关键实现（`app/services/ingestion.py`）

### 3.1 #3：PDF 合并单元格还原（`_fill_pdf_merged_cells`）

PyMuPDF `table.extract()` 对合并区域：左上角格含文本，延续位返回 `None`（真空格为 `""`）。
据此先做行内横向填充（跨列合并）、再做列内纵向填充（跨行合并），`None` 才被正确还原；
`""` 保持不动，避免把真空格误填。`_pymupdf_table_to_markdown` 先还原再转 Markdown。

### 3.2 #1：2 列无边框表格（`_detect_borderless_tables` + 过滤函数）

锚点门槛由 ≥3 列放宽到 ≥2 列；2 列候选须同时满足：
- 至少 2 行、右侧列非全空；
- 左侧列不是编号/项目符号（`_left_column_is_numbering`，覆盖 `1.`/`1、`/`（一）`/`•` 等）；
- 裸数字（`1`/`2`/`3`）左列：右列为长正文（平均 ≥12 字）判为编号列表；
- 平均单元格长度 ≥12 或存在 ≥20 字单元格判为「双栏正文 / 定义列表」，保留为正文（宁漏勿错）。

### 3.3 #4：PDF 跨页表格续接（`_merge_cross_page_tables`）

有边框表格先收集 `(page, page_height, bbox, grid)`，跨页合并条件（同时满足）：连续页、
列数相同、上一物理表格贴页底（≥70% 页高）、下一物理表格贴页顶（≤30% 页高）、两表首行
不同（首行相同说明续页自带表头，不合并）。合并后 grid 拼接，Markdown 以首页表头为表头。

### 3.4 #5：多栏阅读顺序（`_split_into_columns` + `_sort_entries_reading_order`）

按 x 空隙（>12% 版面宽）把窄块分成多栏，栏内按 `(y, x)` 排序、栏间从左到右；通栏块
（宽度 ≥60% 版面宽，如标题/宽表格）作为栏间分隔按 y 插入；末尾兜底补回未输出块，保证不丢内容。

### 3.5 #7：各格式细节

- **PPTX**：`read_pptx` 新增图表转 Markdown（类别列 + 系列行）、组合/SmartArt 递归提取
  `a:t` 文本、演讲者备注单独成块（`【演讲者备注】`）；
- **XLSX**：`read_xlsx` 非 read_only 路径双开 workbook（data_only=True 取值 +
  data_only=False 取公式），值为 None 时回退公式文本；`_split_sheet_into_tables` 按整行
  全空做纵向多表拆分（空行段 ≥2 或上下块列数不同才切开），并剔除全空列、丢弃 <2 行残块；
- **DOCX**：`read_docx` 按 `document.xml.rels` 读取页眉/页脚，并提取 `w:txbxContent` 文本框；
- **HTML/EPUB**：`_HTMLTextExtractor` 提取 `<img>` 的 alt/title 文本（`[图片：…]`）。

## 四、验证结果

- `compileall` 通过；`app.cli / app.eval / app.routers.knowledge / app.services.rag` 导入正常；
- 新增 `scripts/smoke_ingestion_p1.py`（可重复冒烟），覆盖 #1/#3/#4/#5/#7 全部分支，全部 PASS；
- **真实文档覆盖度核查**：`examples/` 下 5 篇制度文档经结构化分析均为**纯文本**——
  PDF 有边框表格=0、图片=0、多栏空隙页=0；DOCX 表格/合并单元格/文本框=0，
  页眉页脚 XML 为空段落。即这 5 篇**不涉及 #1/#3/#4/#5/#7 所针对的结构**，
  仅能证明文本提取链路无回归，不能证明各 P1 修复生效；
- **自建真实感文档端到端**（`scripts/smoke_ingestion_p1.py` 内构造）：
  - PDF 合并单元格「审批权限表」（`insert_htmlbox` + rowspan）→ 纵向合并延续位正确回填；
  - PDF 跨页 + 合并单元格「审批权限表」（页 1 贴底 / 页 2 贴顶，第 1 列纵向合并）→ 合并为一张、
    补回表头、合并格回填、续页行齐全；
  - PDF 2 列无边框表（岗位/职责）正确识别、编号列表正确不识别；
  - PDF 双栏正文阅读顺序先左后右且不被误判为表格；
  - DOCX 页眉/页脚/文本框 + `gridSpan` 合并表头正确提取；
  - XLSX 公式回退 + sheet 纵向多表拆分、PPTX 备注/图表/表格均正确。

## 五、文件变更清单

**修改**：`app/services/ingestion.py`、`app/services/eval.py`（--modes）、`app/cli.py`（eval --modes）、
`requirements.txt`（FlagEmbedding 拆出）、`Dockerfile`（装 rerank 可选依赖）、
`app/core/database.py`（documents 增量同步迁移）、
`app/models/knowledge.py`（source_mtime/source_size/source_path）、`docker-compose.deploy.yml`、
`DEPLOY.md`、`app/routers/knowledge.py`（老格式筛除提示）

**新增**：`scripts/smoke_ingestion_p1.py`、`2026-09-19-企业知识库助手开发记录.md`（本文件）

**修改（文档）**：`文档解析与入库技术说明`、`文档入库工程化技术指标`、
`待解决问题与解决方案`（问题三状态更新）、`README.md`（新增 6.1 自动化测试）

**新增（测试）**：`tests/`（`__init__.py`、`fixtures.py`、`test_ingestion_core.py`、
`test_ingestion_pdf.py`、`test_ingestion_formats.py`、`test_retrieval.py`、
`test_eval.py`、`test_examples_regression.py`）、`pytest.ini`、`conftest.py`、
`requirements-dev.txt`、`requirements-rerank.txt`、`.github/workflows/ci.yml`

**新增（P2 第三批）**：`requirements-lock.txt`、`scripts/download-rerank-model.ps1`、
`examples/审批权限表.xlsx`、`eval/table-eval.yaml`、
`tests/test_ocr.py`、`tests/test_legacy.py`、`tests/test_table_eval.py`

## 六、遗留与待办

| # | 待办 | 说明 |
|---|---|---|
| 1 | #6 图片/图表结构化、#8 OCR 落地 + 后处理 | 属 P3 多模态，依赖 OCR/VLM，独立立项 |
| 2 | #18 CI 回归门禁 | 已搭 `.github/workflows/ci.yml`（确定性门禁 + 手动评测回归）；待推送后在 Actions 页实测、补评测基线 secret |
| 3 | #16 OCR 三态诊断、#23 老格式、#27 解析降级链、#28 模型入镜像、#29 增量同步、#30 表格质量基线 | P2 底座项 |
| 4 | 2 列无边框表仍是启发式（编号/长度过滤），未建标注集量化 ACC-01/ACC-03 | 建议后续补参数化测试与真实样本回归 |

## 七、补充（同日）：P2 #17 `tests/` + pytest

在 P1 修复的冒烟脚本基础上，落地 `tests/` + pytest 回归底座：

- `tests/fixtures.py`：集中所有文档构造器（PDF/DOCX/XLSX/PPTX/HTML），供 pytest 与
  `scripts/smoke_ingestion_p1.py` 共用，消除两处构造代码漂移；
- `tests/test_ingestion_core.py`：切块/表格转 Markdown/编码探测/内容嗅探/合并单元格/跨页合并/阅读顺序等纯函数；
- `tests/test_ingestion_pdf.py`：PDF 端到端（2 列无边框表、合并单元格、跨页、多栏阅读顺序）；
- `tests/test_ingestion_formats.py`：DOCX/XLSX/PPTX/HTML 端到端；
- `tests/test_retrieval.py`：中文二元组分词 + BM25 排序；
- `pytest.ini`（testpaths=tests）、根 `conftest.py`、`requirements-dev.txt`（pytest）。

验证：`pytest -v` **26 passed**（DB-free、约 1.3s）；`scripts/smoke_ingestion_p1.py` 同源全部 PASS。
期间发现并记录了 charset-normalizer 对**极短** GB18030 样本可能误判（真实文档长度足够、可正确识别），
测试中已改用真实长度样本并在用例内注明。

## 八、补充（同日）：P2 #18 CI + `app.eval --modes`

### 8.1 `app.eval --modes`（三方式对比 CLI）

前端评测页已能做 dense/hybrid/rerank 三方式对比，本次把同一能力补齐到 CLI，消除两者不一致：

- `app/services/eval.py`：新增 `VALID_MODES`、`parse_modes()`、`run_eval_modes()`、
  `print_comparison()`、`write_json_modes()`、`write_markdown_modes()`；`main()` 支持 `--modes`；
  单模式行为保持完全向后兼容。
- `app/cli.py eval` 同步新增 `--modes`。
- 用法：`python -m app.eval eval/hr-eval.yaml --modes dense hybrid rerank --json out.json`。

### 8.2 #18 CI（`.github/workflows/ci.yml`）

- **确定性门禁（push/PR 自动跑）**：`pip install -r requirements-dev.txt` →
  `compileall` → `pytest`（含 examples 真实文档解析回归）。
- **评测回归（workflow_dispatch 手动触发，需 `QWEN_API_KEY` secret + pgvector 服务容器）**：
  init-db → 导入 `hr` 示例 → `python -m app.eval --modes dense hybrid` →
  以 hr 集 1.0 基线做非退化门禁；rerank 因本地 2.3GB 模型权重不在仓库、不入 CI。
- 为让 CI 轻量安装，把 `FlagEmbedding`（会带入 torch）从 `requirements.txt` 拆到可选
  `requirements-rerank.txt`，`Dockerfile` 同步改为两者都装（生产仍启用 rerank）。

验证：`pytest` **31 passed**（新增 `tests/test_eval.py`、`tests/test_examples_regression.py`）；
`python -m app.eval --help` 与 `python -m app.cli eval --help` 均出现 `--modes`；
`print_comparison` / `write_json_modes` / `write_markdown_modes` 冒烟通过。
（GitHub Actions 工作流无法在本地执行，需推送后在仓库 Actions 页验证。）

## 九、补充（同日）：P2 遗留项逐项落地

| # | 内容 | 落地方式 | 验证 |
|---|---|---|---|
| 16 | OCR 三态诊断 | `_ocr_page` 区分「未装依赖 / 识别异常 / 空结果」写 debug 日志 | ✅ `tests/test_ocr.py`（4 例，caplog） |
| 27 | docx/xlsx 解析降级链 | 结构化解析失败时降级为 `<w:t>`/共享字符串文本兜底 | ✅ 构造损坏 docx/xlsx 触发降级（2 例） |
| 19 | 依赖锁定 | 新增 `requirements-lock.txt`（当前 .venv 精确版本） | ✅ lock 生成；rapidocr/onnxruntime 矩阵待独立环境 |
| 23 | 老格式 | 入库时筛除老格式（不在上传白名单），并提示转换为新格式；放弃 LibreOffice 转换 | ✅ `tests/test_legacy.py`（参数化 7 格式 + 转换提示） |
| 29 | 增量同步 | `documents` 增 `source_mtime/source_size/source_path`（幂等迁移）+ `sync` CLI + `_should_sync` | ✅ 变更检测单测；DB 端到端待启动 |
| 30 | 表格块质量基线 | `examples/审批权限表.xlsx` + `eval/table-eval.yaml` | ✅ 样本/标注对齐单测（2 例）；基线数值待 DB+API 采集 |
| 28 | 模型入镜像分发 | `requirements-rerank.txt` 拆出 + `scripts/download-rerank-model.ps1` + DEPLOY 说明 | ⚠️ 镜像构建未实测 |

验证：`compileall` 通过；`pytest` **50 passed**（2.5s，DB-free）；`python -m app.cli --help` 出现
`sync` 命令；`scripts/smoke_ingestion_p1.py` 仍全部 PASS。

**仍需外部环境才可闭环**：#28 镜像构建、
#29/#30 的 DB 端到端与基线数值（启动 pgvector + Embedding API）、
#19 的 rapidocr/onnxruntime 独立矩阵（见第十节核查结论）。

## 十、补充（同日收尾）：#19 兼容矩阵核查与当日工作总结

### 10.1 #19 依赖兼容矩阵核查（已定位结论，安装实测未闭环）

目标：确认可选 OCR 依赖 `rapidocr-onnxruntime`（及其传递依赖 `onnxruntime` /
`opencv-python` / `shapely` / `pyclipper`）与现有 `numpy 2.2.6`、`torch 2.13.0+cu126`
能否共存。

核查结论（依赖元数据核查，非安装实测）：

- 当前 .venv：`numpy==2.2.6`、`torch==2.13.0+cu126`、`FlagEmbedding==1.4.2`、
  `Pillow==12.3.0`、`six==1.17.0`；尚未安装 onnxruntime / opencv-python / shapely /
  pyclipper / rapidocr-onnxruntime；
- 旧版 rapidocr 要求 `numpy<2.0`（与 numpy 2.2.6 冲突）；**新版
  `rapidocr-onnxruntime>=1.4.0` 已放宽为 `numpy<3.0`**，可与 numpy 2.2.6 共存；
- 因此应安装 `rapidocr-onnxruntime>=1.4.0`（`requirements.txt` 注释同步改为该下限）。

未闭环原因：`pip install "rapidocr-onnxruntime>=1.4.0"` 在当前网络环境长时间无响应
（挂起）被中断。**#19 仍需在具备 PyPI 网络的环境完成安装实测**，随后补锁
`onnxruntime / opencv-python / shapely / pyclipper / rapidocr-onnxruntime` 精确版本到
`requirements-lock.txt`，并跑通 OCR 冒烟（渲染含文字页 → `_ocr_page` 识别中文）。

### 10.2 当日收尾验证

- `compileall` 通过；`pytest` **50 passed**（2.5s，DB-free，含 examples 真实文档解析回归）；
- `scripts/smoke_ingestion_p1.py` 与测试同源，全部 PASS；
- 工作区已按主题提交并推送：`22dd433`（主体）、`bf9e2e2`（OCR 三态用例在无 numpy
  环境跳过，修复 CI）；当日收尾时工作树干净。

### 10.3 当日工作总结

| 阶段 | 完成内容 | 状态 |
|---|---|---|
| P1（正确性深水区） | #1 2 列无边框表、#3 PDF 合并单元格、#4 跨页表格续接、#5 多栏阅读顺序、#7 各格式细节 | ✅ 全部解决并自测 |
| P2 底座 | #16 OCR 三态日志、#17 tests/ + pytest（50 例）、#18 CI（已 GitHub 实测通过）、#23 老格式筛除、#27 解析降级链 | ✅ 已解决 |
| P2 待外部环境 | #19 依赖矩阵（已定位 `>=1.4.0` 结论，待网络安装实测）、#28 镜像构建、#29 sync DB 端到端、#30 基线数值采集 | ⚠️ 待闭环 |
| P3 多模态 | #6 图片/图表、#8 OCR 落地 | 独立立项，未启动 |

---

# 2026-09-21：Rerank 模型路径去硬编码 + 缺失优雅降级

> 原文件：2026-09-21-企业知识库助手开发记录.md

日期：2026-09-21
项目目录：后端 `E:\EnterpriseKB`（本次不涉及前端）

## 一、背景

`config/models.yaml` 的 `retrieval.rerank.model` 此前硬编码了本机 Windows 绝对路径
`E:/EnterpriseKB/models/bge-reranker-v2-m3`，且 `retrieval.rerank.enabled=true` 默认开启。
在 Linux / Docker 部署时该路径不存在，`get_reranker()` 会抛 `RuntimeError`，而检索层
未做降级，导致开启 rerank 后首次问答 `/chat` 直接 500。违反 AGENTS「不硬编码」与
「安全默认值」约定。

## 二、修复内容

1. **去除硬编码路径**：`config/models.yaml` 的 `retrieval.rerank.model` 改为可移植的
   HuggingFace 模型 ID `BAAI/bge-reranker-v2-m3`；本地权重路径改由环境变量
   `RERANK_MODEL_PATH` 覆盖（`app/core/config.py` 的 `retrieval_config()` 优先读环境变量）。
2. **优雅降级**：`app/services/rerank.py` 新增 `score_rerank()`——模型加载/依赖缺失失败时
   返回 `None` 并记录一次 warning，进程内缓存该失败避免每次问答重试加载 2.3GB 模型；
   `app/services/retrieval.py` 的单库/跨库检索在 rerank 不可用时分别降级为
   RRF 混合排序 / 跨库稠密距离排序，`best_rerank_score=None` 使防幻觉门禁自动退回
   稠密距离单阈值，问答不再 500。
3. **保留本机行为**：本地 `.env` 增加 `RERANK_MODEL_PATH=E:/EnterpriseKB/models/bge-reranker-v2-m3`
   指向已下载的模型目录，本机 GPU 重排能力不变。
4. **同步模板与文档**：`.env.example` / `deploy.env.example` 新增 `RERANK_MODEL_PATH` 说明；
   README / DEPLOY.md / `向量化与检索技术说明` / `技术全景与学习手册`
   同步更新模型路径与降级行为描述。

## 三、验证

- `python -m compileall -q app` 通过；
- `pytest -q` **53 passed**（新增 `tests/test_rerank.py` 3 例：空候选、失败降级并缓存、
  环境变量覆盖回落）；
- 配置读取冒烟：`.env` 生效时 `retrieval_config().rerank_model` 返回本机路径，
  `RERANK_MODEL_PATH` 置空时回落 `BAAI/bge-reranker-v2-m3`。

## 四、遗留

- 模型缺失时的真实降级链路（缺模型环境下跑一次 `/chat`）仍需在无 FlagEmbedding/无权重
  的环境中端到端确认（当前为纯函数 + 配置层面验证）。

## 五、生产部署方案决定（方案一：宿主机数据卷挂载）

> 2026-09-21 确认：生产环境采用**方案一**落地 Rerank 模型分发。

- **决定**：模型（约 2.3GB）放宿主机固定目录，通过 Docker 数据卷挂载进 backend 容器，
  用 `RERANK_MODEL_PATH` 指向**容器内**挂载路径；不把模型打进镜像、不依赖容器内自动下载。
- **理由**：不依赖外网下载、镜像体积可控、模型可独立升级/备份、容器重建不丢模型。
- **落地文件**：
  - `scripts/download-rerank-model.sh`（Linux 服务器下载，ModelScope/HuggingFace 双源）；
  - `docker-compose.deploy.rerank.example.yml`（可选 override：声明卷挂载 + `RERANK_MODEL_PATH`）；
  - `DEPLOY.md` 第 6 条补充 Linux 下载命令与 override 用法。
- **操作要点**：`RERANK_MODEL_PATH` 填容器内路径（挂载目标），非宿主机路径；
  模型缺失/加载失败时后端自动降级为混合检索，不影响问答可用性。

## 六、后续待解决问题清单（2026-09-21 盘点）

> 状态：`[x]` 已解决　`[ ]` 待解决。按「先止血 / 低风险 → 后外部环境 → 再功能」排序。

### A. 部署 / 可观测 / 模板一致性

- [x] 方案一落地文件：`scripts/download-rerank-model.sh`、`docker-compose.deploy.rerank.example.yml`、DEPLOY 步骤
- [x] 生产 compose 透传 `RERANK_MODEL_PATH`（backend environment）
- [x] `deploy.env.example` 补 `CONVERSATION_ARCHIVE_DAYS` / `CONVERSATION_RETENTION_DAYS`
- [x] `.env.example` / `deploy.env.example` 补 `DATABASE_URL` 说明
- [x] DEPLOY.md 离线编排示例补 `MAX_PARSE_BYTES`
- [x] `app/api.py` lifespan 保留策略失败不再静默吞异常（改 warning + exc_info）
- [ ] 方案一真实 Linux 服务器端到端验证（下载→挂载→启动→提问确认 rerank 生效）——需部署环境
- [ ] #28 镜像构建冒烟实测
- [ ] 会话保留策略定时任务（crontab / APScheduler）
- [x] `audit_logs` 保留策略（`AUDIT_RETENTION_DAYS` + `apply_audit_retention()` + CLI `audit-retention`，默认永久保留）
- [ ] Docker Desktop #531/#532 socket bug 跟踪（本机开发环境）

### B. 外部环境闭环（文档中 ⚠️ 待实测项）

- [ ] #19 `rapidocr-onnxruntime` 兼容矩阵安装实测（需 PyPI 网络）
- [ ] #29 增量同步 `sync` 的 DB 端到端验证
- [ ] #30 表格块 hit@k/MRR 基线数值采集
- [ ] CI 评测回归接入（`QWEN_API_KEY` secret + 基线门禁）

### C. 功能缺口

- [ ] #6 图片/图表结构化 + 视觉向量检索（P3）
- [ ] #8 OCR 落地 + 后处理（P3）
- [ ] 跨会话长期记忆（用户画像 / 偏好）
- [ ] 知识沉淀（对话 → 知识库，人工标记 → 审核 → 入库）
- [ ] 多轮记忆升级：方案 B 结构化指代消解 / 证据窗口按语义相关性
- [ ] SSO 真实 IdP 端到端实测 + 前端 SSO 入口
- [ ] MCP 只读接入 + 首个 Skill
- [ ] BM25 分词升级（jieba / 自定义词典）
- [ ] 评测集扩充（负面样本、真实样本 ≥10 篇）

### D. 量化与门禁

- [x] pytest `--cov` 覆盖率统计接入（2026-09-21：装 `pytest-cov`，`pytest.ini` 默认 `--cov=app --cov-report=term-missing`）
- [ ] 未量化指标：ACC-01 表格还原率、ACC-04 正文表格不重复率、PERF-01/02 性能
- [x] `conversation_messages.citations` 引用去重（2026-09-21：落库只存 `chunk_id + 短元数据`，读取时按 chunk_id 重建摘要）
- [x] 文档与代码漂移清理（2026-09-21：`技术全景与学习手册.md` 代码地图/学习路线/架构图、
  `向量化与检索技术说明.md` 文件清单、README 代码结构图与 `should_refuse`→`gate_decision` 等旧路径/旧函数名已对齐）

## 七、审计日志保留策略（A 类收尾）

- **新增** `AUDIT_RETENTION_DAYS` 环境变量（`app/core/config.py` → `audit_retention_days()`）：
  留空或 ≤0 时**永久保留**（默认，安全第一）。
- **新增** `app/services/audit.py::apply_audit_retention()`：按 `ts < now - days` 批量删除超期
  审计记录，返回删除数量；未配置时直接返回 `deleted=0` 不触碰数据库。
- **触发入口**：`app/api.py` lifespan 启动时执行一次（成功后写 `audit_retention` 审计事件，
  失败记 warning 不阻断启动）；CLI `python -m app.cli audit-retention` 可挂系统定时任务。
- **同步**：`.env.example` / `deploy.env.example` 新增该变量；DEPLOY.md 配置表补齐会话/审计保留三项。
- **验证**：`compileall` 通过；`pytest` **55 passed**（新增 `tests/test_audit_retention.py` 2 例：
  配置解析、未配置关闭分支）。删除路径的 DB 端到端待真实库验证。

## 八、citations 引用去重（D 类）

- **问题**：`conversation_messages.citations` 每条引用都存完整 `excerpt`（约 280 字），
  同一文档块被多轮引用时摘要重复存储、随消息线性膨胀（`会话持久化与规模化演进` L1 已识别）。
- **方案**：落库只存稳定指针与短元数据 `{chunk_id, filename, page_number, chunk_index,
  knowledge_base}`；`excerpt` 在读取消息时按 `chunk_id` 批量查 `DocumentChunk.content`
  实时重建（块首 280 字）。`chunk_id` 为新增稳定引用（`Citation` 新增字段）。
- **向后兼容**：旧数据 citations（带 excerpt）读取时直接透传；chunk 已被删除时 excerpt
  置空、仍保留 filename/page_number/chunk_index 便于溯源；后端响应字段不变，前端无需改动。
- **不动**：`context_chunks`（会话内 reuse 记忆）仍存完整 content/excerpt，与本次无关；
  `review_items.citations` 保持原样（量级远小于消息表）。
- **涉及文件**：`app/services/rag.py`（`Citation.chunk_id`、`citation_to_dict`、
  `compact_citation_dicts`、`head_excerpt`、`serialize_chunks`/`citations_from_stored_chunks`）、
  `app/routers/conversations.py`（落库瘦身 + `_citation_excerpt_map`/`_serialize_message` 读取重建）。
- **验证**：`compileall` 通过；`pytest` **59 passed**（新增 `tests/test_citation_compact.py` 4 例）。
  落库→读取重建的 DB 端到端待真实库验证。

## 九、pytest --cov 覆盖率统计（D 类）

- **依赖**：安装 `pytest-cov>=7.0`（带入 `coverage`）；`requirements-dev.txt` 同步新增该依赖。
- **配置**：`pytest.ini` 的 `addopts` 增加 `--cov=app --cov-report=term-missing`，
  使 `python -m pytest` 默认输出各模块覆盖率与总覆盖率（CI 同样生效）。
- **基线（2026-09-21，59 例）**：总覆盖率 **39%**；高覆盖集中在
  `ingestion.py`（68%）、`retrieval.py`（52%）、`rerank.py`（48%）、`config.py`（71%）等已测纯函数；
  低覆盖为路由层、SSO、评测、会话落库、memory 等需要 DB / LLM / 网络的链路，属预期。
- **门禁策略**：暂不设 `--cov-fail-under` 硬阈值（当前无 DB/LLM 端到端测试，设阈值会误伤 CI），
  覆盖率随功能开发与真实库端到端测试逐步提升；后续可按「核心服务 ≥70%」再上阈值。

## 十、低配服务器（2G 内存 / 50G SSD）适配

- **背景**：线上服务器仅 2G 内存 / 50G SSD，跑不动本地 BGE Rerank（2.3GB 模型 CPU 加载
  即 OOM）。新增两个开关把「重排」做成完全可选，低配也能跑通。
- **`RERANK_ENABLED` 环境变量**（`app/core/config.py` → `retrieval_config()`）：留空沿用
  `config/models.yaml` 的 `retrieval.rerank.enabled`；设 `false` 则跳过 Rerank 分支，
  退回混合检索（dense+BM25+RRF），防幻觉门禁自动退化为稠密距离单阈值。
- **`INSTALL_RERANK` 构建参数**（`Dockerfile` + compose `build.args`）：`false` 时镜像内
  不安装 `requirements-rerank.txt`（torch，省 ~1.5GB+ 体积与构建时间）；即使误开 rerank，
  `score_rerank()` 也会因缺依赖优雅降级（见第二节）。
- **同步**：`.env.example` / `deploy.env.example` 新增两变量说明；
  `方案一-Linux端到端部署验证` 新增第 10 节「低配适配」。
- **验证**：`compileall` 通过；`retrieval_config()` 默认 `rerank_enabled=True`、
  `RERANK_ENABLED=false` 时 `False`；`pytest` 59 passed。

---

# 2026-09-23：Linux 部署验证 + 上线联调修复 + RERANK_ENABLED/OCR 缺陷修复

> 原文件：2026-09-23-企业知识库助手开发记录.md

日期：2026-09-22 ~ 2026-09-23
项目目录：后端 `E:\EnterpriseKB` + 前端 `E:\EnterpriseKBWeb`（nginx.conf）

## 一、背景

用户准备在线上 Linux 服务器验证遗留问题（方案一：Linux 端到端部署 + BGE Rerank 生效），
服务器规格：内存 7.4Gi、无 swap、磁盘 111G 可用。现有 `scripts/deploy-linux-rerank.sh`
只覆盖「检查 → 下载模型 → 生成 override → 构建启动」，后续初始化 / 授权 / 端到端验证
仍需手工执行，且低配（<8G）开 Rerank 的 swap 准备与降级判定没有自动化。本次补齐
「一次性验证脚本」，并在梳理过程中修正了部署文档里 `grant` / `ingest` 的顺序问题。

## 二、改动内容

### 1. 新增 `scripts/verify-linux-deploy.sh`

一条命令完成「环境自检 → swap → .env → 模型 → 构建启动 → 初始化 → 端到端验证 → 汇总」：

- **资源自检**：打印 docker / compose 版本、内存、CPU、磁盘、swap；内存 <8G 且开 Rerank
  时给出警告；
- **网络自检**：curl 探测 Docker Hub / PyPI / npm registry / dashscope / DeepSeek /
  ModelScope / HF 镜像，仅提示不阻断；
- **swap**：`ADD_SWAP=1` 且 root 且无 swap 时自动 `fallocate + mkswap + swapon` 并写
  `/etc/fstab`（失败回退 dd；非 root 仅提示）；
- **.env**：缺失时从 `deploy.env.example` 复制；校验 `QWEN_API_KEY` 非空且非占位符；
  `AUTH_JWT_SECRET` 为空时自动 `openssl rand -hex 32` 生成；按 `RERANK` 写
  `INSTALL_RERANK` / `RERANK_ENABLED`；
- **模型**：复用 `download-rerank-model.sh`（ModelScope/HuggingFace 双源），校验
  `config.json`，`SKIP_MODEL_DOWNLOAD=1` 可跳过；
- **override**：自动生成 `docker-compose.deploy.rerank.yml`（挂载模型目录 + 容器内
  `RERANK_MODEL_PATH`）；
- **构建启动**：`docker compose up -d --build`，循环等待 db healthy（默认 180s）；
- **初始化**：`create-user`（幂等）→ `ingest`（自动建库）→ `grant`（依赖库已存在）；
- **端到端验证**：`/health`、`/ready`、CLI `ask`（回答 + 引用）、Rerank 降级判定
  （grep 日志 `Rerank 不可用`）；每项 PASS/FAIL，最后汇总并返回退出码（0=全部通过）。

关键判定口径：Rerank 生效 = 日志**无** `Rerank 不可用` 降级 warning（与 09-21 记录的
降级文案一致）；低配模式 `RERANK=0` 则跳过该判定。

### 2. 修正部署文档顺序问题

`grant` 命令在知识库不存在时会报错退出（`app/cli.py` 的 `grant_access`），而
`ingest` 会自动创建知识库。原 `DEPLOY.md` 第 8 节与 `方案一-Linux端到端部署验证`
第 6 节都把 `grant` 写在 `ingest` 之前，会导致首次部署时 `grant` 失败。已两处统一改为
「create-user → ingest → grant」并补充顺序说明；同时在方案一文档前置条件处引用验证脚本。

### 3. 修正示例文档导入路径（examples 未打入镜像）

`Dockerfile` 只 `COPY app` 与 `COPY config`，`examples/` 未进入镜像，导致容器内执行
`python -m app.cli ingest examples/employee-handbook.md` 报 `FileNotFoundError`（线上实测触发）。
已修正 `DEPLOY.md` 第 8 节、`docs/方案一` 第 6 节与 `scripts/verify-linux-deploy.sh` 第 9 步：
先用 `docker compose cp examples/employee-handbook.md backend:/tmp/employee-handbook.md`
把示例复制进容器，再用容器内路径 `/tmp/employee-handbook.md` 导入。

### 4. 修正容器内 Python 非 UTF-8 编码（中文输出报 ascii 错误）

线上实测 `ingest` 打印中文（文档解析结果 / warning）时报
`'ascii' codec can't encode characters ... ordinal not in range(128)`。根因是镜像未显式声明
UTF-8，容器内 Python 的 stdout/stderr 编码回落到 ascii。修复：
- `Dockerfile` 的 `ENV` 增加 `PYTHONUTF8=1`、`LANG=C.UTF-8`、`LC_ALL=C.UTF-8`；
- `scripts/verify-linux-deploy.sh` 的所有 `exec ... python -m app.cli ...` 统一加
  `env PYTHONUTF8=1` 前缀，兼容尚未重建的旧镜像。

> 已运行的旧容器在不重建镜像时，可用 `docker compose exec backend env PYTHONUTF8=1
> python -m app.cli ...` 临时绕过；重新 `--build` 后无需再加前缀。

### 5. 补充澄清：本次 ascii 报错的直接根因是 API Key 占位符

线上加 `env PYTHONUTF8=1` 后仍报同一 ascii 错误，说明并非 stdout 编码问题。最终定位：
`.env` 的 `QWEN_API_KEY` 仍为模板占位符 `sk-你的千问密钥`（含中文）。openai SDK 构造
`Authorization: Bearer sk-你的千问密钥` 时，httpx 按 ascii 编码 header 失败，报错
`position 10-15` 恰为「你的千问密钥」6 个中文字符的位置。修复：`.env` 填入真实纯
ASCII key 后 `docker compose up -d` 重建 backend（`restart` 不会重新读取 `.env`）。
`Dockerfile` 的 UTF-8 加固（`PYTHONUTF8` / `LANG` / `LC_ALL`）仍保留，用于防御中文
输出到 stdout/stderr 的潜在问题。

### 6. 修复前端 Nginx 漏配 /health /ready 反代

线上 `curl http://<IP>:8080/health` 返回的是前端 `index.html`（SPA fallback），而非
`{"status":"ok"}`。根因：`EnterpriseKBWeb/nginx.conf` 只反代了 `/knowledge-bases`、
`/auth`、`/chat`、`/conversations`、`/eval`、`/audit`、`/review`、`/docs`、`/openapi.json`，
漏掉了公开探针 `/health` 与 `/ready`，导致命中 `location /` 的 `try_files ... /index.html`。
已补 `location = /health`、`location = /ready` 精确反代（置于 SPA fallback 之前）。
修复后需重建前端镜像（`docker compose ... up -d --build frontend`）生效。

### 7. 线上闭环（09-23）：端口放行与认证登录

- **对外端口**：腾讯云安全组仅放行 80（系统 firewalld `not running`，排除系统防火墙），
  将 `.env` 的 `WEB_PORT` 改为 80 对外，前端 `http://<IP>/` 可访问；
- **认证登录**：`AUTH_DEV_MODE=false` 时 `POST /auth/login` 返回 501「Dev login disabled」
  属设计行为（生产登录仅 SSO，尚未接真实 IdP，用户表无密码）。验证阶段临时
  `AUTH_DEV_MODE=true` 并重建 backend，前端以 `admin` 登录成功，完整链路可验证；
  验证完必须改回 false；
- **入库/授权**：`ingest`（Embedding 连通）、`create-user`、`grant` 均已跑通。

> 关键教训：`.env` 改动不会自动生效，需 `docker compose up -d --force-recreate backend`
> 重建容器；重建后容器内 `/tmp` 会清空，示例文档要重新 `docker compose cp`。

## 三、验证

- 脚本经 `bash -n` 语法检查通过；
- 文件行尾确认为 LF（Windows 仓库提交后经 git 检出到 Linux 不会引入 `\r`）；
- 复用既有 `download-rerank-model.sh` / compose 编排，未新增依赖、未改接口与表结构。

## 四、后续待办（下次继续验证，均需服务器 / 外部环境）

1. **Rerank 生效确认**：`ask` 后 `docker compose logs backend | grep -i 'Rerank 不可用'`
   应无输出（模型挂载 `/opt/kb/models/bge-reranker-v2-m3` → 容器 `/models/...`）；
2. **前端探针反代**：nginx.conf 已补 `/health` `/ready`，服务器 `up -d --build frontend`
   重建后 `curl http://<IP>/health` 应返回 `{"status":"ok"}`；
3. **#19** `rapidocr-onnxruntime>=1.4.0` 安装实测 + OCR 冒烟（需 PyPI 网络）；
4. **#29** 增量同步 `sync` 的 DB 端到端验证；
5. **#30** 表格块 hit@k/MRR 基线数值采集（导入 table-eval 样本跑 eval）；
6. **模型缺失降级链路**：移走模型 / 关 rerank 后跑 `/chat`，确认优雅降级不 500；
7. **会话保留策略定时任务**（crontab / APScheduler）——对应 docs/待解决问题 问题四；
8. **SSO 真实 IdP 端到端实测**（生产登录正式方案）；
9. **CI 评测回归接入**（GitHub Actions 加 `QWEN_API_KEY` secret + 基线门禁）。

## 五、RERANK_ENABLED 空值回退缺陷修复 + 验证脚本判定盲点（2026-09-23 续）

### 1. 缺陷定位

线上跑 `verify-linux-deploy.sh` 时发现两处「假 PASS」：

- 第 10 步打印 `retrieval_config: /models/bge-reranker-v2-m3 False`，却判定
  「未检测到 Rerank 降级，Rerank 生效」——根因是脚本只 grep「Rerank 不可用」告警，
  未校验 `rerank_enabled` 是否真的为 True。
- 第 9 步 `grep 'Imported'` 会匹配 `Imported 0 chunks`（文档内容 SHA-256 去重命中时），
  在全新环境下会掩盖「未导入任何块」的真失败。

进一步追到 `rerank_enabled=False` 的根因：`app/core/config.py` 的 `_env_bool()` 只把
「未设置」当 None，而 compose 的 `RERANK_ENABLED: ${RERANK_ENABLED:-}` 在 `.env` 留空时
会注入空字符串，被 `_env_bool` 解析为 False，覆盖了 `config/models.yaml` 的
`retrieval.rerank.enabled: true`，与「留空沿用配置」的文档约定不一致。

### 2. 修复内容

1. **`app/core/config.py::_env_bool`**：空字符串（strip 后为空）与「未设置」同样返回 None，
   使 `RERANK_ENABLED` 留空时回落到 `models.yaml` 默认值；显式 `false` 仍为 False。
2. **`scripts/verify-linux-deploy.sh`**：
   - 第 5 步 `RERANK=1` 分支增加 `set_env RERANK_ENABLED true`（与 `INSTALL_RERANK true` 对称，
     使脚本自愈，不再受 `.env` 遗留空值 / false 影响）；
   - 第 9 步 ingest 判定改为只认 `Imported N（N≥1）`，`Imported 0` 记为 WARN（去重命中），
     无匹配则 FAIL；
   - 第 10 步 Rerank 判定先校验 `retrieval_config` 的 `rerank_enabled=True`，为 False 直接 FAIL，
     再查「Rerank 不可用」降级告警。
3. **`tests/test_rerank.py`**：新增 `test_retrieval_config_rerank_enabled_env_fallback`
   （未设置 / 空值回落 True、显式 false 关闭）。

### 3. 验证

- `python -m compileall -q app` 通过；
- `pytest -q` 60 passed（新增 1 例）；
- `scripts/verify-linux-deploy.sh` 行尾保持 LF（无 CR 字节）。

## 六、OCR 依赖 opencv 系统库缺陷修复（2026-09-23 再续）

### 1. 缺陷定位

线上验证 #19 时，`pip install "rapidocr-onnxruntime>=1.4.0,<2.0"` 后 `import rapidocr_onnxruntime`
报 `ImportError: libxcb.so.1: cannot open shared object file`。根因：`rapidocr-onnxruntime`
依赖完整版 `opencv-python`（含 GUI 绑定），其需要 X11/GL 系统库（libxcb / libGL / libglib 等），
而 `python:3.10-slim` 镜像不含这些库。此前 #19 只核对过 rapidocr 与 numpy 的兼容矩阵，
未覆盖 opencv 的系统级依赖，本次实测补齐该缺口。

### 2. 修复内容（生产方案：系统库，而非 opencv-python-headless）

- 新增 `requirements-ocr.txt`：`rapidocr-onnxruntime>=1.4.0,<2.0`（opencv-python 为其依赖）。
- `Dockerfile` 新增 `ARG INSTALL_OCR=false` 分支：`INSTALL_OCR=true` 时
  `apt-get install` opencv 所需的 X11/GL 系统库（libgl1 / libglib2.0-0 / libsm6 /
  libxext6 / libxrender1 / libfontconfig1 / libxcb* / libxkbcommon*）后
  `pip install -r requirements-ocr.txt`，并清理 apt 缓存。
- `docker-compose.deploy.yml` backend build.args 透传 `INSTALL_OCR: ${INSTALL_OCR:-false}`。
- `deploy.env.example` 新增 `INSTALL_OCR=false` 说明。
- `requirements.txt` / `requirements-lock.txt` / `文档入库工程化技术指标`（DEP-02、#19）
  同步更新进度说明。

> 选型说明：优先考虑过 `opencv-python-headless`（无 GUI 依赖、镜像更小），但其与
> `rapidocr-onnxruntime` 声明的 `opencv-python` 依赖会产生「双 cv2」文件冲突，pip 元数据
> 不干净；故生产镜像统一走「保留 opencv-python + apt 补系统库」，确定性最高。

### 3. 待办

- OCR 冒烟通过后，把 `onnxruntime / opencv-python / shapely / pyclipper / rapidocr-onnxruntime`
  的精确版本回填 `requirements-lock.txt`（待 `pip freeze` 输出）。

### 4. 选型修正（2026-09-23 线上实测后）

线上实测发现：`opencv-python-headless==5.0.0.93` 替代完整版后 OCR 正常识别中文，且
无需任何 X11/GL 系统库（slim 镜像零系统依赖即可跑）。此前「headless 与 rapidocr 的
opencv-python 依赖存在双 cv2 冲突」的顾虑，在「先装 rapidocr → 卸载 opencv-python →
再装 headless」的三步流程下不成立。故生产方案由「apt 补系统库」修正为「headless 三步替换」：

- `Dockerfile` INSTALL_OCR 分支改为：`pip install -r requirements-ocr.txt` →
  `pip uninstall -y opencv-python` → `pip install opencv-python-headless==5.0.0.93`；
- `requirements-ocr.txt` 锁定 `rapidocr-onnxruntime==1.4.4 / onnxruntime==1.23.2 /
  pyclipper==1.4.0 / shapely==2.1.2`；
- `requirements-lock.txt` 回填 `numpy==2.2.6 / onnxruntime==1.23.2 /
  opencv-python-headless==5.0.0.93 / pyclipper==1.4.0 / shapely==2.1.2 /
  rapidocr-onnxruntime==1.4.4`。#19 闭环。

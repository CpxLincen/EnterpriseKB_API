# 遗留问题跟踪

> 用途：轻量勾选式跟踪，只列「还没做完」的项，方便日常跟进。
> 细节与验证方法见 `项目技术文档.md` 各章节（尤其「遗留问题与验证清单」）。
> 最近更新：2026-09-23　约定：`[ ]` 待办　`[x]` 完成　`⏸️` 暂缓（缺外部条件）

## 一、需外部条件（暂缓）

- [ ] 1. **SSO 真实 IdP 端到端**：配 Keycloak / Azure AD / 企业微信 / 钉钉，验证「跳转→回调换 token→id_token 校验→用户落库」；前端 SSO 登录入口尚未实现。
- [ ] 2. **CI 评测回归接入**：GitHub 仓库 Secrets 配 `QWEN_API_KEY`，`workflow_dispatch` 跑 dense/hybrid 非退化门禁。
- [ ] 3. **保留策略周期调度**：`apply_retention` 已实现、删除路径已验证，但无周期任务——生产切换后挂 cron（APScheduler 未实现）。

## 二、功能未实现

- [ ] 4. **#6 图片/图表结构化 + 视觉向量检索**（P3）：文档内图片/图表当前完全丢失（`content_type` 仅 text/table）。
- [ ] 5. **#8 OCR 后处理**（P3）：OCR 依赖已装并验证，但检测框按坐标还原阅读顺序、去水印/页眉页脚噪声仍未做。
- [ ] 6. **跨会话长期记忆**：`user_profiles` 画像（部门/角色/偏好风格/常用知识库），登录加载、回答 prompt 末尾附画像提示。
- [ ] 7. **知识沉淀**：对话→知识库（人工标记 → 管理员审核 → 入库，不自动直写）。
- [ ] 8. **多轮记忆升级**：方案 B 结构化指代消解（改写输出 `ref_rounds`，证据窗口按轮次聚焦）。
- [ ] 9. **会话规模化演进**：L1 时间分区+索引 / L2 消息体外置+冷归档 / L3 分库分表+异步落库（按用量阈值触发，当前量级无需）。
- [ ] 10. **BM25 分词升级**：中文二元组 → jieba 分词 + 企业自定义词典。
- [ ] 11. **MCP 只读接入 + 首个 Skill**：只读工具接入，配套首个 Skill（后续独立立项）。
- [ ] 12. **SSO 前端登录入口**：`LoginPage` 目前只有 dev 用户名登录，OIDC 入口未接。

## 三、量化 / 基建 / 质量

- [ ] 13. **未量化指标**：ACC-01 表格行列还原率、ACC-03 阅读顺序正确率、ACC-04 正文表格不重复率、PERF-01 文本解析吞吐、PERF-02 扫描页 OCR 单页耗时。
- [ ] 14. **真实样本回归集扩充**：`examples/` 目前 <10 篇，缺扫描 PDF / 多栏 / 合并单元格真实样本。
- [ ] 15. **评测集扩充**：负面样本偏少、真实样本 <10 篇。
- [ ] 16. **pytest 覆盖率提升**：基线 39%，目标核心服务 ≥70% 后再上 `--cov-fail-under`。
- [ ] 17. **ROB-05 事务完整性系统验证**：坏文件故障注入，确认失败后无「半截文档」残留。
- [ ] 18. **Docker Desktop #531/#532 socket bug 跟踪**（本机开发环境）。

## 四、已闭环（2026-09-23 验证，参照用）

- [x] Rerank 生效 / 前端探针反代 / #19 OCR（含精确版本回填）/ #29 增量同步 / #30 表格基线
- [x] 模型缺失优雅降级 / 会话·审计保留策略（删除路径）/ citations 引用去重
- [x] #28 镜像构建 / 文档入库 P0–P2（除 #6 图片图表、#8 后处理外均已闭环）
- [x] 文档整合：`项目开发记录.md` + `项目技术文档.md`

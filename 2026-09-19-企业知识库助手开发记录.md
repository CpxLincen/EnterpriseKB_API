# 企业知识库助手开发记录（文档入库 P1 正确性深水区：#1/#3/#4/#5/#7）

日期：2026-09-19
项目目录：后端 `E:\EnterpriseKB`（本次不涉及前端）

## 一、本次会话目标

依据 `docs/文档入库工程化技术指标.md` 的 P1 分级，落地文档入库「正确性深水区」剩余项：

- #1 2 列无边框表格不检测；
- #3 PDF 合并单元格无法可靠还原；
- #4 PDF 跨页表格被切开、续页丢表头；
- #5 多栏 PDF 版面乱序；
- #7 各格式细节缺口（PPTX 备注/SmartArt/图表、XLSX 公式与 sheet 内多表、
  DOCX 页眉页脚/文本框、EPUB 图片 alt）。

## 二、完成项概览

| # | 内容 | 状态 |
|---|---|---|
| 1 | 无边框表格门槛 ≥3 列 → ≥2 列，加「编号/项目符号」负向过滤 + 长单元格长度过滤 | ✅ |
| 3 | PDF `extract()` 的 `None`（合并延续位）先横后纵回填主格值，真空格 `""` 不动 | ✅ |
| 4 | 跨页有边框表格续接：连续页同列数 + 贴页底/顶 + 首行不同 → 合并并补表头 | ✅ |
| 5 | 多栏阅读顺序：x 空隙分栏（先左后右、栏内自上而下）+ 通栏块按 y 插入 | ✅ |
| 7 | PPTX 备注/图表/SmartArt、XLSX 公式回退 + sheet 多表、DOCX 页眉页脚/文本框、HTML/EPUB 图片 alt | ✅ |

## 三、关键实现（`app/services/ingestion.py`）

### 3.1 #3：PDF 合并单元格还原（`_fill_pdf_merged_cells`）

PyMuPDF `table.extract()` 对合并区域：左上角格含文本，延续位返回 `None`（真空格为 `""`）。
据此先做行内横向填充（跨列合并）、再做列内纵向填充（跨行合并），`None` 才被正确还原；
`""` 保持不动，避免把真空格误填。`_pymupdf_table_to_markdown` 先还原再转 Markdown。

### 3.2 #1：2 列无边框表格（`_detect_borderless_tables` + 过滤函数）

锚点门槛由 ≥3 列放宽到 ≥2 列；2 列候选须同时满足：
- 至少 2 行、右侧列非全空；
- 左侧列不是编号/项目符号（`_left_column_is_numbering`，覆盖 `1.`/`1、`/`（一）`/`•` 等）；
- 裸数字（`1`/`2`/`3`）左列：右列为长正文（平均 ≥12 字）判为编号列表；
- 平均单元格长度 ≥12 或存在 ≥20 字单元格判为「双栏正文 / 定义列表」，保留为正文（宁漏勿错）。

### 3.3 #4：PDF 跨页表格续接（`_merge_cross_page_tables`）

有边框表格先收集 `(page, page_height, bbox, grid)`，跨页合并条件（同时满足）：连续页、
列数相同、上一物理表格贴页底（≥70% 页高）、下一物理表格贴页顶（≤30% 页高）、两表首行
不同（首行相同说明续页自带表头，不合并）。合并后 grid 拼接，Markdown 以首页表头为表头。

### 3.4 #5：多栏阅读顺序（`_split_into_columns` + `_sort_entries_reading_order`）

按 x 空隙（>12% 版面宽）把窄块分成多栏，栏内按 `(y, x)` 排序、栏间从左到右；通栏块
（宽度 ≥60% 版面宽，如标题/宽表格）作为栏间分隔按 y 插入；末尾兜底补回未输出块，保证不丢内容。

### 3.5 #7：各格式细节

- **PPTX**：`read_pptx` 新增图表转 Markdown（类别列 + 系列行）、组合/SmartArt 递归提取
  `a:t` 文本、演讲者备注单独成块（`【演讲者备注】`）；
- **XLSX**：`read_xlsx` 非 read_only 路径双开 workbook（data_only=True 取值 +
  data_only=False 取公式），值为 None 时回退公式文本；`_split_sheet_into_tables` 按整行
  全空做纵向多表拆分（空行段 ≥2 或上下块列数不同才切开），并剔除全空列、丢弃 <2 行残块；
- **DOCX**：`read_docx` 按 `document.xml.rels` 读取页眉/页脚，并提取 `w:txbxContent` 文本框；
- **HTML/EPUB**：`_HTMLTextExtractor` 提取 `<img>` 的 alt/title 文本（`[图片：…]`）。

## 四、验证结果

- `compileall` 通过；`app.cli / app.eval / app.routers.knowledge / app.services.rag` 导入正常；
- 新增 `scripts/smoke_ingestion_p1.py`（可重复冒烟），覆盖 #1/#3/#4/#5/#7 全部分支，全部 PASS；
- **真实文档覆盖度核查**：`examples/` 下 5 篇制度文档经结构化分析均为**纯文本**——
  PDF 有边框表格=0、图片=0、多栏空隙页=0；DOCX 表格/合并单元格/文本框=0，
  页眉页脚 XML 为空段落。即这 5 篇**不涉及 #1/#3/#4/#5/#7 所针对的结构**，
  仅能证明文本提取链路无回归，不能证明各 P1 修复生效；
- **自建真实感文档端到端**（`scripts/smoke_ingestion_p1.py` 内构造）：
  - PDF 合并单元格「审批权限表」（`insert_htmlbox` + rowspan）→ 纵向合并延续位正确回填；
  - PDF 跨页 + 合并单元格「审批权限表」（页 1 贴底 / 页 2 贴顶，第 1 列纵向合并）→ 合并为一张、
    补回表头、合并格回填、续页行齐全；
  - PDF 2 列无边框表（岗位/职责）正确识别、编号列表正确不识别；
  - PDF 双栏正文阅读顺序先左后右且不被误判为表格；
  - DOCX 页眉/页脚/文本框 + `gridSpan` 合并表头正确提取；
  - XLSX 公式回退 + sheet 纵向多表拆分、PPTX 备注/图表/表格均正确。

## 五、文件变更清单

**修改**：`app/services/ingestion.py`、`app/services/eval.py`（--modes）、`app/cli.py`（eval --modes）、
`requirements.txt`（FlagEmbedding 拆出）、`Dockerfile`（装 rerank 可选依赖）、
`app/core/database.py`（documents 增量同步迁移）、
`app/models/knowledge.py`（source_mtime/source_size/source_path）、`docker-compose.deploy.yml`、
`DEPLOY.md`、`app/routers/knowledge.py`（老格式筛除提示）

**新增**：`scripts/smoke_ingestion_p1.py`、`2026-09-19-企业知识库助手开发记录.md`（本文件）

**修改（文档）**：`docs/文档解析与入库技术说明.md`、`docs/文档入库工程化技术指标.md`、
`docs/待解决问题与解决方案.md`（问题三状态更新）、`README.md`（新增 6.1 自动化测试）

**新增（测试）**：`tests/`（`__init__.py`、`fixtures.py`、`test_ingestion_core.py`、
`test_ingestion_pdf.py`、`test_ingestion_formats.py`、`test_retrieval.py`、
`test_eval.py`、`test_examples_regression.py`）、`pytest.ini`、`conftest.py`、
`requirements-dev.txt`、`requirements-rerank.txt`、`.github/workflows/ci.yml`

**新增（P2 第三批）**：`requirements-lock.txt`、`scripts/download-rerank-model.ps1`、
`examples/审批权限表.xlsx`、`eval/table-eval.yaml`、
`tests/test_ocr.py`、`tests/test_legacy.py`、`tests/test_table_eval.py`

## 六、遗留与待办

| # | 待办 | 说明 |
|---|---|---|
| 1 | #6 图片/图表结构化、#8 OCR 落地 + 后处理 | 属 P3 多模态，依赖 OCR/VLM，独立立项 |
| 2 | #18 CI 回归门禁 | 已搭 `.github/workflows/ci.yml`（确定性门禁 + 手动评测回归）；待推送后在 Actions 页实测、补评测基线 secret |
| 3 | #16 OCR 三态诊断、#23 老格式、#27 解析降级链、#28 模型入镜像、#29 增量同步、#30 表格质量基线 | P2 底座项 |
| 4 | 2 列无边框表仍是启发式（编号/长度过滤），未建标注集量化 ACC-01/ACC-03 | 建议后续补参数化测试与真实样本回归 |

## 七、补充（同日）：P2 #17 `tests/` + pytest

在 P1 修复的冒烟脚本基础上，落地 `tests/` + pytest 回归底座：

- `tests/fixtures.py`：集中所有文档构造器（PDF/DOCX/XLSX/PPTX/HTML），供 pytest 与
  `scripts/smoke_ingestion_p1.py` 共用，消除两处构造代码漂移；
- `tests/test_ingestion_core.py`：切块/表格转 Markdown/编码探测/内容嗅探/合并单元格/跨页合并/阅读顺序等纯函数；
- `tests/test_ingestion_pdf.py`：PDF 端到端（2 列无边框表、合并单元格、跨页、多栏阅读顺序）；
- `tests/test_ingestion_formats.py`：DOCX/XLSX/PPTX/HTML 端到端；
- `tests/test_retrieval.py`：中文二元组分词 + BM25 排序；
- `pytest.ini`（testpaths=tests）、根 `conftest.py`、`requirements-dev.txt`（pytest）。

验证：`pytest -v` **26 passed**（DB-free、约 1.3s）；`scripts/smoke_ingestion_p1.py` 同源全部 PASS。
期间发现并记录了 charset-normalizer 对**极短** GB18030 样本可能误判（真实文档长度足够、可正确识别），
测试中已改用真实长度样本并在用例内注明。

## 八、补充（同日）：P2 #18 CI + `app.eval --modes`

### 8.1 `app.eval --modes`（三方式对比 CLI）

前端评测页已能做 dense/hybrid/rerank 三方式对比，本次把同一能力补齐到 CLI，消除两者不一致：

- `app/services/eval.py`：新增 `VALID_MODES`、`parse_modes()`、`run_eval_modes()`、
  `print_comparison()`、`write_json_modes()`、`write_markdown_modes()`；`main()` 支持 `--modes`；
  单模式行为保持完全向后兼容。
- `app/cli.py eval` 同步新增 `--modes`。
- 用法：`python -m app.eval eval/hr-eval.yaml --modes dense hybrid rerank --json out.json`。

### 8.2 #18 CI（`.github/workflows/ci.yml`）

- **确定性门禁（push/PR 自动跑）**：`pip install -r requirements-dev.txt` →
  `compileall` → `pytest`（含 examples 真实文档解析回归）。
- **评测回归（workflow_dispatch 手动触发，需 `QWEN_API_KEY` secret + pgvector 服务容器）**：
  init-db → 导入 `hr` 示例 → `python -m app.eval --modes dense hybrid` →
  以 hr 集 1.0 基线做非退化门禁；rerank 因本地 2.3GB 模型权重不在仓库、不入 CI。
- 为让 CI 轻量安装，把 `FlagEmbedding`（会带入 torch）从 `requirements.txt` 拆到可选
  `requirements-rerank.txt`，`Dockerfile` 同步改为两者都装（生产仍启用 rerank）。

验证：`pytest` **31 passed**（新增 `tests/test_eval.py`、`tests/test_examples_regression.py`）；
`python -m app.eval --help` 与 `python -m app.cli eval --help` 均出现 `--modes`；
`print_comparison` / `write_json_modes` / `write_markdown_modes` 冒烟通过。
（GitHub Actions 工作流无法在本地执行，需推送后在仓库 Actions 页验证。）

## 九、补充（同日）：P2 遗留项逐项落地

| # | 内容 | 落地方式 | 验证 |
|---|---|---|---|
| 16 | OCR 三态诊断 | `_ocr_page` 区分「未装依赖 / 识别异常 / 空结果」写 debug 日志 | ✅ `tests/test_ocr.py`（4 例，caplog） |
| 27 | docx/xlsx 解析降级链 | 结构化解析失败时降级为 `<w:t>`/共享字符串文本兜底 | ✅ 构造损坏 docx/xlsx 触发降级（2 例） |
| 19 | 依赖锁定 | 新增 `requirements-lock.txt`（当前 .venv 精确版本） | ✅ lock 生成；rapidocr/onnxruntime 矩阵待独立环境 |
| 23 | 老格式 | 入库时筛除老格式（不在上传白名单），并提示转换为新格式；放弃 LibreOffice 转换 | ✅ `tests/test_legacy.py`（参数化 7 格式 + 转换提示） |
| 29 | 增量同步 | `documents` 增 `source_mtime/source_size/source_path`（幂等迁移）+ `sync` CLI + `_should_sync` | ✅ 变更检测单测；DB 端到端待启动 |
| 30 | 表格块质量基线 | `examples/审批权限表.xlsx` + `eval/table-eval.yaml` | ✅ 样本/标注对齐单测（2 例）；基线数值待 DB+API 采集 |
| 28 | 模型入镜像分发 | `requirements-rerank.txt` 拆出 + `scripts/download-rerank-model.ps1` + DEPLOY 说明 | ⚠️ 镜像构建未实测 |

验证：`compileall` 通过；`pytest` **50 passed**（2.5s，DB-free）；`python -m app.cli --help` 出现
`sync` 命令；`scripts/smoke_ingestion_p1.py` 仍全部 PASS。

**仍需外部环境才可闭环**：#28 镜像构建、
#29/#30 的 DB 端到端与基线数值（启动 pgvector + Embedding API）、
#19 的 rapidocr/onnxruntime 独立矩阵（见第十节核查结论）。

## 十、补充（同日收尾）：#19 兼容矩阵核查与当日工作总结

### 10.1 #19 依赖兼容矩阵核查（已定位结论，安装实测未闭环）

目标：确认可选 OCR 依赖 `rapidocr-onnxruntime`（及其传递依赖 `onnxruntime` /
`opencv-python` / `shapely` / `pyclipper`）与现有 `numpy 2.2.6`、`torch 2.13.0+cu126`
能否共存。

核查结论（依赖元数据核查，非安装实测）：

- 当前 .venv：`numpy==2.2.6`、`torch==2.13.0+cu126`、`FlagEmbedding==1.4.2`、
  `Pillow==12.3.0`、`six==1.17.0`；尚未安装 onnxruntime / opencv-python / shapely /
  pyclipper / rapidocr-onnxruntime；
- 旧版 rapidocr 要求 `numpy<2.0`（与 numpy 2.2.6 冲突）；**新版
  `rapidocr-onnxruntime>=1.4.0` 已放宽为 `numpy<3.0`**，可与 numpy 2.2.6 共存；
- 因此应安装 `rapidocr-onnxruntime>=1.4.0`（`requirements.txt` 注释同步改为该下限）。

未闭环原因：`pip install "rapidocr-onnxruntime>=1.4.0"` 在当前网络环境长时间无响应
（挂起）被中断。**#19 仍需在具备 PyPI 网络的环境完成安装实测**，随后补锁
`onnxruntime / opencv-python / shapely / pyclipper / rapidocr-onnxruntime` 精确版本到
`requirements-lock.txt`，并跑通 OCR 冒烟（渲染含文字页 → `_ocr_page` 识别中文）。

### 10.2 当日收尾验证

- `compileall` 通过；`pytest` **50 passed**（2.5s，DB-free，含 examples 真实文档解析回归）；
- `scripts/smoke_ingestion_p1.py` 与测试同源，全部 PASS；
- 工作区已按主题提交并推送：`22dd433`（主体）、`bf9e2e2`（OCR 三态用例在无 numpy
  环境跳过，修复 CI）；当日收尾时工作树干净。

### 10.3 当日工作总结

| 阶段 | 完成内容 | 状态 |
|---|---|---|
| P1（正确性深水区） | #1 2 列无边框表、#3 PDF 合并单元格、#4 跨页表格续接、#5 多栏阅读顺序、#7 各格式细节 | ✅ 全部解决并自测 |
| P2 底座 | #16 OCR 三态日志、#17 tests/ + pytest（50 例）、#18 CI（已 GitHub 实测通过）、#23 老格式筛除、#27 解析降级链 | ✅ 已解决 |
| P2 待外部环境 | #19 依赖矩阵（已定位 `>=1.4.0` 结论，待网络安装实测）、#28 镜像构建、#29 sync DB 端到端、#30 基线数值采集 | ⚠️ 待闭环 |
| P3 多模态 | #6 图片/图表、#8 OCR 落地 | 独立立项，未启动 |

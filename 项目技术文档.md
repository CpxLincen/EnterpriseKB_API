# 项目技术文档
> 本文件由 `docs/` 目录下历史技术文档整合而成（2026-09-23），原分文档已删除。
> 按主题顺序排列。后续技术说明直接在本文件对应章节更新，不再拆分多个文档。

## 目录
1. 技术全景与学习手册
2. 向量化与检索技术说明
3. 文档解析与入库技术说明
4. 文档入库工程化技术指标
5. 记忆策略与方案
6. 会话持久化与规模化演进
7. 待解决问题与解决方案
8. 方案一：Linux 端到端部署验证
9. 遗留问题与验证清单

---

# 技术全景与学习手册

> 原文件：技术全景与学习手册.md

> 本文档面向**学习与随时查阅**：系统梳理本项目用到的全部技术、每项技术解决什么问题、
> 关键概念解释、代码地图与配置速查。基于 2026-09-05 时的代码与开发记录整理。
>
> 项目目录：后端 `E:\EnterpriseKB`，前端 `E:\EnterpriseKBWeb`

---

## 目录

1. [项目一句话概述](#一项目一句话概述)
2. [总体架构](#二总体架构)
3. [技术栈总览](#三技术栈总览)
4. [核心技术详解](#四核心技术详解)
5. [关键概念讲解（学习向）](#五关键概念讲解学习向)
6. [代码地图](#六代码地图)
7. [配置与端口速查](#七配置与端口速查)
8. [学习路线建议](#八学习路线建议)
9. [待解决问题与后续规划](#九待解决问题与后续规划)

---

## 一、项目一句话概述

一个 **RAG（检索增强生成）企业知识库问答系统**：导入 Markdown / TXT / 文字型 PDF
制度文档 → 切块、向量化存入 **PostgreSQL + pgvector** → 用户提问时做
**混合检索（稠密向量 + BM25 稀疏 + RRF 融合 + BGE Rerank 精排）** → 交给可切换的
**OpenAI 兼容大模型（Qwen / DeepSeek）** 生成**带引用**的回答；并带 **JWT 认证、
SSO(OIDC) 预留、知识库级权限、安全加固、审计日志、离线评测**。

---

## 二、总体架构

```text
┌──────────── 前端 React/Vite（浏览器 :5173 开发 / Nginx :80 生产）────────────┐
│  登录页 → 路由守卫 → 问询 / 知识库 / 评测 / 审计 / 复核 / 历史会话                     │
└──────────────────────────────┬─────────────────────────────────────────────┘
                               │ HTTP（Bearer Token / 同源反代）
┌──────────────────────────────▼─────────────────────────────────────────────┐
│ 后端 FastAPI（:8000）                                                       │
│  auth_middleware → 路由依赖(权限) → 业务逻辑                                │
│  ├─ 认证 app/core/security.py + app/routers/（JWT / SSO / 权限 / 审计）                            │
│  ├─ 入库 app/services/ingestion.py（读文件→切块→去重→Embedding→入库）                │
│  ├─ 检索 app/services/retrieval.py（稠密+BM25+RRF） + app/services/rerank.py（BGE）           │
│  ├─ 问答 app/services/rag.py（检索→防幻觉门禁→上下文→LLM）                            │
│  └─ 评测 app/services/eval.py + app/routers/eval.py（离线回归 + 前端评测页）              │
└───────────────┬───────────────────────────────┬────────────────────────────┘
                │ SQLAlchemy + psycopg          │ OpenAI 兼容 API
                ▼                               ▼
   PostgreSQL 16 + pgvector(:5432)      Qwen / DeepSeek（云端 LLM）
                                                      │
                                  BGE Rerank 本地模型（PyTorch GPU 推理）
```

一次问答的完整链路：

```text
问题 → 认证/授权 → Embedding(问题) → 稠密检索(pgvector 余弦) + 稀疏检索(BM25)
     → RRF 融合 → BGE Rerank 精排 → top5 上下文 → 防幻觉门禁 → LLM 回答 + 引用
```

---

## 三、技术栈总览

| 层 | 技术 | 版本/说明 | 解决的问题 |
|---|---|---|---|
| Web 框架 | **FastAPI** | ≥0.115 | 快速构建类型安全的 REST API，自动生成 OpenAPI 文档 |
| ASGI 服务器 | **Uvicorn** | ≥0.30 | 运行 FastAPI 应用，高性能异步服务器 |
| 数据校验 | **Pydantic v2** | ≥2.9 | 请求/响应模型校验、配置数据类 |
| ORM | **SQLAlchemy 2.0** | ≥2.0 | 用 Python 对象操作数据库，声明式建模 |
| 数据库 | **PostgreSQL 16** | pgvector 镜像 | 关系存储 + 向量检索二合一 |
| 向量扩展 | **pgvector** | ≥0.3 | 在 PG 中存向量、做余弦相似度检索 |
| 数据库驱动 | **psycopg v3** | ≥3.2 | Python 连接 PostgreSQL 的高效驱动 |
| CLI | **Typer** | ≥0.12 | 基于类型注解的轻量命令行框架 |
| 认证 | **PyJWT** | ≥2.8 | 签发/校验 JWT，验签 id_token |
| 模型接入 | **openai** SDK | ≥1.50 | 统一调用 OpenAI 兼容接口（Qwen/DeepSeek） |
| 配置 | **python-dotenv / PyYAML** | - | 读 `.env` 环境变量与 `models.yaml` 配置 |
| PDF 解析 | **pypdf** | ≥6.10 | 提取文字型 PDF 的文本与页码 |
| 上传解析 | **python-multipart** | ≥0.0.12 | FastAPI 处理 multipart 文件上传 |
| 重排 | **FlagEmbedding** | ≥1.2 | 加载 BGE 交叉编码器做精排 |
| 深度学习 | **PyTorch** | 2.13.0+cu126 | GPU 张量计算，跑 BGE 模型 |
| 模型库 | **transformers** | 5.x | HuggingFace 模型加载与推理底座 |
| 前端框架 | **React 18** | 18.3 | 组件化构建 UI |
| 语言 | **TypeScript** | 5.6 | 类型安全的前端开发 |
| 构建 | **Vite 5** | 5.4 | 极快的开发服务器与生产打包 |
| 路由 | **React Router 6** | 6.28 | 前端 SPA 路由与路由守卫 |
| 包管理/运行时 | **Bun** | 1.x | 替代 npm 的快速包管理与运行 |
| 容器 | **Docker / Compose** | - | 一键化部署与环境隔离 |
| 反代 | **Nginx** | alpine | 托管前端静态资源 + 反代后端 API（同源） |

**模型清单**：

| 模型 | 用途 | 说明 |
|---|---|---|
| Qwen `text-embedding-v4` | Embedding | 1024 维，文档与问题向量化 |
| Qwen `qwen-plus` | 回答生成 | 默认聊天模型 |
| DeepSeek `deepseek-chat` | 回答生成 | 可切换；无 Embedding，自动回落 Qwen |
| BGE `bge-reranker-v2-m3` | Rerank | 多语言交叉编码器，本地 GPU 推理 |

---

## 四、核心技术详解

### 4.1 FastAPI（Web 框架）

**是什么**：Python 现代 Web 框架，基于类型注解与 ASGI，自动生成交互式 API 文档
（`/docs`、`/openapi.json`）。

**本项目怎么用**：
- `app/api.py` 只做应用装配（CORS / 认证中间件 / 聚合路由）；REST 路由在 `app/routers/`（health/auth/knowledge/chat/conversations/eval/audit/review）；
- **lifespan** 在启动时调用 `init_db()`（替代已弃用的 `@app.on_event("startup")`）；
- **中间件**：`app.middleware("http")(auth_middleware)` 全局校验 Bearer Token，
  `CORSMiddleware` 处理跨域；
- **依赖注入**：`Depends(get_current_user)`、`Depends(require_kb_access(write=True))` 做权限；
- `APIRouter` 按模块拆分路由，并在 `app/routers/__init__.py` 的 `api_router` 聚合。

**解决什么**：用最少的样板代码构建带类型校验、文档、认证、权限的 HTTP 服务。

### 4.2 Uvicorn（ASGI 服务器）

**是什么**：ASGI 应用服务器，把 FastAPI 应用跑起来并处理并发请求。

**本项目怎么用**：`uvicorn app.api:app --host 127.0.0.1 --port 8000`；
容器内 `--host 0.0.0.0 --port 8000`。

**解决什么**：开发/生产环境的进程级 HTTP 服务运行。

### 4.3 Pydantic v2（数据校验）

**是什么**：用类型注解声明数据模型，自动校验、序列化/反序列化。

**本项目怎么用**：
- 请求体：`ChatRequest`（question/knowledge_base）、`LoginRequest`、`RunEvalRequest`；
- 配置快照：`app/core/config.py` 里的 `ProviderConfig`、`RetrievalConfig`、`SsoConfig` 等
  `@dataclass(frozen=True)`。

**解决什么**：请求参数非法时自动 422，避免手写一堆校验逻辑。

### 4.4 PostgreSQL 16 + pgvector（存储与向量检索）

**是什么**：PostgreSQL 是成熟的关系型数据库；pgvector 是它的向量扩展，新增 `vector`
类型与 `<->`（L2）、`<=>`（余弦）、`<#>`（内积）等距离算子，可建索引。

**本项目怎么用**：
- 三张业务表：`knowledge_bases`、`documents`、`document_chunks`（`embedding` 为 `Vector` 类型）；
- 两张认证表：`users`、`knowledge_base_access`；
- `init_db()` 执行 `CREATE EXTENSION IF NOT EXISTS vector` 并建表；
- 检索用 `embedding.cosine_distance(vector)` 升序取 TopN。

**解决什么**：把「关系数据」和「向量相似度检索」放在同一个库里，无需再单独维护
向量数据库（如 Milvus/Faiss），运维简单、事务一致。

### 4.5 SQLAlchemy 2.0 + psycopg（ORM 与驱动）

**是什么**：SQLAlchemy 是 Python 最流行的 ORM/数据库工具；2.0 引入
`DeclarativeBase` + `Mapped`/`mapped_column` 的类型化声明式映射。psycopg 3 是
PostgreSQL 的 Python 驱动。

**本项目怎么用**：
- `app/core/database.py`：`create_engine(database_url(), pool_pre_ping=True, connect_args={"connect_timeout": 10})`
  和 `SessionLocal = sessionmaker(...)`；
- `app/models/knowledge.py` / `app/models/auth.py`：声明式模型，外键 + `relationship` 级联删除；
- 查询用 2.0 风格 `select(...)`、`joinedload`（预取避免 N+1）、`func.count` 聚合。

**解决什么**：用 Python 对象而非手写 SQL 操作数据库；`pool_pre_ping` 防止数据库重启后
失效连接报错；`connect_timeout` 让数据库不可达时快速失败。

### 4.6 文档入库（ingestion）

技术点：**pypdf**、**编码降级**、**SHA-256 去重**、**语义切块**、**批量 Embedding**。

- `read_document()`：PDF 用 `pypdf` 逐页提取（带页码）；`.md/.txt` 先 UTF-8、
  失败降级 GB18030（兼容中文 Windows GBK）；
- 去重：对文件原始字节算 SHA-256，同库已存在则跳过；
- 切块 `chunk_text()`：目标 650 字、重叠 80 字，优先在换行/句末标点/停顿处断开，
  断点不足半块才硬切，过滤 ≤20 字碎片；
- Embedding：一次性批量调用 Qwen，写入 `document_chunks`（content/page_number/
  chunk_index/embedding）。

**解决什么**：把任意长度的文档拆成语义较完整的块并向量化，是检索质量的“上限”。

### 4.7 混合检索（app/services/retrieval.py）

三路信号 + 融合 + 精排，这是本项目的检索核心：

1. **稠密检索**：pgvector 余弦距离升序取 `candidates=20`（语义相关）；
2. **稀疏检索**：自研 **BM25**（Okapi，k1=1.5、b=0.75），中文按**字符二元组**分词
   （`年假如何计算`→`年假/假如/如何/何计/计算`），取前 20（精确词面）；
3. **RRF 融合**：`1/(k+rank)`（k=60）把两路排名叠加，取并集重排；
4. **BGE Rerank**：RRF 前 `rerank_candidates=10` 个候选送交叉编码器逐对打分，
   按分重排取 `top_k=5`。

`mode` 可切换：`dense`（纯向量）/ `hybrid`（向量+BM25+RRF）/ `rerank`（再加 BGE）。

**解决什么**：稠密路解决“同义改写/语义相关”，稀疏路解决“专有名词/编号/精确词面”，
RRF 融合两者，交叉编码器做最终精排，显著提升 Top-K 命中率。

### 4.8 BGE Rerank + FlagEmbedding + PyTorch（精排）

- `app/services/rerank.py` 封装 `FlagEmbedding.FlagReranker`，模型 `BAAI/bge-reranker-v2-m3`
  （多语言、支持中文，约 568M 参数），本地权重路径用 `RERANK_MODEL_PATH` 环境变量覆盖
  （`config/models.yaml` 默认模型 ID）；模型缺失时自动降级为混合检索；
- `fp16: auto`：检测到 CUDA 自动半精度；当前 torch `2.13.0+cu126`，
  推理在 **GPU(cuda:0) + float16**，单次约 29ms；
- 按模型名进程内懒加载缓存。

**解决什么**：双塔向量检索只能“分别编码 query 与 doc”，而交叉编码器把 (query, doc)
拼在一起联合编码，能捕捉细粒度交互，显著提升 Top-K 精度。

### 4.9 防幻觉门禁（app/services/rag.py 的 gate_decision）

以**稠密距离为主、Rerank 分为辅**：

| 场景 | 行为 |
|---|---|
| 无候选 | 拒答 |
| 稠密距离 ≤ 0.65 且重排分 < `rerank_floor=0.05` | 判“语义太弱”，拒答（收紧） |
| 稠密距离 ≤ 0.65 且重排分 ≥ 0.05 | 放行 |
| 稠密距离 > 0.65 且重排分 ≥ `rerank_rescue=0.5` | 救援放行（防 BM25/Rerank 强命中被误拒） |
| 稠密距离 > 0.65 且重排分 < 0.5 | 拒答 |

阈值 `rerank_floor/rescue` 按当前 Embedding 模型标定（见 `eval/gate-signals.tsv`）。

**解决什么**：既避免“明明有依据却被拒”，又避免“弱命中被强行作答”，减少对 LLM
兜底拒答的依赖，把“拒答”变成确定性的门禁行为。

### 4.10 模型接入层（openai SDK + 可切换供应商）

- `app/services/providers.py` 用 `openai.OpenAI(base_url=..., api_key=...)` 统一封装
  `embed()`（批量向量化）与 `chat()`（`temperature=0.1` 低温稳定）；
- Qwen 走 DashScope 兼容端点，DeepSeek 走 `api.deepseek.com`；
- 聊天模型与 Embedding 模型**分离配置**：DeepSeek 无 Embedding，自动回落 Qwen；
- 切块入库时记录 `embedding_model` + `embedding_dimensions`，换模型/维度会拒绝写入，
  防止向量空间错配。

**解决什么**：业务代码不绑定任何一家模型厂商，改配置即可切换模型。

### 4.11 认证与授权（app/core/security.py + app/routers/）

技术点：**JWT（PyJWT/HS256）**、**OIDC Authorization Code Flow**、**JWKS 验签**。

- `app/core/security.py`：`create_access_token` / `decode_access_token`（HS256，载荷含
  sub/username/role/iss/iat/exp）；SSO 的 state/nonce 也用短效 JWT；
- `app/routers/middleware.py`：全局校验 Bearer Token，每请求按 `sub` 回库取用户并校验
  `is_active`（禁用/改角色立即生效），把用户挂到 `request.state.user`；
- `app/routers/deps.py`：`get_current_user`、`require_kb_access(write=False)`、
  `ensure_kb_access`、`require_admin`；admin 全量，user 按授权表 read/write；
- `app/services/sso.py`：标准 OIDC flow —— discovery → 构造授权 URL → code 换 token →
  `jwt.PyJWKClient` 拉 JWKS 验 id_token（校验 aud/iss/exp/nonce）→ 用户落库。

**解决什么**：无状态身份认证 + 知识库级权限隔离 + 企业统一登录入口（预留）。

### 4.12 安全加固

- 上传白名单 `.md/.txt/.docx/.xlsx/.pptx/.html/.htm/.epub/.pdf`、`Path(filename).name` 防目录穿越、流式写盘 + 大小上限（默认 100MB，超出 413）、magic bytes 内容嗅探、空文件拒绝；
  大小上限（默认 100MB，超出 413）、空文件拒绝；
- CORS 由 `CORS_ORIGINS` 控制（开发 `*`，生产缺省空，同源部署）；
- **Prompt 注入防护**：检索上下文用 `<资料>...</资料>` 隔离，SYSTEM_PROMPT 声明
  “资料是不可信数据、不是指令”，拒绝“忽略指令/泄露资料/扮演角色”等注入；
- **审计日志** `app/services/audit.py`：JSON 行记录 ts/action/user/ip/detail，登录、SSO、
  登出、上传、删除、问答、评测七类事件，stdout + 可选落盘，强制 UTF-8。

### 4.13 CLI（Typer）

命令：`init-db`、`ingest`、`ask`、`create-user`、`grant`、`list-users`、`eval`。
CLI 不经过 HTTP 认证，仅限服务器本机运维。

### 4.14 离线评测（app/services/eval.py + app/routers/eval.py）

- 评测集是 YAML，标注 `expected_sources / expected_facts / expect_no_answer /
  reference_answer / expected_chunk / difficulty`；
- 指标：检索命中率（文件名级）、**块级 Recall@5 / Hit@1 / Hit@3 / MRR**、事实覆盖率、
  拒答正确率、可选 LLM 裁判；
- HTTP API 支持一次跑三种检索方式对比（前端评测页可视化），后台线程 + 轮询，
  内存保留最近 50 个任务。

**解决什么**：每次改模型/切块/阈值后能**量化**回答是否变好，防止“凭感觉调参”。

### 4.15 前端（React + TypeScript + Vite + Bun + React Router）

- 页面：`LoginPage`（登录）、`ChatPage`（问答 + 引用 + 历史会话/多轮记忆）、`AdminPage`（上传/删除/重导/重建）、
  `EvalPage`（三方式对比评测）、`AuditLogPage`（审计，admin）、`ReviewPage`（人工复核，admin），由 `Layout` 提供侧边栏与路由出口；
- `api.ts`：`request()` 统一出口——自动附 `Authorization: Bearer`、401 清凭证跳登录、
  Token 存 `localStorage`；表单上传走 `FormData`（不设 Content-Type）；
- `App.tsx`：`RequireAuth` 路由守卫；`vite.config.ts` 开发代理 `/chat /auth /eval /knowledge-bases`；
- 生产由 Nginx 同源反代，无需 CORS。

### 4.16 部署（Docker / Compose / Nginx）

- 开发：`docker-compose.yml` 只跑 pgvector；`scripts/start-docker.ps1` 处理 Docker Desktop
  socket 残留 bug（#531/#532）后启动；
- 生产：`docker-compose.deploy.yml` 编排 db + backend + frontend，镜像
  `enterprise-kb-backend`（python:3.10-slim）与 `enterprise-kb-frontend`
  （oven/bun 构建 + nginx:alpine 运行）；Nginx 做 SPA 回退 + API 反代。

---

## 五、关键概念讲解（学习向）

### 5.1 RAG（检索增强生成）

大模型知识有截止时间、不掌握企业内部资料；RAG 的解法是：**先检索**相关资料片段，
**再让模型只依据这些片段回答**。公式化：`回答 = LLM(问题 + 检索到的上下文)`。
本项目的上下文用 `<资料>` 标签包裹、编号并带文件名/页码，模型引用如 `[1]`。

### 5.2 Embedding（向量化）

把文本映射成高维向量，使“语义相近的文本向量距离近”。本项目用 Qwen
`text-embedding-v4`（1024 维）。度量用**余弦相似度**：

```text
cosine(A,B) = (A·B) / (|A|·|B|)   → 值越接近 1 越相似
距离 = 1 - cosine                 → 值越接近 0 越相关
```

### 5.3 稠密 vs 稀疏检索

- **稠密（向量）**：靠 Embedding 捕捉语义，能命中“同义改写”，但可能漏掉“精确词面”；
- **稀疏（BM25）**：靠关键词词频匹配，擅长专有名词、编号、金额等精确命中，
  但对改写/同义不敏感。

### 5.4 BM25（Okapi BM25）

经典信息检索排序函数，核心思想：**词频越高、越稀有（IDF 大）、文档越短**，该词
对该文档越重要。公式：

```text
score(D,Q) = Σ IDF(qi) · [ f(qi,D)·(k1+1) ] / [ f(qi,D) + k1·(1 - b + b·|D|/avgdl) ]
IDF(qi)    = ln(1 + (N - df + 0.5) / (df + 0.5))
```

本项目参数 k1=1.5、b=0.75；中文分词用字符二元组（bigram）避免分词工具依赖。

### 5.5 RRF（倒数排名融合）

把多路检索结果的“排名”而非“分数”融合（因为不同检索器的分数不可比）：

```text
RRFscore(d) = Σ_r 1 / (k + rank_r(d))      # k=60 起平滑作用
```

### 5.6 交叉编码器 vs 双塔

- **双塔（Bi-encoder）**：query 与 doc 分别编码成向量，可用余弦快速检索；
- **交叉编码器（Cross-encoder）**：把 (query, doc) 拼接后一起编码，逐对打分，
  精度高但慢，只能用于候选集较小的**精排**阶段。BGE Reranker 即此类。

### 5.7 JWT（JSON Web Token）

三段式 `Header.Payload.Signature`，HS256 用共享密钥签名。无状态：服务端不存会话，
只靠签名验证；因此本项目每次请求仍按 `sub` 回库取用户，保证禁用/改角色即时生效。

### 5.8 OIDC（OpenID Connect）

基于 OAuth 2.0 的身份认证层，Authorization Code Flow：跳转 IdP → 用户授权 →
回调拿 code → code 换 token → 用 JWKS 验签 id_token（校验 aud/iss/exp/nonce）。
本项目代码就绪，尚未对真实 IdP 端到端实测。

### 5.9 pgvector

PostgreSQL 扩展，新增 `vector(n)` 类型与距离算子，可建 IVFFlat/HNSW 索引。
本项目用 `<=>`（余弦）对应 SQLAlchemy 的 `cosine_distance`。

---

## 六、代码地图

### 后端 `E:\EnterpriseKB\app`

| 文件 | 职责 |
|---|---|
| `api.py` | 应用装配：创建 app、注册 CORS/认证中间件、聚合路由 |
| `cli.py` / `eval.py` | 命令行入口（`python -m app.cli` / `python -m app.eval`） |
| `core/config.py` | 配置（.env + models.yaml）：ProviderConfig / RetrievalConfig / MemoryConfig |
| `core/database.py` | engine / SessionLocal / Base / init_db（幂等迁移） |
| `core/security.py` | JWT 签发/校验、SSO state |
| `models/knowledge.py` | KnowledgeBase / Document / DocumentChunk |
| `models/auth.py` | User / KnowledgeBaseAccess |
| `models/conversation.py` | Conversation / ConversationMessage |
| `models/audit.py` | AuditLog |
| `models/review.py` | ReviewItem（人工复核） |
| `schemas/` | Pydantic 请求模型（ChatRequest / LoginRequest / RunEvalRequest 等） |
| `services/providers.py` | OpenAI 兼容客户端（embed / chat / chat_stream） |
| `services/ingestion.py` | 多格式解析 → 切块 → 去重 → 向量化 → 入库 |
| `services/retrieval.py` | tokenize / BM25 / RRF / hybrid_search / multi_hybrid_search |
| `services/rerank.py` | BGE FlagReranker 封装（懒加载、fp16 auto、缺失降级） |
| `services/rag.py` | 检索编排 + gate_decision 门禁 + 上下文 + 引用 |
| `services/eval.py` | 离线评测引擎（指标、报告、三方式对比） |
| `services/memory.py` | 会话内短期记忆（查询改写 + 证据窗口） |
| `services/conversation.py` | 会话保留策略 apply_retention |
| `services/audit.py` | JSON 行审计日志 + 落库 |
| `services/sso.py` | OIDC 流程实现 |
| `services/review.py` | 人工复核入队 |
| `routers/` | 路由层：health/auth/knowledge/chat/conversations/eval/audit/review + deps/middleware |
| `config/models.yaml` | 模型供应商与检索参数 |

### 前端 `E:\EnterpriseKBWeb\src`

| 文件 | 职责 |
|---|---|
| `api.ts` | API 封装、Token 管理、统一请求出口 |
| `main.tsx` / `App.tsx` | 入口、路由、RequireAuth 守卫 |
| `components/Layout.tsx` | 侧边栏、用户区、登出 |
| `pages/LoginPage.tsx` | 登录 |
| `pages/ChatPage.tsx` | 智能问询 + 引用 + 历史会话/多轮记忆 |
| `pages/AdminPage.tsx` | 知识库管理（上传/删除/重导/重建） |
| `pages/EvalPage.tsx` | 评测（三方式对比 + 逐题） |
| `pages/AuditLogPage.tsx` | 审计日志查询（admin） |
| `pages/ReviewPage.tsx` | 人工复核（admin） |
| `styles.css` | 全部样式 |

---

## 七、配置与端口速查

### 检索参数（`config/models.yaml`）

| 参数 | 值 | 含义 |
|---|---|---|
| top_k | 5 | 返回给 LLM 的块数 |
| candidates | 20 | 稠密/稀疏每路候选数 |
| rrf_k | 60 | RRF 融合常数 |
| rerank.enabled | true | 是否启用 BGE 精排 |
| rerank.candidates | 10 | 送 Rerank 的候选数 |
| rerank.fp16 | auto | 半精度（GPU 自动开） |
| gate.rerank_floor | 0.05 | 门禁收紧阈值 |
| gate.rerank_rescue | 0.5 | 门禁救援阈值 |
| retrieval_threshold | 0.65 | 稠密距离拒答阈值 |

### 端口

| 端口 | 服务 |
|---|---|
| 5432 | PostgreSQL + pgvector |
| 8000 | 后端 FastAPI |
| 5173 | 前端开发服务器（Vite） |
| 8080 | 生产前端（Nginx，可用 WEB_PORT 覆盖） |

### 关键环境变量（`.env`）

`QWEN_API_KEY`、`DEEPSEEK_API_KEY`、`DATABASE_URL`、`AUTH_JWT_SECRET`、
`AUTH_DEV_MODE`、`SSO_PROVIDER/OIDC_*`、`CORS_ORIGINS`、`MAX_UPLOAD_BYTES`、`AUDIT_LOG_FILE`。

---

## 八、学习路线建议

1. **先跑通**：`README.md` 的快速启动 → `python -m app.cli ask "年假如何计算？"`；
2. **看问答链路**：`app/routers/chat.py` → `app/services/rag.py` → `app/services/retrieval.py` → `app/services/providers.py`（一条线）；
3. **看入库链路**：`app/routers/knowledge.py` → `app/services/ingestion.py` → `app/models/knowledge.py` → `app/core/database.py`；
4. **看认证链路**：`app/routers/middleware.py` → `app/core/security.py` → `app/routers/deps.py` → `app/routers/auth.py`；
5. **看评测**：`eval/README.md` + `app/services/eval.py` + `app/routers/eval.py` + `eval/hr-eval.yaml`；
6. **查细节**：`向量化与检索技术说明`（检索与向量化专题）、
   根目录各份《开发记录》（按日期演进全貌）。

---

## 九、待解决问题与后续规划

> 本节为 2026-09-10 补充，梳理三个待解决问题及其方案；落地状态以
> `待解决问题与解决方案` 为准（会话持久化、自动路由、格式/表格解析均已实现）。
> 上文正文为早期代码快照，09-08 分层重构后文件已迁至
> `app/core/`、`app/models/`、`app/schemas/`、`app/services/`、`app/routers/` 等目录。

### 9.1 问答记录未持久化（切页即失）

- 现象：前端仅用 `useState` 保存消息，路由切换即丢；
- 方案：后端新增 `conversations` / `conversation_messages` 两表 + 会话 API，
  前端加「历史会话列表 + 新建会话」；
- 补充：用户所提的“记忆模块”应拆成两层——**会话持久化**（记录不丢、可审计）与
  **多轮记忆**（历史轮次喂回模型、支持追问），当前痛点仅属第一层，第二层单独立项。

### 9.2 提问前必须选知识库（体验差）

- 现象：`/chat` 的 `knowledge_base` 必填，员工不知道制度归哪个库；
- 方案：把 `knowledge_base` 改为**可选**，为空时**自动路由**——只在当前用户
  **有读权限**的库中检索，跨库聚合 top-k，引用标注来源库；前端默认「自动」，
  折叠「高级」手动指定入口；
- 补充：库少时直接「全库聚合检索」，库多再上「先轻量选库 → 再精检」两阶段路由。

### 9.3 文档类型识别 + 图片/图表/表格解析

- 现状：已支持 `.md/.txt/.docx/文字型 PDF`；不支持 `.xlsx/.pptx/扫描 PDF`，
  PDF/DOCX 内图片、图表、表格未结构化；
- 方案：分层推进——先「格式扩展（xlsx/pptx）+ 表格转 Markdown 文本」，再
  「图片/扫描 OCR + 版面分析 + VLM 生成描述入库」，最后按需「视觉向量多路检索」；
  块类型扩展 `content_type`（text/table/image），图片块存描述 + 原图位置，引用可回看原图。

> 三问题优先级：问题一、二相互独立且无外部依赖，优先并行；问题三周期最长，拆小迭代推进。

---

# 向量化与检索技术说明

> 原文件：向量化与检索技术说明.md

> 本文档描述项目「企业知识库助手」（后端 `E:\EnterpriseKB`）在**文档向量化（入库）**与
> **检索（问答）**两个环节采用的处理方式，参数以 `config/models.yaml` 当前配置为准。

---

## 总览

```text
┌──────────────────────────── 入库（Ingestion）────────────────────────────┐
文档(.md/.txt/.pdf) → 读取文本 → SHA-256 去重 → 语义切块 → 批量 Embedding → pgvector 入库

┌──────────────────────────── 检索（Retrieval）────────────────────────────┐
用户问题 → 问题向量化 → 稠密检索(pgvector 余弦) + 稀疏检索(BM25)
        → RRF 融合 →（可选）BGE Rerank → top_k → 组装上下文 → LLM 回答 + 引用
```

核心思路：**稠密向量负责语义相关，BM25 稀疏负责精确词面命中（专有名词、编号等），
RRF 融合两路候选，BGE 交叉编码器再做精排**，最终把最相关的少量文本块交给大模型作答。

---

## 一、文档向量化（入库）的处理方式

入口：`app/services/ingestion.py` 的 `ingest_file()`。

```text
读文件 → 内容哈希去重 → 校验模型 → 切块 → 批量 Embedding → 写入 pgvector
```

### 1. 读取文档（`read_document()`）

| 类型 | 处理方式 |
|---|---|
| PDF | 用 `pypdf` **逐页**提取文本，页码从 1 开始记录；页面无文字时提取为空字符串 |
| Markdown / TXT | 整篇读取，优先 **UTF-8**；失败降级 **GB18030**（兼容中文 Windows 的 GBK/GB2312）；两种编码都失败则明确报错 |

### 2. 内容哈希去重

- 对文件**原始字节**计算 SHA-256，存入 `documents.content_hash`；
- 同一知识库内哈希相同 → 视为已导入，直接跳过（返回 0），避免重复入库。

### 3. Embedding 模型一致性校验

- 新建知识库时记录 `embedding_model` 与 `embedding_dimensions`；
- 每次导入/问答都会校验当前配置与知识库一致，防止 pgvector 向量维度错配。

### 4. 语义切块（`chunk_text()`）

| 参数/规则 | 取值 |
|---|---|
| 目标块大小 | 650 字符 |
| 相邻块重叠 | 80 字符 |
| 断点优先级 | 换行 > 句末标点（。！？）> 停顿（，；、）> 空格 |
| 硬切条件 | 找不到合适断点（断点位置不足半块）时才硬切 650 |
| 碎片过滤 | 丢弃长度 ≤ 20 字符的残留 |
| 预处理 | 3 个及以上连续换行压缩为 2 个；去首尾空白 |

- 优先在**自然断点**断开，避免切断句子；
- 下一块从上一块结尾**往前回退 80 字**开始，保证跨块语义衔接。

### 5. 批量生成 Embedding（`app/services/providers.py`）

- 调用 **Qwen `text-embedding-v4`**（OpenAI 兼容接口，1024 维）；
- 所有文本块**一次性批量请求**，响应顺序与输入一致；
- 聊天模型与 Embedding 模型分离配置（DeepSeek 无 Embedding 时自动回落 Qwen）。

### 6. 写入数据库（`app/models/knowledge.py`）

每个文本块生成一条 `document_chunks` 记录：

| 字段 | 含义 |
|---|---|
| `content` | 文本块内容 |
| `page_number` | 页码（非 PDF 为 NULL） |
| `chunk_index` | 块在文档内的序号（从 0 开始） |
| `embedding` | pgvector 的 `Vector` 类型（1024 维） |

最后一次性 `commit`，并使该知识库的 BM25 缓存失效（`retrieval.invalidate`）。

---

## 二、检索（问答）时的处理方式

入口：`app/services/rag.py` 的 `ask()` + `app/services/retrieval.py` 的 `hybrid_search()`。

```text
问题向量化 → 稠密检索(pgvector 余弦) + 稀疏检索(BM25)
           → RRF 融合 →（可选）BGE Rerank → top_k → 上下文组装 → LLM 回答 + 引用
```

### 1. 问题向量化

用户问题用**同一个 Embedding 模型**（Qwen `text-embedding-v4`，1024 维）转成向量，
保证与文档块处于同一向量空间。

### 2. 稠密检索（语义匹配）

- 在 pgvector 中用 `cosine_distance` **升序**取前 `candidates=20` 个候选；
- 距离 = 1 − 余弦相似度，越小越相关。

### 3. 稀疏检索（关键词匹配，`Bm25` 类）

- 标准 **Okapi BM25**（k1=1.5，b=0.75）；
- 分词 `tokenize()`：
  - **中文按字符二元组**：`年假如何计算` → `年假 / 假如 / 如何 / 何计 / 计算`；
  - 英文/数字按整词，统一小写；
- 同样取前 `candidates=20` 个候选；
- BM25 索引**按知识库缓存在进程内存**，文档入库/删除时自动失效重建。

> 两路互补：稠密路捕捉语义相关，稀疏路捕捉精确词面（专有名词、编号、工号等）。

### 4. RRF 融合（Reciprocal Rank Fusion）

- 两路候选各自按排名贡献 `1 / (k + rank)`（`k = rrf_k = 60`），同一块分数叠加；
- 取两路候选的**并集**，按融合分数重新排序（分数相同时按稠密距离升序兜底）。

### 5. 三种检索方式（`mode` 可切换）

| mode | 行为 |
|---|---|
| `dense` | 纯向量：直接取稠密前 top_k |
| `hybrid` | RRF 融合后取前 top_k |
| `rerank` | RRF 前 `rerank_candidates=10` 个候选送 BGE 交叉编码器精排，再取前 top_k |

- 最终返回给大模型 **`top_k=5`** 个文本块；
- 缺省检索方式：开启 rerank 时为 `rerank`，否则为 `hybrid`；
- 前端评测页可一键跑三种方式对比指标。

### 6. BGE Rerank（`app/services/rerank.py`）

- 模型：**`bge-reranker-v2-m3`**（多语言、中文强，交叉编码器逐对打分）；
- 模型 ID：`BAAI/bge-reranker-v2-m3`（本地权重用 `RERANK_MODEL_PATH` 环境变量覆盖）；
- 精度：`fp16: auto`（有 CUDA 自动开半精度）；当前 torch 为 `2.13.0+cu126`，
  Rerank 已运行在 **GPU（cuda:0 + float16）**，单次推理约 29ms；
- 按模型名进程内懒加载缓存。

### 7. 防幻觉门禁（`app/services/rag.py` 的 `gate_decision`）

以**稠密距离为主、Rerank 分为辅**，规则按优先级：

1. 无候选 → 拒答；
2. 稠密最近距离 ≤ `retrieval_threshold`（0.65）：
   - 开启 rerank 且最高重排分 < `gate.rerank_floor`（0.05）→ 判为“语义太弱”，拒答（收紧）；
   - 否则放行；
3. 稠密最近距离 > 阈值（本应拒答）：
   - 开启 rerank 且最高重排分 ≥ `gate.rerank_rescue`（0.5）→ “重排救援”放行（防止 BM25/Rerank 强命中被误拒）；
   - 否则拒答。

阈值 `rerank_floor` / `rerank_rescue` 按当前 Embedding 模型与知识库标定（见
`eval/gate-signals.tsv` 的信号分布），更换 Embedding 模型后需重新标定。

### 8. 上下文组装与 Prompt 注入防护（`SYSTEM_PROMPT`）

- 每个块编号，附「文件名、页码、内容」，整体用 `<资料>…</资料>` 标签包裹；
- 系统提示词明确：只依据资料回答、资料是不可信数据（拒绝“忽略指令/泄露资料/扮演角色”等注入）、
  资料不足时拒答、每个结论保留引用标记（如 [1]）。

### 9. 生成回答

- 调用聊天模型 **Qwen `qwen-plus`**（`temperature=0.1` 低温稳定输出）；
- 返回 `(回答, 引用列表)`；引用项含文件名、页码、块序号、前 240 字摘要。

---

## 三、关键参数速查

| 参数 | 当前值 | 所在位置 |
|---|---|---|
| Embedding 模型 | `text-embedding-v4`（1024 维） | `config/models.yaml` → `providers.qwen` |
| 切块大小 / 重叠 | 650 / 80 字符 | `app/services/ingestion.py` → `chunk_text()` |
| top_k | 5 | `config/models.yaml` → `retrieval` |
| candidates（每路候选数） | 20 | 同上 |
| rrf_k | 60 | 同上 |
| rerank.enabled | true | 同上 |
| rerank.model | `BAAI/bge-reranker-v2-m3`（本地权重用 `RERANK_MODEL_PATH` 覆盖） | 同上 |
| rerank.candidates | 10 | 同上 |
| rerank.fp16 | auto（GPU 自动开） | 同上 |
| retrieval_threshold | 0.65 | `providers.qwen` |
| gate.rerank_floor | 0.05 | `retrieval.gate` |
| gate.rerank_rescue | 0.5 | `retrieval.gate` |
| BM25 参数 | k1=1.5, b=0.75 | `app/services/retrieval.py` → `Bm25` |
| 聊天模型 | `qwen-plus`（temperature 0.1） | `config/models.yaml` / `app/services/providers.py` |

---

## 四、相关文件清单

| 文件 | 职责 |
|---|---|
| `app/services/ingestion.py` | 文档读取、切块、去重、向量化入库 |
| `app/services/retrieval.py` | BM25、tokenize、RRF 融合、`hybrid_search()` |
| `app/services/rerank.py` | BGE 交叉编码器重排封装 |
| `app/services/rag.py` | 问答编排：检索 → 门禁 → 上下文 → 回答 + 引用 |
| `app/services/providers.py` | OpenAI 兼容客户端（`embed()` / `chat()`） |
| `app/models/knowledge.py` | 知识库 / 文档 / 文本块三张表 |
| `app/core/config.py` | 配置读取（ProviderConfig / RetrievalConfig） |
| `config/models.yaml` | 模型供应商与检索参数 |

---

## 五、当前限制与后续扩展（2026-09-10 待办）

> 本节为 2026-09-10 补充，记录入库/检索环节的已知限制与规划，**仅规划、暂不实现**；
> 详细方案见 `待解决问题与解决方案`。上文正文为早期快照，
> 09-08 分层重构后个别文件已迁移（如 `app/ingestion.py` → `app/services/ingestion.py`），
> 以当前代码为准。

### 5.1 入库侧：文档类型与多模态内容（对应「问题三」）

- **已支持**：`.md / .txt / .docx（段落 + 表格）/ .xlsx / .pptx / .html / .epub / PDF（正文 + 有边框/无边框表格，均转 Markdown）`；
- **可选能力**：扫描 PDF OCR（`pip install rapidocr-onnxruntime` 后自动识别无文字页面）；
- **块类型**：`DocumentChunk.content_type`（text / table），表格块以 Markdown 表格文本整体入库并按完整行切分；
- **混合 PDF 提示**：部分扫描页未识别时经 warnings 返回（CLI 打印 / 前端展示）；
- **待扩展**：2 列无边框表格、PPTX/HTML/PDF 合并单元格、图片/图表结构化、视觉向量检索；
- **表格**：统一转 Markdown 表格文本，保留行列结构；
- **图片/图表**：OCR + 版面分析 + VLM 生成文字描述后走现有向量管线（轻量路线），
  进阶再引入 CLIP 等视觉向量做多路检索；引用保留页码/图号可回看原图；
- **扫描 PDF**：OCR + 版面分析先行，再走现有流程；
- **块类型扩展**：`DocumentChunk` 增加 `content_type`（text / table / image）。

### 5.2 检索侧：跨知识库与自动路由（对应「问题二」）

- 现状：`/chat` 的 `knowledge_base` 必填，由用户选库；
- 规划：`knowledge_base` 改为可选，为空时自动路由——候选为当前用户**有读权限**
  的全部知识库；库少时直接「全库聚合检索」，库多时「先轻量选库 → 再精检」两阶段；
- 引用需携带 `knowledge_base`，跨库结果统一做防幻觉门禁与来源标注。

### 5.3 会话持久化（对应「问题一」，不在本文档范围内）

- 问答记录的持久化属「会话/消息」层，与本检索专题无关，详见
  `技术全景与学习手册` 第九节与 `待解决问题与解决方案`。

---

# 文档解析与入库技术说明

> 原文件：文档解析与入库技术说明.md

> 记录时间：2026-09-15
> 状态：本文档系统梳理「文档解析 → 切块 → 向量化入库」这一链路的设计、已实现能力、
> 遗留问题与解决方案、以及后续路线图，作为工程化迭代的基准。实现以
> `app/services/ingestion.py` 为准。

---

## 一、定位与总览

文档解析入库是 RAG 系统的「上限」：解析质量决定检索与回答质量。本链路的目标是
把多种格式的企业制度文档，尽可能**结构化、无损、可追溯**地转成文本块并向量化，
同时满足工程化约束（向后兼容、可观测、可测试、可部署、可维护）。

```text
上传（白名单 + 大小限制 + 流式写盘）
  → SHA-256 去重 → 多格式解析（正文 + 表格结构化）
  → 切块（正文按标点 / 表格按完整行） → 批量 Embedding → 写入 pgvector
```

## 二、当前已实现能力

| 格式 | 解析方式 | 表格 | 合并单元格 |
|---|---|---|---|
| `.md` / `.txt` | UTF-8 优先，降级 GB18030 | - | - |
| `.docx` | XML 树按文档顺序识别段落 + `<w:tbl>` | ✅ 转 Markdown | ✅ `gridSpan` + `vMerge` |
| `.xlsx` | openpyxl 逐 sheet | ✅ 整 sheet 转 Markdown | ✅ `merged_cells` 回填 |
| `.pptx` | python-pptx 逐页标题/正文/表格/备注/图表/SmartArt | ✅ 转 Markdown | ✅ `gridSpan` / `rowSpan` |
| `.html` / `.epub` | 标准库 HTMLParser，EPUB 按 OPF spine | ✅ `<table>` 转 Markdown | ✅ `rowspan` / `colspan` |
| `.pdf` | PyMuPDF：正文 + 表格，降级 pypdf | ✅ 有边框（lines）+ 无边框（词坐标） | ✅ `None` 延续位回填 |

配套能力：
- **块类型**：`document_chunks.content_type`（`text` / `table`），旧库幂等 `ALTER TABLE` 补齐；
- **表格切块**：`chunk_table_text()` 按完整行切分，不拆散行列；
- **扫描 PDF OCR**：可选能力（PyMuPDF 渲染 + `rapidocr-onnxruntime`，延迟导入、装即用）；
- **混合 PDF 提示**：部分扫描页经 `warnings` 返回（CLI 打印 / 前端展示），不再静默跳过；
- **审计指标**：上传事件记录 `chunks` 数（`extra` 字段可扩展）。

## 三、关键设计

### 3.1 统一解析结果 `ParsedBlock`

所有 `read_*` 返回 `list[ParsedBlock(text, page_number, content_type)]`，供
`_split_blocks()` 按 `content_type` 分派切块（正文走 `chunk_text`，表格走
`chunk_table_text`），再统一写入 `DocumentChunk`。

### 3.2 表格转 Markdown

`_table_to_markdown()`：第一行视为表头，处理不等长行（补空）、过滤全空行，
输出 `| A | B |` + `|---|---|`；可选 caption（xlsx 的 sheet 名）。

### 3.3 PDF 表格检测（两路）

1. **有边框表格**：`find_tables()` 默认 `lines` 策略，`table.extract()` 直接取文本；
2. **无边框表格**（自研，`_detect_borderless_tables()`）：
   - `get_text("words")` 取词坐标 → 按 y 聚类成行 → 按「相对水平间隙」切单元格
     （用行内间隙中位数做阈值，区分列间空隙与单元格内空格）→ 连续多列行分组 →
     跨行列锚点聚类 → 按锚点填充（允许空单元格）；
   - **门槛：≥2 列**；2 列候选对左侧列做「编号/项目符号」负向过滤，并对长单元格
     （正文/定义列表形态）做长度过滤，避免把编号列表 / 双栏正文误判为表格；
   - 检测到的表格区域会从正文文本块中剔除，避免重复入库。

> PyMuPDF 首次 `find_tables()` 会向 stdout 打印一条 pymupdf_layout 提示，已由
> `_find_tables()` 在进程首次调用时吞掉，避免污染审计日志 JSON 行。

### 3.4 合并单元格处理

- **DOCX**：解析 `<w:gridSpan>`（水平跨列，复制展开）与 `<w:vMerge>`
  （垂直合并，`restart` 值向下填充到 `continue`），展平成规则二维表；
- **XLSX**：改用非 read_only 模式（read_only 无 `merged_cells`），
  对每个合并区域把左上角值回填到区域内所有单元格；
- **PDF**：✅ `extract()` 对合并格返回 `None`（真空格返回 `""`），据此先横向后纵向
  把 `None` 还原为合并主格值、不动真空格；
- **PPTX / HTML**：✅ PPTX 用 DrawingML `gridSpan`/`rowSpan`、HTML 用 `rowspan`/`colspan` 展平。

## 四、遗留问题与解决方案

### 4.1 正确性（功能缺口）

| # | 问题 | 根因 | 解决方案 |
|---|---|---|---|
| 1 | 2 列无边框表格不检测 | 为防「编号列表/标题」误报，门槛设为 ≥3 列 | ✅ 已解决（2026-09-19）：≥2 列 + 编号/项目符号负向过滤 + 长单元格长度过滤 |
| 2 | PPTX / HTML 合并单元格未处理 | ✅ 已解决（2026-09-17） | PPTX 用 DrawingML `gridSpan/rowSpan`；HTML 用 `rowspan/colspan` 展平 |
| 3 | PDF 合并单元格无法可靠还原 | `extract()` 返回空，信息丢失 | ✅ 已解决（2026-09-19）：`None` 延续位先横后纵回填主格值，真空格不动 |
| 4 | PDF 跨页表格被切开、续页丢表头 | `find_tables()` 逐页检测 | ✅ 已解决（2026-09-19）：连续页同列数 + 贴页底/顶 + 首行不同 → 合并并补表头 |
| 5 | 多栏 PDF 版面乱序 | `(y0, x0)` 排序对多栏失效 | ✅ 已解决（2026-09-19）：自研 x 空隙分栏 + 通栏块插入的阅读顺序排序 |
| 6 | 图片/图表完全丢失（第三层未做） | 只提取文本与表格 | 路线 A：OCR + VLM 生成描述入库；路线 B：CLIP 视觉向量多路检索 |
| 7 | 各格式细节缺口 | 未提取 | ✅ 已解决（2026-09-19）：PPTX 备注/图表/SmartArt、XLSX 公式回退 + sheet 多表、DOCX 页眉页脚/文本框、HTML/EPUB 图片 alt |
| 8 | OCR 未落地 + 后处理缺失 | `rapidocr` 未装；按检测框拼接会乱序 | 装 rapidocr 并验证兼容；按检测框坐标排序还原阅读顺序 |

### 4.2 数据质量

| # | 问题 | 解决方案 |
|---|---|---|
| 9 | 超长表格切块后丢表头 | ✅ 已解决（2026-09-17）：每块重复表头行 + 分隔行 |
| 10 | 表格块无 overlap（与文本块不一致） | ✅ 已明确策略（2026-09-17）：表格按完整行切分不重叠，靠重复表头保语义 |
| 11 | 引用摘要对表格不友好（`app/services/rag.py` `_excerpt`） | ✅ 已解决（2026-09-17）：表格分支「表头 + 命中行」 |

### 4.3 健壮性

| # | 问题 | 解决方案 |
|---|---|---|
| 12 | 仅扩展名白名单，不校验内容 | ✅ 已解决（2026-09-17）：magic bytes 内容嗅探，与扩展名交叉校验 |
| 13 | 大 DOCX/PPTX XML 全量读内存 | ✅ 已解决（2026-09-17）：MAX_PARSE_BYTES 文件/解压双重上限 + xlsx 大文件 read_only 切换 |
| 14 | 失败整文件回滚、无部分成功 | ⚠️ 部分解决（2026-09-17）：单事务回滚 + 上传失败审计 `document_upload_failed` |

### 4.4 可观测

| # | 问题 | 解决方案 |
|---|---|---|
| 15 | 解析缺结构化指标 | ✅ 已解决（2026-09-17）：审计 `extra` 增加 `format / text_chunks / table_chunks / skipped_pages / ocr_pages / parse_ms` |
| 16 | OCR 失败静默无诊断 | ✅ 已解决（2026-09-19）：区分三态写 debug 日志 |

### 4.5 可测试 / 可部署

| # | 问题 | 解决方案 |
|---|---|---|
| 17 | 无 pytest | ✅ 已解决（2026-09-19）：`tests/` + pytest（50 例），文档构造器共享于 `tests/fixtures.py` |
| 18 | 无 CI 回归门禁 | ✅ 已解决（2026-09-19）：确定性门禁（compileall+pytest+examples 回归）已 GitHub 实测通过；手动评测回归待配 `QWEN_API_KEY`（见 `.github/workflows/ci.yml`） |
| 19 | 依赖未锁定、兼容未验证 | ⚠️ 部分解决（2026-09-19）：`requirements-lock.txt`；已确认 `rapidocr-onnxruntime>=1.4.0` 与 numpy 2.2.6 兼容，安装实测待网络 |

### 4.6 数据治理

| # | 问题 | 解决方案 |
|---|---|---|
| 20 | `content_type` 迁移未在真实库验证 | ✅ 已解决（2026-09-17）：真实库 `init_db` + 含表格文档端到端确认 |
| 21 | 旧库数据无法区分表格 | ✅ 已解决（2026-09-17）：提供 `reingest` CLI，按新策略全量重导 |
| 22 | 缺「重建索引」机制 | ✅ 已解决（2026-09-17）：`python -m app.cli rebuild <kb>`，事务性替换 |

## 五、路线图

| 阶段 | 内容 | 说明 |
|---|---|---|
| **P0**（止血，收益大） | #9 表格补表头、#12 内容嗅探、#20 迁移验证、#21/#22 重建索引 CLI | 低风险、直接提升正确性与治理 |
| **P1**（正确性深水区） | #1 无边框 2 列、#2/#3 合并单元格、#4 跨页表格、#5 多栏、#7 各格式细节 | ✅ 已完成（2026-09-19，含 `scripts/smoke_ingestion_p1.py` 冒烟） |
| **P2**（补齐可观测/可测试） | #15 解析指标 ✅、#17 pytest ✅（2026-09-19，50 例）、#18 CI ✅（已实测通过） | 工程化底座 |
| **P3**（多模态大工程） | #6 图片/图表（第三层）、#8 OCR 落地 + 后处理 | 依赖 OCR/VLM，独立立项 |

---

## 附：与其它文档的关系

- 检索与向量化专题：`向量化与检索技术说明`
- 三问题整体方案与状态：`待解决问题与解决方案`（问题三）
- 工程化约定：根目录 `AGENTS.md`

---

# 文档入库工程化技术指标

> 原文件：文档入库工程化技术指标.md

> 记录日期：2026-09-17
> 定位：本文档是「文档入库（Ingestion）」环节的**工程化验收口径**，回答两个问题——
> ① 一条文档入库链路要做到什么程度才算「生产可用」；② 如何量化、如何测量、如何验收。
>
> 关联文档：
> - 实现与设计基准：`文档解析与入库技术说明`
> - 三问题整体方案：`待解决问题与解决方案`（问题三）
> - 工程化约定：根目录 `AGENTS.md`

---

## 一、目的与使用方式

文档入库是 RAG 的「上限」，解析质量直接决定检索与回答质量。本文档把“做得好”从
主观判断转成**可测量、可回归、可门禁**的指标，用于：

1. **改前对齐**：动解析代码前先明确要提升哪个指标、目标值多少；
2. **改后自检**：对照指标表逐项确认没有退化；
3. **CI 门禁**：把「确定性指标」接入自动化回归，不达标即阻断发布（见第五节）。

指标遵循一个原则：**能测的必须测，测不了的先建设测量手段**。本文档每项指标都给出
「测量方式」，避免只列口号。

---

## 二、指标体系

共 8 个维度、34 项指标。指标 ID 命名：`COV`（覆盖）/ `ACC`（正确性）/ `ROB`（健壮性）/
`OBS`（可观测）/ `TST`（可测试）/ `DEP`（可部署）/ `PERF`（性能）/ `GOV`（数据治理）。

**现状标记**：✅ 已达标　⚠️ 部分实现 / 未验证　❌ 未实现

### 维度一：格式与内容覆盖（COV）

| ID | 指标 | 定义与量化口径 | 目标值（验收线） | 测量方式 | 现状 |
|---|---|---|---|---|---|
| COV-01 | 格式覆盖率 | 已支持格式数 ÷ 企业文档格式清单数 | 覆盖 `.md/.txt/.docx/.xlsx/.pptx/.html/.htm/.epub/.pdf` 全量 | 格式清单 + 逐格式端到端入库冒烟 | ✅ 9 种原生格式全覆盖；老格式入库时筛除（提示转换，不计入覆盖） |
| COV-02 | 扫描 PDF 可入库率 | 扫描页（无文字层）经 OCR 产出非空文本的页数 ÷ 扫描页总数 | 100%（安装 OCR 依赖后） | 构造扫描 PDF 样本集，统计 `skipped_pages` | ⚠️ OCR 壳已搭、依赖未装、后处理缺失 |
| COV-03 | 内容形态覆盖 | 入库内容覆盖 正文 / 表格 / 图片描述 三类 | 正文 ✅、表格 ✅、图片描述（VLM）列为 P3 | 统计 `document_chunks.content_type` 分布 | ⚠️ 仅 text/table，image 未做 |
| COV-04 | 编码兼容率 | 常见中文编码可正确解码的文件占比 | UTF-8 / GBK / GB18030 / Big5 / UTF-16 全覆盖 | 构造多编码样本集 | ✅ 已实现（UTF-8 → charset-normalizer → GB18030） |

### 维度二：解析正确性与结构保真（ACC）

| ID | 指标 | 定义与量化口径 | 目标值（验收线） | 测量方式 | 现状 |
|---|---|---|---|---|---|
| ACC-01 | 表格行列还原准确率 | 行列数与原文一致的表格数 ÷ 标注表格总数（分“有边框 / 无边框”分别统计） | 有边框 ≥95%；无边框（≥2 列）≥90% | 标注集 + `_table_to_markdown` 输出对比 | ⚠️ 有边框/无边框（含 2 列）均已实现，未建标注集量化 |
| ACC-02 | 合并单元格还原准确率 | 展开填充后行列对齐正确的表格数 ÷ 含合并单元格表格总数 | DOCX / XLSX / HTML / PPTX / PDF ≥95% | 构造含合并单元格样本 + 参数化测试 | ✅ 已实现（DOCX/XLSX/HTML/PPTX/PDF 均展开） |
| ACC-03 | 阅读顺序正确率 | 多栏 / 图文混排文档中，块排序与人工阅读顺序一致的比例 | ≥90% | 标注多栏样本，对比块序列 | ✅ 已实现（2026-09-19：x 空隙分栏 + 通栏块插入），未建标注集量化 |
| ACC-04 | 正文表格不重复率 | 表格内文字未被重复作为正文入库的页数 ÷ 含表格页总数 | 100%（重复入库 = 0） | 对同一文档统计「表格文字是否同时出现在 text 块」 | ⚠️ 已实现区域剔除，未量化验证 |
| ACC-05 | 表格切块表头保留率 | 超长表格被切块后，每个后续块仍含表头的块数占比 | 100% | 构造超长表格，检查每块首行 | ✅ 已实现（后续块重复表头 + 分隔行） |
| ACC-06 | 表格引用摘要可用性 | 引用摘要能体现「表头 + 命中行」的比例 | 100% | 抽查含表格引用，人工/脚本判断 | ✅ 已实现（表头 + 命中行，按命中分数排序） |
| ACC-07 | 表格列召回增强 | 表头与行内容拼接成 `key: value` 的表格块占比 | 100%（表格语义增强后） | 检查 table 块是否含「表头:值」形态 | ✅ 已实现（`_table_to_markdown` enrich_cells，列名合入单元格） |

### 维度三：健壮性与容错（ROB）

| ID | 指标 | 定义与量化口径 | 目标值（验收线） | 测量方式 | 现状 |
|---|---|---|---|---|---|
| ROB-01 | 坏文件明确拒绝率 | 对空文件/损坏文件/伪造后缀，返回明确错误而非 500/崩溃的占比 | 100% | 构造坏样本（空文件、截断 zip、改后缀）批量入库 | ⚠️ 空文件/损坏文件有处理，伪造后缀未防 |
| ROB-02 | 内容嗅探覆盖 | 用 magic bytes 识别真实类型并校验扩展名的文件占比 | 100% | 对伪造后缀样本断言被拦截 | ✅ 已实现（magic bytes + 扩展名交叉校验） |
| ROB-03 | 解析降级链覆盖率 | 每种格式拥有 ≥1 条 fallback 路径的格式占比 | 100% | 检查各 `read_*` 是否有备用解析器 | ✅ 已实现：PDF PyMuPDF→pypdf、docx/xlsx 结构化→文本兜底 |
| ROB-04 | 单文件内存峰值 | 解析单个文件的最大内存占用 | ≤ 512MB，超限切流式或拒绝 | 大文件压测 + `tracemalloc`/进程 RSS | ✅ 已实现（MAX_PARSE_BYTES 文件/解压双重上限 + xlsx 大文件 read_only 切换） |
| ROB-05 | 事务完整性 | 入库失败后不产生“半截文档”的比例 | 100%（0 半截入库） | 注入中途异常，断言 documents/chunks 无残留 | ⚠️ 依赖 `session.commit()`，未系统验证（问题 #14） |

### 维度四：可观测性（OBS）

| ID | 指标 | 定义与量化口径 | 目标值（验收线） | 测量方式 | 现状 |
|---|---|---|---|---|---|
| OBS-01 | 解析指标完整率 | 上传审计事件的 `extra` 含 `format/text_chunks/table_chunks/skipped_pages/parse_ms/ocr_pages` 的字段覆盖率 | 100% | 检查 `audit` 事件字段 | ✅ 已实现（extra 含完整解析指标） |
| OBS-02 | OCR 三态诊断 | OCR 失败可区分「未装依赖 / 识别异常 / 空结果」三态 | 三态可区分 | 分别模拟三种情况查看日志 | ✅ 已实现（2026-09-19）：三态 debug 日志 |
| OBS-03 | 警告可追溯 | 解析非致命警告（扫描页跳过等）在 CLI 与 API 均可见 | 100% | 上传混合 PDF，断言前端/CLI 收到 warning | ✅ 已实现 |

### 维度五：可测试与可回归（TST）

| ID | 指标 | 定义与量化口径 | 目标值（验收线） | 测量方式 | 现状 |
|---|---|---|---|---|---|
| TST-01 | pytest 用例覆盖 | 各 `read_*` / 表格转 Markdown / 切块 / 无边框检测 / 合并单元格的参数化用例数 | 核心链路 100% 覆盖，用例 ≥30 条 | `pytest --cov` 统计 | ✅ 已建 `tests/`（50 例 pytest，DB-free）；`--cov` 覆盖率统计待接入 |
| TST-02 | 真实样本回归集 | 固定真实文档（含表格/扫描/多栏/合并单元格）纳入回归 | ≥10 篇真实文档 | `examples/` + 断言解析产物稳定 | ⚠️ examples 共 9 个样例纳入回归（含 5 篇真实制度文档，`tests/test_examples_regression.py`）；真实样本量未达 10 篇，且缺扫描/多栏/合并单元格真实样本 |
| TST-03 | CI 门禁 | CI 自动跑 pytest + 解析回归 + 评测 hit@k/MRR | 不达标阻断发布 | CI 配置 | ✅ 确定性门禁（compileall+pytest+examples 回归）已 GitHub 实测通过；评测回归（手动触发）待配 `QWEN_API_KEY` secret 实测 |

### 维度六：可部署与依赖治理（DEP）

| ID | 指标 | 定义与量化口径 | 目标值（验收线） | 测量方式 | 现状 |
|---|---|---|---|---|---|
| DEP-01 | 重依赖可选性 | 未安装 OCR / FlagEmbedding 等重依赖时，核心入库不崩溃且给出明确提示 | 100% | 卸载可选依赖后跑核心链路 | ✅ 延迟导入 + 明确提示 |
| DEP-02 | 依赖锁定与兼容 | 关键依赖版本锁定，且 rapidocr/onnxruntime 与 numpy/torch 矩阵实测通过 | 100% | 版本矩阵实测 + lock 文件 | ✅ 已锁 `requirements-lock.txt`（含 OCR 精确版本）；已实测 `rapidocr-onnxruntime==1.4.4` 与 numpy 2.2.6 兼容；opencv 系统库问题用 `opencv-python-headless` 解决，线上 OCR 冒烟通过（2026-09-23） |
| DEP-03 | 镜像可构建 | Docker 镜像构建路径在新依赖加入后仍可用 | 100% | `docker build` 冒烟 | ⚠️ 已拆 `requirements-rerank.txt` + 下载脚本/文档；镜像构建待实测 |

### 维度七：性能与成本（PERF）

| ID | 指标 | 定义与量化口径 | 目标值（验收线） | 测量方式 | 现状 |
|---|---|---|---|---|---|
| PERF-01 | 纯文本解析吞吐 | 每秒可解析的文本型 PDF/文档页数 | 单页 ≤50ms（文字型） | 批量压测 | ⚠️ 未量化 |
| PERF-02 | 扫描页 OCR 单页耗时 | 单页扫描 PDF OCR 耗时 | ≤1s/页（CPU） | 扫描样本压测 | ⚠️ 未装依赖、未测 |
| PERF-03 | 批量 Embedding 稳健性 | 大文件切块后单次批量 Embedding 的失败重试与顺序一致性 | 100% 顺序一致、可重试 | 大文件入库验证 | ✅ 批量调用、顺序校验已做 |

### 维度八：数据治理（GOV）

| ID | 指标 | 定义与量化口径 | 目标值（验收线） | 测量方式 | 现状 |
|---|---|---|---|---|---|
| GOV-01 | 内容去重命中率 | 同内容重复导入时按 SHA-256 跳过且返回 0 的占比 | 100% | 重复导入同一文件断言返回 0 | ✅ 已实现 |
| GOV-02 | 内容类型迁移验证 | `content_type` 幂等迁移在**真实库**端到端验证通过 | 通过 | 启动 `init_db` + 导入含表格文档确认 | ✅ 已通过（2026-09-17 真实库验证） |
| GOV-03 | 旧库重导能力 | 提供 `reingest` CLI 按新策略全量重导旧库 | 可用 | 对旧库执行 reingest 并比对 | ✅ 已实现（`python -m app.cli reingest`） |
| GOV-04 | 重建索引能力 | 提供 `rebuild <kb>` CLI，事务性替换现有索引 | 可用 | 改配置后 rebuild 并断言可检索 | ✅ 已实现（`python -m app.cli rebuild`） |
| GOV-05 | 增量同步与版本 | 文档变更（mtime/size）可检测并重建，版本可追溯 | 可用 | 修改文档后增量同步 | ⚠️ 已实现 sync（mtime/size 变更检测）+ 单测；DB 端到端待验证 |
| GOV-06 | 表格块质量基线 | 表格块的检索 hit@k/MRR 有量化基线并纳入回归 | 建立基线 | 评测集含表格题 | ⚠️ 已建 table-eval.yaml + 表格样本（对齐已验证）；基线数值待 DB 采集 |

---

## 三、当前项目在文档入库的遗留问题清单

> 编号沿用 `文档解析与入库技术说明` 第四节（#1–#22），并补充本次从代码与
> 指标反推的新增项（#23–#32）。优先级：**P0 止血 / P1 正确性 / P2 底座 / P3 多模态**。

### 3.1 正确性（功能缺口）

| # | 问题 | 根因 / 影响 | 解决方案 | 优先级 |
|---|---|---|---|---|
| 1 | 2 列无边框表格不检测 | ≥3 列门槛防误报，2 列被漏 | ✅ 已解决（2026-09-19）：≥2 列 + 编号/项目符号负向过滤 + 长单元格长度过滤 | P1 |
| 2 | PPTX / HTML 合并单元格未处理 | ✅ 已解决（2026-09-17） | PPTX 用 DrawingML `gridSpan/rowSpan`；HTML 用 `rowspan/colspan` 展平 | P1 |
| 3 | PDF 合并单元格无法可靠还原 | `extract()` 返回空、信息丢失 | ✅ 已解决（2026-09-19）：`None` 延续位先横后纵回填，真空格不动 | P1 |
| 4 | PDF 跨页表格被切开、续页丢表头 | `find_tables()` 逐页检测 | ✅ 已解决（2026-09-19）：连续页同列数 + 贴页底/顶 + 首行不同 → 合并补表头 | P1 |
| 5 | 多栏 PDF 版面乱序 | `(y0, x0)` 排序对多栏失效 | ✅ 已解决（2026-09-19）：自研 x 空隙分栏 + 通栏块插入的阅读顺序排序 | P1 |
| 6 | 图片/图表完全丢失 | 只提取文本与表格 | 路线 A：OCR + VLM 描述入库；路线 B：CLIP 视觉向量多路检索 | P3 |
| 7 | 各格式细节缺口 | 未提取 | ✅ 已解决（2026-09-19）：PPTX 备注/图表/SmartArt、XLSX 公式回退 + sheet 多表、DOCX 页眉页脚/文本框、HTML/EPUB 图片 alt（EPUB 图片 OCR 属 #6） | P1–P2 |
| 8 | OCR 未落地 + 后处理缺失 | rapidocr 未装；检测框拼接乱序 | 装 rapidocr 并验证兼容；按坐标排序还原阅读顺序、去水印 | P3 |
| 23 | 老格式 `.doc/.xls/.ppt/.rtf/.odt` 不支持 | 企业存量文档常见 | ✅ 已解决（2026-09-19）：入库时筛除老格式并提示转换为新格式（放弃 LibreOffice 转换） | P2 |
| 24 | 编码探测仅 UTF-8→GB18030 | ✅ 已解决（2026-09-17） | UTF-8 → charset-normalizer → GB18030，覆盖 GBK/Big5/UTF-16 | P1 |
| 25 | 表格语义未增强 | ✅ 已解决（2026-09-17）：`_table_to_markdown` enrich_cells，非空单元格合入列名 | P1 |
| 26 | PPTX/XLSX 超大文件内存风险 | ✅ 已解决（2026-09-17）：`_check_zip_expansion` + xlsx 大文件 read_only 切换 | P1 |

### 3.2 数据质量

| # | 问题 | 解决方案 | 优先级 |
|---|---|---|---|
| 9 | 超长表格切块后丢表头 | ✅ 已解决（2026-09-17）：每块重复表头行 + 分隔行 | P0 |
| 10 | 表格块无 overlap（与文本块不一致） | ✅ 已明确策略（2026-09-17）：表格块按完整行切分**不重叠**，靠重复表头保语义 | P1 |
| 11 | 引用摘要对表格不友好 | ✅ 已解决（2026-09-17）：`_excerpt` 表格分支「表头 + 命中行」 | P1 |

### 3.3 健壮性

| # | 问题 | 解决方案 | 优先级 |
|---|---|---|---|
| 12 | 仅扩展名白名单，不校验内容 | ✅ 已解决（2026-09-17）：magic bytes 内容嗅探 + 扩展名交叉校验 | P0 |
| 13 | 大 DOCX/PPTX XML 全量读内存 | ✅ 已解决（2026-09-17）：MAX_PARSE_BYTES 文件/解压双重上限 | P1 |
| 14 | 失败整文件回滚、无部分成功 | ⚠️ 部分解决（2026-09-17）：单文件单事务提交已保证回滚；上传失败已写 `document_upload_failed` 审计，`ProviderError` 统一封装模型调用异常 | P1 |
| 27 | 部分格式缺解析降级链 | docx/xlsx 等单解析器增加备用（如 python-docx 回退） | ✅ 已解决（2026-09-19）：docx/xlsx 结构化失败降级为文本兜底 | P2 |

### 3.4 可观测

| # | 问题 | 解决方案 | 优先级 |
|---|---|---|---|
| 15 | 解析缺结构化指标 | ✅ 已解决（2026-09-17）：审计 `extra` 增加 `format/text_chunks/table_chunks/skipped_pages/ocr_pages/parse_ms` | P0 |
| 16 | OCR 失败静默无诊断 | 区分「未装依赖 / 识别异常 / 空结果」三态写 debug 日志 | ✅ 已解决（2026-09-19）：三态 debug 日志 | P2 |

### 3.5 可测试 / 可部署

| # | 问题 | 解决方案 | 优先级 |
|---|---|---|---|
| 17 | 无 pytest | ✅ 已解决（2026-09-19）：`tests/` + pytest（50 例）+ `scripts/smoke_ingestion_p1.py` 复用 `tests/fixtures.py` | P2 |
| 18 | 无 CI 回归门禁 | ✅ 已解决（2026-09-19）：确定性门禁（compileall+pytest+examples 回归）已 GitHub 实测通过；手动评测回归（dense/hybrid）待配 `QWEN_API_KEY`；rerank 因本地模型权重不入 CI | P2 |
| 19 | 依赖未锁定、兼容未验证 | rapidocr/onnxruntime 与 numpy/torch 矩阵实测；版本锁定 | ✅ 已解决（2026-09-23）：`requirements-ocr.txt` + `requirements-lock.txt` 精确版本；`opencv-python-headless` 替代完整版，Dockerfile `INSTALL_OCR` 分支；线上 OCR 冒烟通过 | P2 |
| 28 | 重排/OCR 模型未纳入镜像分发 | 部署镜像纳入 FlagEmbedding 与 OCR 模型分发 | ⚠️ 部分解决（2026-09-19）：`requirements-rerank.txt` 拆出 + `download-rerank-model.ps1` + DEPLOY 说明；镜像构建待实测 | P2 |

### 3.6 数据治理

| # | 问题 | 解决方案 | 优先级 |
|---|---|---|---|
| 20 | `content_type` 迁移未在真实库验证 | ✅ 已解决（2026-09-17）：真实库 `init_db` + 含表格文档端到端确认 | P0 |
| 21 | 旧库数据无法区分表格 | ✅ 已解决（2026-09-17）：提供 `reingest` CLI，按新策略全量重导 | P0 |
| 22 | 缺「重建索引」机制 | ✅ 已解决（2026-09-17）：`python -m app.cli rebuild <kb>`，事务性替换 | P0 |
| 29 | 文档版本 / 增量同步缺失 | 记录版本与 `mtime/size`，变更检测后增量重建 | ⚠️ 部分解决（2026-09-19）：`sync` 命令 + 变更检测单测；DB 端到端待验证 | P2 |
| 30 | 表格块质量无量化基线 | 评测集补表格题，量化表格 hit@k/MRR | ⚠️ 部分解决（2026-09-19）：`table-eval.yaml` + 表格样本（对齐已验证）；基线数值待 DB 采集 | P2 |

### 3.7 遗留问题优先级汇总

| 优先级 | 编号 | 说明 |
|---|---|---|
| **P0（止血）** | ✅ 已全部解决（2026-09-17） | #9、#12、#15、#20、#21、#22 已落地并真实库验证 |
| **P1（正确性深水区）** | ✅ 已全部解决（2026-09-19） | #1、#3、#4、#5、#7 已落地；另补充 `scripts/smoke_ingestion_p1.py` 冒烟脚本 |
| **P2（底座）** | ✅ #16、#17、#18、#23、#27 已解决；#19、#28、#29、#30 部分解决 | 剩余需外部环境（DB / 镜像构建 / 独立依赖矩阵）实测 |
| **P3（多模态大工程）** | #6、#8 | 依赖 OCR/VLM，独立立项 |

---

## 四、指标与遗留问题的映射

每项指标都能回溯到具体遗留问题，便于「先立指标 → 再消问题」：

| 指标 | 对应遗留问题 |
|---|---|
| COV-01 / COV-04 | #23、#24 |
| COV-02 / COV-03 | #6、#8 |
| ACC-01 ~ ACC-03 | #1、#3、#5 |
| ACC-02 | #2 |
| ACC-04 ~ ACC-07 | #4、#9、#10、#11、#25 |
| ROB-01 ~ ROB-05 | #12、#13、#14、#27 |
| OBS-01 ~ OBS-03 | #15、#16 |
| TST-01 ~ TST-03 | #17、#18 |
| DEP-01 ~ DEP-03 | #19、#28 |
| GOV-01 ~ GOV-06 | #20、#21、#22、#29、#30 |

---

## 五、验收门禁建议（接入 CI 后）

1. **必须通过（确定性）**：`py_compile`/`compileall`、`pytest`（reader / 表格 / 切块 /
   无边框 / 合并单元格）、examples 真实文档解析回归、`ROB-01/02` 坏文件与伪造后缀用例；
2. **必须不退化（量化基线）**：评测集 `hit@k / MRR / 拒答正确率` 对比上一基线，不达标阻断；
3. **发布前人工确认**：`COV-01` 格式覆盖清单、`DEP-03` 镜像构建冒烟、
   `GOV-02` 真实库 `content_type` 迁移验证。

---

## 六、修订记录

| 日期 | 修订内容 |
|---|---|
| 2026-09-17 | 首版：建立 8 维度 34 项指标体系；汇总遗留问题 #1–#30 并分级 |
| 2026-09-17 | P0 全部落地：表格补表头、内容嗅探、解析指标、迁移验证、reingest/rebuild CLI |
| 2026-09-17 | P1 第一批：编码探测、HTML/PPTX 合并单元格、表格引用摘要、上传失败审计、ProviderError 封装 |
| 2026-09-17 | P1 第二批：表格语义增强（enrich_cells）、解析内存保护（MAX_PARSE_BYTES + zip 膨胀防护 + xlsx 流式） |
| 2026-09-19 | P1 第三批：#1 2 列无边框表、#3 PDF 合并单元格、#4 PDF 跨页表格续接、#5 多栏阅读顺序、#7 各格式细节；新增 `scripts/smoke_ingestion_p1.py` |
| 2026-09-19 | P2 部分：#17 建 `tests/` + pytest（26 例，含 `tests/fixtures.py` 共享文档构造器），#18 CI 仍待接入 |
| 2026-09-19 | P2 续：#18 搭 `.github/workflows/ci.yml`；`app.eval` / `app.cli eval` 新增 `--modes`（三方式对比 CLI）；FlagEmbedding 拆为可选 `requirements-rerank.txt` |
| 2026-09-19 | P2 第三批：#16 OCR 三态 debug 日志、#27 docx/xlsx 解析降级链、#19 依赖锁定、#23 老格式 LibreOffice 转换、#29 增量同步 sync、#30 表格基线样本/评测集、#28 模型下载脚本与部署说明 |
| 2026-09-19 | P2 收尾：#18 CI 已 GitHub 实测通过（含 OCR 用例无 numpy 跳过修复）；pytest 达 50 例；#19 定位 `rapidocr-onnxruntime>=1.4.0` 与 numpy 2.2.6 兼容，安装实测待网络 |
| 2026-09-23 | #19 依赖兼容闭环：`opencv-python-headless` 替代完整版、`requirements-ocr.txt` + `requirements-lock.txt` 精确版本、Dockerfile `INSTALL_OCR` 分支 |

---

# 记忆策略与方案

> 原文件：记忆策略与方案.md

> 记录时间：2026-09-13
> 状态：策略梳理已完成；**会话内短期记忆（方案 D）与证据窗口（方案 A）已落地实现**；
> 跨会话长期记忆 / 知识沉淀待选型后实施。
> 关联文档：`待解决问题与解决方案`（问题一：记忆模块两层划分）。

本文档沉淀「记忆」相关知识点：先把记忆按场景分层，再逐一给出对应方案与取舍，
并记录最终选定的会话内短期记忆（方案 D）的详细设计与判定规则，供后续查阅与迭代。

---

## 一、记忆分层总览

判断标准：本项目核心场景是「员工查制度」，因此优先级：**会话内短期 > 跨会话长期 > 知识沉淀**。

| 层次 | 记住多久 | 记住什么 | 现状 |
|---|---|---|---|
| 会话持久化 | 永久（落库） | 每轮问/答、引用、门禁判定 | ✅ 已完成（09-11，`conversations` / `conversation_messages`） |
| 会话内短期记忆 | 单次会话内 | 前几轮上下文，支撑追问、指代消解 | ✅ 已完成（本次，方案 D） |
| 跨会话长期记忆 | 永久、跨会话 | 用户身份 / 偏好 / 习惯，支撑个性化 | ❌ 未做（建议先做「结构化画像」轻量版） |
| 知识沉淀 | 永久、进知识库 | 对话中值得沉淀的可复用事实 | ❌ 未做（属增强项） |

---

## 二、场景一：会话内多轮追问（短期记忆）

**典型场景**：用户问「合同金额 100 万以上谁审批？」，接着问「那 100 万以下的呢？」——
第二个问题脱离了检索语境，需要把历史带进去才能答对。

| 方案 | 做法 | 优点 | 代价 / 风险 | 适用 |
|---|---|---|---|---|
| A. 最近 N 轮原文回填 | 把最近 N 轮 user/assistant 文本直接拼进 prompt | 实现最简单，指代消解强 | token 随轮数线性涨；历史旧引用易与新检索混淆；不改写则「100万以下的呢」检索不到 | 轮数少、要快 |
| B. 查询改写（Query Rewriting） | 追问先让 LLM 结合历史改写为独立问题，再走现有检索 | 检索质量最高，引用干净、来源可溯源 | 每轮多一次 LLM 调用（成本 + 延迟）；依赖改写质量 | RAG 场景首选 |
| C. 滚动摘要（Summary Memory） | 历史逐轮压缩成摘要，只带摘要 + 最近 1-2 轮原文 | token 可控，长对话不爆窗口 | 摘要丢细节、可能丢关键数字（金额 / 文号）；引用易失真 | 超长对话 |
| D. 混合：改写 + 短窗口原文 + 引用复用 | B 为主，加最近 1-2 轮原文做指代补充；纯加工类追问复用上一轮引用 | 检索准 + 指代稳 + 引用可追溯 | 复杂度最高，要定义「何时复用引用」 | 生产级首选 |

**关键设计点**：
1. **是否重新检索**：追问若沿用上一轮检索结果，引用归属要标「复用上一轮」；否则走改写后检索。
2. **引用归属**：多轮回答的 `[1][2]` 要能追溯到具体哪一轮的 `citations`（JSONB 已存好，可复用）。
3. **历史写入位置**：改写 / 摘要所需历史，从 `conversation_messages` 取最近 N 条即可，**无需新表**。

**选定方案：D（混合）**，详细设计见第四节。

---

## 三、场景二：跨会话长期记忆（用户画像 / 偏好）

**典型场景**：记住「我是财务部的」「我常查合同金额审批」「我偏好看条文原文而不是大白话」，
下次跨会话直接适用。

| 方案 | 做法 | 优点 | 代价 / 风险 | 适用 |
|---|---|---|---|---|
| A. 结构化画像表 | 新建 `user_profiles` 表存固定槽位（部门、角色、偏好回答风格、常用知识库） | 确定性高、可控、审计友好、零额外检索 | 槽位要预定义，扩展需改表 | 企业内网、字段已知（推荐先做） |
| B. 记忆库（向量检索式） | 「重要记忆」写成自然语言条目 + 向量，回答时检索相关记忆注入 prompt | 灵活、可自然语言扩展 | 多一路检索 + embedding 成本；可能检索到无关 / 过时记忆；需「记忆淘汰」机制 | 长期灵活积累 |
| C. 摘要式长期记忆 | 每次会话结束后 LLM 生成用户摘要，随会话滚动合并 | 实现中等 | 细节丢失、合并策略复杂 | 需积累到一定量级 |
| D. 混合：结构化为主 + 记忆库补充 | A 存确定字段，B 存少量自由记忆 | 兼顾确定性与灵活 | 两套都要维护 | 成熟期 |

---

## 四、场景三：对话中沉淀知识（记忆 → 知识库）

**典型场景**：用户在对话中发现「制度里没写，但口头惯例是 X」，希望沉淀为可检索知识。

| 方案 | 做法 | 风险 / 代价 | 适用 |
|---|---|---|---|
| A. 人工标记 → 审核 → 入库 | 用户在界面对某轮回答点「沉淀」，管理员审核后写回知识库 | 最稳、可审计 | 现阶段若做，只建议此方案 |
| B. LLM 自动抽取候选 → 人工审核 | 每轮让 LLM 判断是否含「值得沉淀的事实」，生成候选进审核队列 | 有噪声、需审核界面 | 量级上来后 |
| C. 自动直接入库 | 不加人工直接写知识库 | ❌ 高风险，污染知识库、破坏门禁标定 | 不推荐 |

---

## 五、场景四：任务工作记忆（多步任务中间态）—— 现阶段跳过

用户「帮我对比三份合同的审批金额」这类多步任务需要暂存中间结果。
本项目目前是单轮查制度问答，**暂不需要**；等引入 MCP / 多工具 Agent 时再考虑。

---

## 六、方案 D 详细设计（已实现）

### 6.1 数据流

```text
会话内新消息
  → 取最近 N 条历史（conversation_messages，改前）
  → LLM 一次调用：输出 {type, query}
     type ∈ new / followup / reuse
  → 分支：
     new       → 用原问题检索（引用全新）
     followup  → 用 query（改写后的完整问题）重新检索（引用全新）
     reuse     → 不检索，复用上一轮已存 context_chunks 与 citations，直接基于原文作答
  → 生成回答 → 落库（assistant 消息携带 citations + decision + context_chunks）
```

### 6.2 三类意图（由改写 LLM 输出）

| type | 含义 | query | 引用 |
|---|---|---|---|
| `new` | 与历史无关的全新问题 / 首问 | 原问题 | 本轮新检索 |
| `followup` | 承接前文、省略 / 指代前文实体，需要新证据 | 改写后的完整独立问题 | 本轮新检索 |
| `reuse` | 不引入新证据需求，仅对上一轮回答加工 | 空 | 复用上一轮 citations |

### 6.3 何时复用引用——判定规则（可落地、可审计）

**核心判据不是「像不像上一轮」，而是「是否引入了新的证据需求」**：

- 只要需要新事实 / 新对象 / 新金额 / 新条件 / 新时间 → **followup**（重新检索，引用全新）；
- 只要是对既有回答本身的加工（展开、举例、总结、翻译、换个说法、解释某一条）→ **reuse**（引用 = 上一轮，且基于已存 chunk 原文作答，保证能「展开」）。

**确定性兜底（不依赖 LLM 判断也成立）**：

1. 会话首条消息（无历史）→ `new`，无历史可复用；
2. `reuse` 必须满足：上一轮 assistant 消息存在、且 `context_chunks` 与 `citations` 均非空；
   否则**降级为 `followup`**（改写重检，改写失败则用原问题检索）；
3. 改写 LLM 输出解析失败 / 字段缺失 / 类型非法 → 降级为 `new`（原问题直接检索），保证永不因改写而中断问答。

### 6.4 LLM 输出协议（结构化 JSON）

```json
{"type": "new|followup|reuse", "query": "改写后的完整检索问题（reuse 时为空字符串）"}
```

解析时从原始文本中截取第一个 `{` 到最后一个 `}` 再 `json.loads`，失败按 6.3 兜底。

### 6.5 数据模型变更

- `conversation_messages` 新增 `context_chunks`（JSONB，可空）：存本轮检索到的完整文本块
  （`knowledge_base / filename / page_number / chunk_index / content / excerpt`）。
  **目的**：`reuse`（如「展开讲讲」）需要上一轮检索的**完整原文**，而 `citations` 只存了 280 字摘要，
  不足以支撑展开。该字段不向前端暴露（`_serialize_message` 不序列化它）。
- 通过 `init_db()` 幂等 `ALTER TABLE conversation_messages ADD COLUMN IF NOT EXISTS context_chunks JSONB` 补齐旧库。

### 6.6 配置（`config/models.yaml` 的 `memory` 段）

```yaml
memory:
  enabled: true              # 关闭则退回单轮问答
  max_history_messages: 6    # 改写时回看的历史消息条数（user/assistant 交替）
  evidence_window_rounds: 3  # reuse 时合并最近几轮 assistant 的检索块（证据窗口，按块去重）
```

### 6.7 涉及文件

- 新增 `app/services/memory.py`：`build_turn_plan()`（历史加载 + 改写判定 + 兜底）、改写提示词、
  `merge_chunk_lists()` / `_recent_evidence_windows()`（方案 A 证据窗口）;
- 修改 `app/services/rag.py`：`serialize_chunks()` / `context_from_stored_chunks()` /
  `citations_from_stored_chunks()` / `answer_from_stored()` / `iter_answer_reuse_events()`；
  `iter_answer_events()` 增加内部 `chunks` 事件（供落库，前端不感知）；
- 修改 `app/routers/conversations.py`：会话内问答（非流式 / 流式）接入 `build_turn_plan`，
  落库 `context_chunks`；
- 修改 `app/models/conversation.py`、`app/core/database.py`、`app/core/config.py`、`config/models.yaml`。

### 6.8 成本与延迟说明

- 每条会话内消息（非首条）会额外调用一次聊天模型做改写（同步，流式开始前完成）；
- `max_history_messages` 控制回看窗口，避免历史过长撑爆 prompt；
- `evidence_window_rounds` 控制 reuse 时合并的最近轮数，越大证据越全、上下文越长；
- `memory.enabled=false` 可整体关闭，完全回到单轮行为。

---

## 七、分阶段路线建议

1. **一期（已完成）**：会话内短期记忆——方案 D（改写 + 短窗口 + 引用复用）+ 方案 A（证据窗口）；
2. **二期（可选）**：方案 B 轻量版（改写输出 `ref_rounds`，按轮次决定证据窗口范围）；
3. **三期（可选）**：跨会话长期记忆——结构化画像轻量版（`user_profiles` 表：
   部门 / 角色 / 偏好风格 / 常用知识库，登录加载，回答 prompt 末尾附一句画像提示）；
4. **四期（按需）**：知识沉淀——人工标记 → 审核 → 入库。

---

## 八、会话内记忆的局限与升级方案（2026-09-13 梳理，供用量上来后选型）

### 8.1 当前实现的实际行为

| 维度 | 行为 |
|---|---|
| 理解上下文（指代消解） | 改写 LLM 回看 `max_history_messages`=6 条消息（约最近 3 轮） |
| 证据复用 | 方案 A 后：合并最近 `evidence_window_rounds`=3 轮 assistant 的检索块并去重 |
| 回答带入的历史 | reuse 回答 prompt 带最近 6 条历史文本（仅用于理解所指，回答以资料为准） |

### 8.2 当前方案的局限

1. 固定窗口截断：超过窗口的更早轮次指代会丢失；
2. 指代消解靠一次 LLM 输出：多跳追问（“上一轮提到的那个部门，他们的报销标准？”）可能改写不完整；
3. 跨轮引用编号各自从 `[1]` 起，合并展示时可能混淆（当前前端按轮展示，暂未暴露）；
4. 证据窗口是「按轮数」而非「按语义相关性」合并，无关轮次可能被一并带进上下文。

### 8.3 升级方案清单

| 方案 | 做法 | 收益 | 成本 | 状态 |
|---|---|---|---|---|
| A. 证据窗口 | 最近 N 轮检索块去重合并，随问题一起作资料 | 支持「对比 / 综合多轮」追问 | 低 | ✅ 已实现 |
| B. 结构化指代消解 | 改写输出 `{type, query, hops, ref_rounds}`，按 `ref_rounds` 决定并入哪些轮次 | 多跳追问更准、窗口更聚焦 | 中 | 待选型 |
| C. 滚动摘要 + 关键事实条 | 超窗旧轮压缩成摘要，关键数字/条款号抽成结构化事实条保留 | 长对话不丢关键信息、token 可控 | 较高 | 待选型 |
| D. 结构化对话状态（槽位） | 维护当前话题槽位（合同类型/金额档位/审批人…），追问先填槽再检索 | 适合结构化重复问答 | 中 | 待选型 |
| E. 跨会话长期记忆 | 结构化画像 / 向量记忆库 / 知识沉淀 | 跨会话个性化 | 中-高 | 待选型 |
| F. 通用工程做法 | 记忆即检索、自动压缩（reflexion）、Graph 记忆 | 通用但偏重 | 高 | 参考 |

### 8.4 推荐

用量上来后优先 **B 轻量版**（让证据窗口按 `ref_rounds` 聚焦，而非无差别合并最近 N 轮）；
C/D 视「长对话」与「结构化重复问答」的实际占比再评估；E/F 等有跨会话个性化诉求时再做。

---

## 九、方案 A：证据窗口（已实现）

- **配置**：`config/models.yaml` 的 `memory.evidence_window_rounds`（默认 3）。
- **实现**：`app/services/memory.py` 的 `merge_chunk_lists()` 与 `_recent_evidence_windows()`；
  reuse 分支由「复用最近一条 assistant 的 `context_chunks`」改为「合并最近 N 轮并去重」。
- **去重键**：`(knowledge_base, filename, chunk_index)`；**顺序**：最新一轮在前。
- **效果**：reuse 类追问（如「对比刚才两条」「综合上面几轮」）可拿到多轮证据原文，
  引用由 `citations_from_stored_chunks()` 从合并后的块重建，编号与上下文 `[i]` 对齐。

---

# 会话持久化与规模化演进

> 原文件：会话持久化与规模化演进.md

> 记录时间：2026-09-11
> 状态：本文档为**规划**，除「已落地」一节外均未实现，供 Agent 规模变大时按阈值逐级演进。

## 一、已落地（本次迭代）

会话持久化当前实现：

- 两张表：`conversations`（会话元数据 + `status/archived_at` 生命周期字段）、
  `conversation_messages`（完整消息文本 + 引用 JSONB + 门禁判定）；
- **消息游标分页**：`GET /conversations/{id}?limit=&before_id=`，返回 `total / has_more / messages`，
  前端「加载更早」按 `before_id` 键值游标向前翻页（对实时追加的新消息免疫，避免 OFFSET 漂移）；
- **归档**：软归档 `status=archived`（可恢复），`POST .../archive` / `.../unarchive`，
  列表可按 `active / archived / all` 过滤；
- **保留策略**：`CONVERSATION_ARCHIVE_DAYS`（活跃会话超期自动归档）、
  `CONVERSATION_RETENTION_DAYS`（归档会话超期永久删除，连同消息），
  触发入口为启动钩子 / `POST /conversations/retention/run` / `python -m app.cli retention`。

上述即为「分页 + 归档/保留」的第一阶段，足以支撑当前体量。下面按规模阈值给出后续演进方向。

## 二、规模增长的瓶颈预估

按「每轮问答 = 用户消息 + 助手消息」两行、每行约 0.5~5 KB 估算，单租户累积：

| 消息总量 | 估算体积 | 主要瓶颈 |
|---|---|---|
| < 10 万条 | < 0.5 GB | 无，现状即可 |
| 10 万 ~ 100 万 | 0.5~5 GB | 单表扫描变慢、冷数据占库、引用摘要重复膨胀 |
| 100 万 ~ 1000 万 | 5~50 GB | 全表检索/聚合变慢，需要冷热分层与分区 |
| > 1000 万 / 多租户 | > 50 GB | 需要分库分表、独立检索、异步落库 |

关键点：**消息文本本身很小，真正的压力不在磁盘容量，而在「查询性能、重复数据、冷数据长期占用热库、以及全量检索能力」**。

## 三、分级演进方案

### L1（10 万~100 万）：索引优化 + 引用去重 + 时间分区

1. **时间分区表**：`conversation_messages` 按 `created_at` 做 PostgreSQL RANGE 分区（按月/季度），
   老分区可整体 DETACH 归档或 DROP，删除不再逐行扫；`conversations` 亦可用小粒度分区。
2. **引用去重**：把每条回答里的 `citations` 从「完整 JSONB 摘要」改为
   `chunk_id 外键 + 页码/块序号`，摘要**按需实时生成**（或缓存一份在 chunk 侧），
   避免同一文档块被多轮引用时摘要被重复存储（当前是唯一会线性膨胀的重复数据）。
   > ✅ 已实现（2026-09-21）：落库只存 `chunk_id + filename/page_number/chunk_index/
   > knowledge_base`，读取消息时按 `chunk_id` 批量重建块首摘要，见开发记录第八节。
3. **索引**：`conversation_messages(conversation_id, id)` 复合索引（当前已有单列）、
   `conversations(updated_at)`/`created_at` 可改 BRIN（时间单调，体积小、扫描快）。
4. **审计表同样治理**：`audit_logs` 也是只增表，纳入同一套保留策略与分区。

### L2（100 万~1000 万）：冷热分层 + 外置存储 + 全文检索

1. **消息体外置**：`content` 大文本从行内移到**对象存储**（MinIO / S3），
   数据库只存 `message_key + 元数据 + 引用`；热数据保留在 DB，冷数据按需回读并做本地缓存。
2. **冷归档落盘压缩**：过期会话导出为 **Parquet + 压缩**（或 gzip JSONL）存对象存储，
   DB 只留「会话索引 + 位置指针」，需要时按会话回放，长期保存成本降到 1/10 以下。
3. **历史会话检索**：为消息加全文索引（Postgres `tsvector` + jieba 分词，或 `pg_trgm` 做模糊；
   规模再上走 **OpenSearch/Elasticsearch**），支持「搜到过哪句话 / 什么时候问过」。
4. **读写分离 / 缓存**：读多写少，列表与详情读走只读副本；热点会话列表结果进 Redis。

### L3（> 1000 万 / 多租户）：分库分表 + 事件化

1. **多租户隔离**：按 `user_id`（或租户）水平分片（sharding），单租户数据物理隔离、可独立备份/清理。
2. **异步落库**：问答完成后消息写入走**消息队列**（Kafka / NATS），由消费者批量落库，
   削峰并解耦「回答返回」与「持久化」的延迟；失败可重试，保证不丢消息。
3. **CDC 事件流**：消息落库后产出变更事件，供审计、数据分析、记忆模块等下游订阅。
4. **独立检索/归档服务**：检索与冷归档从主问答链路剥离为独立服务，避免互相拖累。

## 四、与「Agent 记忆」的关系（下一步的自然延伸）

会话持久化只解决「记录不丢」。当 Agent 需要**多轮追问、长期记忆、跨会话回忆**时：

- **短期记忆**：最近 N 轮作为上下文回传，需要 query 改写 / 指代消解 + 上下文截断；
- **长期记忆**：对历史消息做**摘要 + Embedding**，单独存一张记忆向量表（可复用现有 pgvector 管线），
  追问时先检索相关记忆再拼上下文；
- **记忆也要生命周期**：记忆条目同样需要「重要度评分 + 保留策略 + 过期清理」，
  与本文档的归档/保留策略保持同一套治理框架。

> 注意：记忆向量表会引入**新的向量存储成本**（每条记忆 1024 维），届时它的体积将显著大于消息文本，
> 需要独立评估保留策略（例如只保留摘要、不保留原始消息向量）。

## 五、落地节奏建议

| 阶段 | 触发条件（经验值） | 优先动作 |
|---|---|---|
| 现在 | 单租户 < 10 万条 | 保持现状，观察 `conversation_messages` 增长曲线 |
| 近期 | 消息 > 10 万条，或详情页变慢 | 引用去重 + `(conversation_id, id)` 复合索引 |
| 中期 | 消息 > 100 万条，或冷数据占库过半 | 时间分区 + 冷归档（Parquet/对象存储）+ 消息体外置 |
| 远期 | 多租户 / > 1000 万条 | 分库分表 + 异步落库 + 独立检索服务 |

## 六、技术选型参考

| 能力 | 候选方案 | 备注 |
|---|---|---|
| 分区 | PostgreSQL 原生 RANGE/LIST 分区 | 无需新组件，优先 |
| 对象存储 | MinIO（私有化）/ S3 | 冷归档、消息体外置 |
| 冷数据格式 | Parquet + gzip/zstd | 体积小、可按列分析 |
| 全文检索 | PG `tsvector` + jieba → OpenSearch | 先 PG 后迁，避免过早引入 |
| 异步队列 | NATS / Kafka | 仅 L3 需要 |
| 缓存 | Redis | 热点列表/会话元数据 |
| 调度 | 系统 cron / Airflow | 保留策略、冷归档任务定时跑 |

> 原则：**先索引与去重，再分区与冷分层，最后才考虑分库分表与消息队列**——
> 每上升一级都引入新的运维复杂度，必须有对应规模数据支撑。

---

# 待解决问题与解决方案

> 原文件：待解决问题与解决方案.md

> 记录时间：2026-09-10
> 状态：均为「待办」，本文档只做问题梳理与方案设计，不含代码改动。

---

## 问题一：问答记录未持久化（切换页面后记录消失）

> 状态更新（2026-09-13）：会话持久化（09-11）与**会话内短期记忆 / 多轮追问（方案 D）**均已实现，
> 记忆策略、方案选型与「何时复用引用」判定规则见 `记忆策略与方案`。
> 跨会话长期记忆与知识沉淀尚未实施，见该文档第七节的分阶段路线。

### 现状与根因

- 前端 `ChatPage` 用 React `useState` 保存消息，路由切换导致组件卸载，消息随之丢失；
- 后端无「会话 / 消息」表，也没有会话查询接口；
- 本质是缺少**会话持久化**，而不是单纯的"没存到本地"。

### 关键澄清：用户所说的"记忆模块"其实是两层，应分开规划

| 层 | 含义 | 解决的问题 |
|---|---|---|
| 会话持久化（Conversation Persistence） | 把会话与消息存到数据库，可回看、可续接、可审计 | 记录消失 |
| 对话记忆（Multi-turn Memory） | 把最近 N 轮问答作为上下文回传给模型，支持追问 | 多轮追问、指代消解 |

两者不同，也都有价值；当前痛点只属于第一层。

### 方案（会话持久化）

1. **数据模型**（后端新增两张表）：
   - `conversations`：`id / user_id / title(首问截断) / knowledge_base(可空) / created_at / updated_at`
   - `conversation_messages`：`id / conversation_id / role(user|assistant) / content / citations(JSONB) / decision / created_at`
2. **API**：
   - `GET  /conversations`（当前用户会话列表）
   - `POST /conversations`（新建会话）
   - `GET  /conversations/{id}/messages`（某会话消息）
   - `POST /conversations/{id}/chat`（会话内问答，服务端落库；可复用现有 `/chat` 与 `/chat/stream` 逻辑）
   - `DELETE /conversations/{id}`
3. **前端**：侧边栏新增「历史会话」列表 + 「新建会话」；进入会话时拉取历史消息；路由切换不再丢失。
4. **权限与审计**：会话归属 `user_id`，普通用户只见自己的会话，管理员可查全部；问答落库与审计日志联动。

### 方案（多轮记忆，第二阶段、可选）

- 追问时把最近 N 轮作为上下文回传；需要额外处理：
  1. **是否需要重新检索**：追问往往是对上文的指代（"那 100 万以下的呢？"），需要 query 改写或判断是否沿用上一轮检索结果；
  2. **引用归属**：多轮回答的引用要能追溯到具体哪一轮的检索块；
  3. **上下文窗口控制**：历史轮次截断策略，避免 token 超限。
- 建议：先做「会话持久化」（直接解决痛点、改动可控），「多轮记忆」单独立项。

---

## 问题二：提问前必须先选知识库，员工不知道选哪个

> 状态更新（2026-09-11）：已实现「全库聚合检索 + 可选手动指定」——
> 后端 `knowledge_base` 改为可选（空 / "auto" 自动路由到当前用户全部可读库），
> 引用与上下文中标注来源知识库；前端知识库选择器默认「自动（推荐）」。

### 现状与根因

- `/chat` 请求体的 `knowledge_base` 必填，前端下拉必选；
- 根因：知识库是**技术实现细节**，不该暴露给普通用户做"路由决策"——员工只关心问什么，不关心答案存在哪个库。

### 方案（推荐：自动路由 + 可选手动指定）

1. **后端**：`/chat` 的 `knowledge_base` 改为可选；为空时自动路由：
   - 候选 = 当前用户**有读权限**的全部知识库；
   - 两阶段路由：
     - 阶段一（轻量选库）：对每个候选库各做一次快速检索（dense top-k 或 BM25 打分），按最高信号取 1~N 个库；
     - 阶段二（精检）：在选中的库里跑完整检索（dense + BM25 + RRF + rerank），跨库融合 top-k；
   - 引用结果中携带 `knowledge_base` 字段，回答可标注"来自《XX制度》"，便于溯源。
2. **更简单的一版（库数量少时首选）**：直接"**全库聚合检索**"——把所有可读库的 chunk 合并后做跨库 top-k，无需选库，也无需路由分类器。
3. **前端**：输入框不再要求选库，默认「自动」；折叠一个「高级」入口供少数需要精确指定库的人使用。
4. **边界**：
   - 自动路由只检索有读权限的库，权限逻辑不变；
   - 「无依据」判定基于聚合后的跨库结果统一做；
   - 多库命中同一主题时，按 rerank 分排序，必要时标注来源库差异。

### 建议

先做「全库聚合检索 + 可选手动指定」（后端把 `knowledge_base` 设为可空，空 = 全部可读库聚合），最贴合"员工只提问"的诉求；等知识库规模变大、聚合检索变慢时，再上「轻量路由/分类器」。

---

## 问题三：文档类型识别 + 图片/图表/表格内容解析

> 状态更新（2026-09-15）：**第一层「格式扩展」与第二层「表格结构化」已实现**——
> 新增 `.xlsx`（openpyxl，整 sheet 转 Markdown 表格）、`.pptx`（python-pptx，含表格）、
> `.html` / `.epub`（标准库正文 + 表格提取）；DOCX 解析重写为按文档顺序识别段落与表格
> （表格转 Markdown，不再被拍平）；**PDF 改用 PyMuPDF `find_tables()` 检测内嵌表格转
> Markdown，并以词坐标列对齐自研检测无边框表格（≥3 列，剔除表格内重复文字）**；
> **合并单元格**：DOCX `gridSpan`/`vMerge` 与 XLSX `merged_cells` 已展开填充；
> `document_chunks` 新增 `content_type`（text / table），表格块按完整行切分保留行列结构；
> 上传白名单与前端文件选择器同步扩展。
> 扫描 PDF OCR 已实现为**可选能力**（PyMuPDF 渲染 + 延迟导入 `rapidocr-onnxruntime`，
> 安装后自动识别扫描页；未安装时给出明确提示）；混合 PDF 的部分扫描页会经 warnings
> 返回并在 CLI/前端提示，不再静默跳过。图片/图表结构化（第三层）与视觉向量检索仍待后续。
>
> 状态更新（2026-09-17）：**入库工程化 P0 已落地**——超长表格切块后每块重复表头（#9）、
> magic bytes 内容嗅探 + 扩展名交叉校验防伪造后缀（#12）、解析结构化指标进审计 `extra`
> （#15）、`content_type` 迁移真实库端到端验证通过（#20）、新增 `python -m app.cli reingest`
> （#21）与 `python -m app.cli rebuild`（#22）两条重建命令。详见
> `文档入库工程化技术指标`。
>
> 状态更新（2026-09-17，P1）：编码探测（UTF-8 → charset-normalizer → GB18030，#24）、
> HTML/PPTX 合并单元格展平（#2）、表格引用摘要「表头 + 命中行」（#11）、上传失败审计与
> `ProviderError` 封装（#14）、表格语义增强「列名：值」（#25）、单文档解析内存保护
> `MAX_PARSE_BYTES` + zip 膨胀防护 + xlsx 大文件流式（#13/#26）均已落地。
>
> 状态更新（2026-09-19，P1 第三批）：**问题三的 P1 正确性深水区已全部解决**——
> 2 列无边框表格检测（#1，编号/项目符号负向过滤 + 长单元格长度过滤）、PDF 合并单元格
> 还原（#3，`None` 延续位回填）、PDF 跨页表格续接补表头（#4）、多栏 PDF 阅读顺序（#5，
> x 空隙分栏 + 通栏块插入）、各格式细节（#7：PPTX 备注/图表/SmartArt、XLSX 公式回退 +
> sheet 内多表、DOCX 页眉页脚/文本框、HTML/EPUB 图片 alt）。配套冒烟脚本
> `scripts/smoke_ingestion_p1.py`。图片/图表结构化（#6）与 OCR 落地（#8）仍属 P3。

> 状态更新（2026-09-19，P2 底座）：**可测试 / 可部署 / 数据治理底座基本落地**——
> #16 OCR 三态 debug 日志、#17 `tests/` + pytest（50 例）、#18 CI 确定性门禁（compileall
> + pytest + examples 回归，已 GitHub 实测通过）、#23 老格式入库筛除（放弃 LibreOffice
> 转换）、#27 docx/xlsx 解析降级链、#29 增量同步 `sync`（mtime/size 变更检测）、#30
> 表格块质量基线样本/评测集、#28 重排模型下载脚本与部署说明。其中 **#19（依赖兼容矩阵）、
> #28（镜像构建）、#29/#30（DB 端到端与基线数值）仍需外部环境实测闭环**：
> #19 已定位 `rapidocr-onnxruntime>=1.4.0`（numpy<3.0）与 numpy 2.2.6 兼容，安装实测待网络。

### 现状与根因

- 已支持：`.md / .txt / .docx / 文字型 PDF`；
- 不支持：`.xlsx / .pptx / .html / .epub / 扫描 PDF`；
- PDF/DOCX 内的**图片、图表、表格**未结构化处理：图片被跳过、表格被按行拍平、图表完全丢失。

### 方案（分层：文本优先 → 表格结构化 → 多模态补充）

#### 第一层：解析格式扩展（文本优先）

| 格式 | 解析方式 | 说明 |
|---|---|---|
| `.xlsx` | openpyxl 逐 sheet / 逐行读取 | 转 Markdown 表格文本 |
| `.pptx` | python-pptx 逐页提取标题/正文/备注 | 保留页序 |
| `.html` / `.epub` | 正文抽取（BeautifulSoup / 专用解析） | 去导航、留正文 |
| PDF | 引入 pymupdf 替代/补充 pypdf | 更好还原版面顺序、识别表格 |

#### 第二层：表格结构化

- DOCX / XLSX / PDF 中的表格统一转成 **Markdown 表格文本**（保留行列结构），
  避免把表内容拆乱或丢失字段对应关系；
- 表格作为一个整体块或"表头+行"切块，嵌入语义信息。

#### 第三层：图片 / 图表（多模态补充，两条技术路线）

1. **路线 A（轻量，推荐先做）：转文本入库**
   - 图片/图表 → OCR（PaddleOCR / 云 OCR）+ 版面分析（PP-Structure / MinerU）+ VLM（如 Qwen-VL）生成**文字描述**；
   - 描述文本切块后走现有向量管线；
   - 引用保留图片位置（页码 / 图号 / 坐标），回答时可跳转原图。
2. **路线 B（进阶，按需再做）：视觉向量多路检索**
   - 用 CLIP 等视觉 Embedding 单独建一路，与文本检索做 RRF 融合；
   - 适合"搜图 / 找图表"类场景。

#### 第四层：扫描 PDF

- OCR + 版面分析先行，把扫描页转成带结构文本，再走现有入库流程；
- 原始扫描图保留，供引用回看。

#### 关键设计点

- **块类型扩展**：`DocumentChunk` 增加 `content_type`（text / table / image），
  image 块存"描述文本 + 原图引用（文件路径/页码/坐标）"；
- **引用可追溯**：图片/图表作为独立块，回答能指回"第 X 页 图 Y"；
- **去重与版本**：图片 hash 去重、文档更新后重建索引、失败重试（与既有待办合并）。

### 建议优先级

1. 先「文本类格式扩展（xlsx/pptx）+ 表格结构化」——收益大、无模型依赖；
2. 再「PDF 图片/扫描 OCR + 图表描述」——依赖 OCR/VLM，需要模型与算力；
3. 视觉向量检索最后按需引入。

---

## 三问题依赖关系与大致节奏

| 问题 | 依赖 | 建议节奏 |
|---|---|---|
| 一、会话持久化 | 无（纯后端 + 前端） | 优先，直接解决高频痛点 |
| 二、自动路由 | 无（后端检索改造 + 前端简化） | 优先，提升核心体验 |
| 三、文档解析 | OCR/VLM 等外部能力 | 分阶段：先表格/格式，后图片/OCR |

> 备注：问题一与问题二相互独立，可并行；问题三周期最长，建议拆成多个小迭代。

---

## 问题四：会话保留策略的定时任务（遗留，待部署 Linux 后处理）

### 现状

- 会话保留策略的处理逻辑已实现（`app/services/conversation.py` 的 `apply_retention()`），
  依据 `CONVERSATION_ARCHIVE_DAYS`（超期活跃会话自动归档）与
  `CONVERSATION_RETENTION_DAYS`（超期归档会话永久删除）自动处理会话状态；
- 触发入口现有三个，但均为**被动触发**：后端启动时执行一次、管理接口
  `POST /conversations/retention/run`、命令行 `python -m app.cli retention`；
- 尚未挂**周期性定时任务**（Windows 下已备好入口脚本 `scripts/run-retention.ps1`）。

### 待办（部署 Linux 后）

1. 在服务器 crontab 挂定时任务，例如每天凌晨执行：
   `cd /opt/EnterpriseKB && .venv/bin/python -m app.cli retention >> /var/log/entkb-retention.log 2>&1`
   （路径按部署位置调整）；
2. 或（更优）在应用内引入常驻调度器（如 APScheduler）周期调用 `apply_retention()`，
   使部署不依赖宿主机 cron、跨平台一致；
3. 定时任务落地后，把本次「遗留」标记为已解决，并补一条审计/监控确认策略按预期执行。

---

# 方案一：Linux 端到端部署验证

> 原文件：方案一-Linux端到端部署验证.md

> 目标：在线上 Linux 服务器把「后端 + 前端 + pgvector + BGE Rerank（宿主机卷挂载）」
> 完整跑起来，并验证 Rerank 确实生效（未降级）。适用 `docker compose` v2。

## 0. 前置条件

```bash
docker --version          # >= 24
docker compose version    # 需为 v2（compose 插件）
free -h                   # 内存建议 >= 8G（Rerank 模型约 2.3GB，CPU 推理更吃内存）
df -h                     # 磁盘预留 >= 20G（模型 + 两个镜像 + 数据库数据卷）
```

> **关于 GPU**：Dockerfile 默认装 PyPI 的 CPU 版 torch。**没有 GPU 也能跑通**，
> 只是 Rerank 用 CPU 推理、首次加载模型较慢、单次提问可能延迟几秒~几十秒，属正常。
> 若要 GPU 加速，需额外装 CUDA 版 torch 并在 compose 加 `gpus` 配置（见文末「可选」）。

> **一键验证脚本**：仓库提供 `scripts/verify-linux-deploy.sh`，可自动完成下方第 2~7 节
> （下载模型 → 生成 override → 构建启动 → 初始化 → 端到端验证）并输出 PASS/FAIL 汇总。
> 完整验证：`bash scripts/verify-linux-deploy.sh`；低配关 Rerank：`RERANK=0 bash scripts/verify-linux-deploy.sh`。

## 1. 放置代码（两个仓库必须为兄弟目录，目录名固定）

`docker-compose.deploy.yml` 的 frontend 构建上下文是 `../EnterpriseKBWeb`，
因此两个目录必须同名、同父目录：

```bash
mkdir -p /opt/kb && cd /opt/kb

# 方式 A：git clone（推荐）
git clone https://github.com/CpxLincen/EnterpriseKB_API.git EnterpriseKB
git clone <前端仓库地址> EnterpriseKBWeb

# 方式 B：scp 上传（无 git 时），保证解压后目录名为 EnterpriseKB / EnterpriseKBWeb
```

预期结构：

```text
/opt/kb/
├── EnterpriseKB/          # 后端（含 docker-compose.deploy.yml、scripts/、config/）
└── EnterpriseKBWeb/       # 前端（含 Dockerfile、nginx.conf、src/）
```

## 2. 下载 BGE Rerank 模型（约 2.3GB）

```bash
cd /opt/kb/EnterpriseKB

# 安装下载工具（二选一；国内推荐 ModelScope）
pip3 install modelscope
# 或：pip3 install -U huggingface_hub

# 下载到宿主机固定目录（推荐路径，与 override 默认一致）
./scripts/download-rerank-model.sh /opt/kb/models/bge-reranker-v2-m3
# 走 HuggingFace（自动用 hf-mirror 镜像）：
# ./scripts/download-rerank-model.sh /opt/kb/models/bge-reranker-v2-m3 huggingface
```

校验模型完整性（应能看到 `config.json`、`pytorch_model.bin` 或 `model.safetensors`、
tokenizer 相关文件）：

```bash
ls -lh /opt/kb/models/bge-reranker-v2-m3
du -sh /opt/kb/models/bge-reranker-v2-m3    # 约 2.3G
test -f /opt/kb/models/bge-reranker-v2-m3/config.json && echo "模型 OK" || echo "模型缺失"
```

## 3. 配置环境变量

```bash
cd /opt/kb/EnterpriseKB
cp deploy.env.example .env
vim .env
```

关键项：

| 变量 | 值 | 说明 |
|---|---|---|
| `QWEN_API_KEY` | `sk-你的密钥` | **必填**，Embedding + 默认聊天模型 |
| `AUTH_JWT_SECRET` | 随机长串 | **生产必填**；可用 `openssl rand -hex 32` 生成 |
| `AUTH_DEV_MODE` | `false` | 保持 false |
| `POSTGRES_PASSWORD` | `enterprise_kb` | 可改（改了无需其它改动，compose 内一致引用） |
| `WEB_PORT` | `8080` | 前端对外端口 |
| 其余 | 留空 | — |

生成随机密钥：

```bash
openssl rand -hex 32
```

## 4. 生成 Rerank 卷挂载 override（方案一）

```bash
cd /opt/kb/EnterpriseKB
cp docker-compose.deploy.rerank.example.yml docker-compose.deploy.rerank.yml
vim docker-compose.deploy.rerank.yml
```

确认内容（宿主机路径 = 第 2 步实际下载位置）：

```yaml
services:
  backend:
    environment:
      RERANK_MODEL_PATH: /models/bge-reranker-v2-m3   # 容器内路径，不要改成宿主机路径
    volumes:
      - /opt/kb/models/bge-reranker-v2-m3:/models/bge-reranker-v2-m3:ro
```

## 5. 构建并启动（首次构建约数分钟）

```bash
cd /opt/kb/EnterpriseKB
docker compose -f docker-compose.deploy.yml -f docker-compose.deploy.rerank.yml up -d --build
docker compose -f docker-compose.deploy.yml -f docker-compose.deploy.rerank.yml ps
```

预期三个服务均 `running` / `healthy`：`db`（healthy）、`backend`、`frontend`。

## 6. 初始化管理员、导入示例文档、授权

```bash
cd /opt/kb/EnterpriseKB
C="docker compose -f docker-compose.deploy.yml -f docker-compose.deploy.rerank.yml"

$C exec backend python -m app.cli create-user admin --role admin
# 后端镜像只含 app/ 与 config/，examples/ 需先复制进容器
$C cp examples/employee-handbook.md backend:/tmp/employee-handbook.md
$C exec backend python -m app.cli ingest /tmp/employee-handbook.md --knowledge-base hr
$C exec backend python -m app.cli grant admin --knowledge-base hr --write
```

> 顺序说明：`ingest` 会自动创建知识库 `hr`；`grant` 要求知识库已存在，
> 因此放在 `ingest` 之后执行。

## 7. 端到端验证

### 7.1 健康/就绪探针

```bash
curl http://127.0.0.1:8080/health    # 期望 {"status":"ok"}
curl http://127.0.0.1:8080/ready     # 期望 {"status":"ready"}
```

### 7.2 提问（CLI，不经过 HTTP 认证，直接触发检索 + Rerank）

```bash
$C exec backend python -m app.cli ask '年假如何计算？' --knowledge-base hr
```

期望：返回带 `[1]` 引用标记的回答 + 引用列表（文件名、页码、块序号）。

### 7.3 判定 Rerank 是否生效（关键）

```bash
$C logs backend | grep -i rerank
```

- **生效**：日志里**没有** `Rerank 不可用，本次及后续将降级为混合检索` 这类 warning。
- **降级（问题）**：出现了该 warning，说明模型没挂上，按第 8 节排查。

辅助确认模型在容器内可见、配置生效：

```bash
$C exec backend ls /models/bge-reranker-v2-m3          # 应看到 config.json 等
$C exec backend python -c "from app.core.config import retrieval_config as r; print(r().rerank_model, r().rerank_enabled)"
# 期望输出：/models/bge-reranker-v2-m3 True
```

> 首次提问会加载 2.3GB 模型到内存：backend 内存明显上升、首次延迟较长属正常。

## 8. 常见问题排查

| 现象 | 排查 |
|---|---|
| `Rerank 不可用...降级` warning | ① override 是否与 `-f` 一起传；② 宿主机路径是否真实存在模型；③ `RERANK_MODEL_PATH` 是否指向容器内路径；④ `exec backend ls /models/...` 是否有文件 |
| `Permission denied` 读模型 | 模型目录权限给读：`chmod -R a+r /opt/kb/models/bge-reranker-v2-m3` |
| backend 起不来 / 连不上 db | `$C logs backend` 看 DATABASE_URL；确认 db healthy 后再看 backend |
| `/ready` 失败 | db 容器未 healthy，等 `$C ps` 显示 healthy |
| 端口 8080 被占 | 改 `.env` 的 `WEB_PORT` |
| 提问返回「未找到依据」 | 先确认第 6 步 ingest 成功（输出 chunks 数 > 0） |

## 9. 可选项：GPU 加速 Rerank

1. 后端镜像改装 CUDA 版 torch（`requirements-rerank.txt` 加 `--index-url https://download.pytorch.org/whl/cu124`）；
2. compose 的 backend 加：
   ```yaml
   deploy:
     resources:
       reservations:
         devices: [{ driver: nvidia, count: 1, capabilities: [gpu] }]
   ```
3. 宿主机装 nvidia-container-toolkit。

> 无 GPU 也能完整验证「方案一」本身；GPU 只是性能问题，不阻塞验证。

## 10. 低配服务器（2G 内存 / 50G SSD）适配

**结论**：2G 内存**跑不动本地 BGE Rerank**（模型 2.3GB，CPU 加载即需 ~2.5GB+ 内存，
必然 OOM）；50G SSD 磁盘足够。建议**关闭本地 Rerank，退回混合检索（dense+BM25+RRF）**。

### 10.1 低配部署步骤（替代上文第 2 / 4 / 5 步）

`.env` 关键设置：

```bash
INSTALL_RERANK=false     # 镜像内不装 torch（省 ~1.5GB+ 镜像体积与构建时间）
RERANK_ENABLED=false     # 运行时跳过 Rerank 分支，退回混合检索
# RERANK_MODEL_PATH 留空即可（无需下载 2.3GB 模型）
```

启动（**不需要** `-f docker-compose.deploy.rerank.yml`，也不需要下载模型）：

```bash
cd /opt/kb/EnterpriseKB
docker compose -f docker-compose.deploy.yml up -d --build
docker compose -f docker-compose.deploy.yml ps
```

用户初始化 / 文档导入 / 提问验证与第 6 / 7 节相同（compose 文件名不带 rerank override）。

### 10.2 内存与磁盘预期

- 关 Rerank 后三服务合计约 1~1.5GB 内存：PostgreSQL + FastAPI（单 worker）+ Nginx；
- **强烈建议给宿主机加 swap**（4G）：
  ```bash
  fallocate -l 4G /swapfile && chmod 600 /swapfile && mkswap /swapfile && swapon /swapfile
  echo '/swapfile none swap sw 0 0' >> /etc/fstab
  ```
- 若 db 启动报内存不足，可在 compose 给 db 加：
  ```yaml
  command: postgres -c shared_buffers=64MB -c max_connections=20 -c work_mem=2MB
  ```

### 10.3 取舍

- 少一层 BGE 精排，检索精度略降；防幻觉门禁退化为「稠密距离单阈值」；
- 2G 内存只适合**小规模 / 验证**；正式生产建议内存 ≥8G，或把 Rerank 换成云端 API（后续可选改造）。

---

# 遗留问题与验证清单

> 原文件：遗留问题与验证清单.md

> 生成日期：2026-09-23
> 用途：汇总当前所有未闭环问题，每项附「验证方法」或「解决方案」，供线上验证与后续开发逐项勾选。
> 关联文档：`待解决问题与解决方案`、`文档入库工程化技术指标`、
> `记忆策略与方案`、`会话持久化与规模化演进`、`方案一-Linux端到端部署验证`

> **验证进度（2026-09-23 更新）**：
> 已闭环 `[x]`：第 1（Rerank 生效）、2（前端探针）、3（#29 增量同步）、4（#30 表格基线）、
> 5（#19 OCR 安装实测 + 精确版本回填）、6（模型缺失优雅降级）、7（保留策略删除路径，
> 周期调度待生产切换后挂 cron）、10（#28 镜像构建）。
> 暂缓 `⏸️`：第 8（SSO，待真实 IdP）、9（CI 评测回归，待 GitHub secret `QWEN_API_KEY`）。
> 其余功能/量化项（第 11–24）仍待办。

## 使用说明

- 勾选约定：`[ ]` 未完成　`[x]` 已完成
- 服务器环境：CentOS；代码 `/opt/kb/EnterpriseKB` + `/opt/kb/EnterpriseKBWeb`；
  BGE 模型 `/opt/kb/models/bge-reranker-v2-m3`；对外 `WEB_PORT=80`
- 下文命令中的 `$C` 先定义一次：
  `C="docker compose -f docker-compose.deploy.yml -f docker-compose.deploy.rerank.yml"`
  （关 Rerank 的场景则去掉第二个 `-f` 文件）
- 旧镜像未重建时，容器内 CLI 建议加 `env PYTHONUTF8=1` 前缀；重建镜像后可不加

---

## 一、待线上服务器验证（按优先级）

### 1. Rerank 是否真正生效（方案一核心）

- **问题**：BGE Rerank 模型经宿主机卷挂载后，是否在问答中真正生效、未降级为混合检索。
- **验证命令**：
  ```bash
  $C exec backend env PYTHONUTF8=1 python -m app.cli ask '年假如何计算？' --knowledge-base hr
  $C logs backend | grep -i 'Rerank 不可用'
  # 辅助确认配置与模型可见性
  $C exec backend python -c "from app.core.config import retrieval_config as r; print(r().rerank_model, r().rerank_enabled)"
  $C exec backend ls /models/bge-reranker-v2-m3
  ```
- **通过标准**：`ask` 返回带 `[1]` 引用的回答；`grep 'Rerank 不可用'` **无输出**；
  配置打印 `/models/bge-reranker-v2-m3 True`；容器内能看到模型文件。
- 状态：`[x]`（2026-09-23 线上已验证闭环）

### 2. 前端探针反代

- **问题**：`nginx.conf` 曾漏配 `/health` `/ready`，探针落入 SPA fallback 返回 index.html（已改代码，待镜像生效）。
- **验证命令**：
  ```bash
  $C up -d --build frontend
  curl http://127.0.0.1:80/health
  curl http://127.0.0.1:80/ready
  ```
- **通过标准**：分别返回 `{"status":"ok"}`、`{"status":"ready"}`。
- 状态：`[x]`（2026-09-23 线上已验证闭环）

### 3. #29 增量同步 sync 的 DB 端到端

- **问题**：`sync` 按 mtime/size 变更检测已实现并有单测，但未在真实库端到端验证。
- **验证命令**：
  ```bash
  # 注意：sync 的源目录需在容器内可访问，可挂载宿主机目录，或把文件 cp 进容器目录
  $C cp examples/employee-handbook.md backend:/tmp/src/employee-handbook.md
  $C exec backend env PYTHONUTF8=1 python -m app.cli sync /tmp/src -k hr
  # 修改文件 mtime/size 后再次 sync，观察 processed 数量是否只含变化文件；追加 --delete-missing 验证删除
  ```
- **通过标准**：首次全量导入；仅改动文件被重导；`--delete-missing` 可删失效文档。
- 状态：`[x]`（2026-09-23 线上已验证闭环）

### 4. #30 表格块质量基线采集

- **问题**：`table-eval.yaml` 与表格样本已建，但 hit@k / MRR 基线数值未在 DB 上采集。
- **验证命令**：
  ```bash
  $C cp examples/审批权限表.xlsx backend:/tmp/审批权限表.xlsx
  $C exec backend env PYTHONUTF8=1 python -m app.cli ingest /tmp/审批权限表.xlsx -k hr
  $C cp -r eval backend:/tmp/eval
  $C exec backend env PYTHONUTF8=1 python -m app.eval /tmp/eval/table-eval.yaml --json /tmp/out.json --markdown /tmp/out.md
  ```
- **通过标准**：产出 hit@k / MRR / 拒答正确率，记录为本版本基线，后续回归比对。
- 状态：`[x]`（2026-09-23 已采集 1.0 基线）

### 5. #19 OCR 依赖兼容安装实测

- **问题**：`rapidocr-onnxruntime>=1.4.0`（numpy<3.0）与 numpy 2.2.6 兼容性仅在元数据层面确认，未安装实测。
- **验证命令**：
  ```bash
  $C exec backend pip install "rapidocr-onnxruntime>=1.4.0,<2.0"
  $C exec backend python -c "import rapidocr_onnxruntime; print('import ok')"
  # 渲染一张含中文页 → 跑 _ocr_page 冒烟（见 tests/test_ocr.py）
  ```
- **通过标准**：安装 + import 成功 + OCR 正确识别中文；随后把 onnxruntime/opencv-python/shapely/pyclipper/rapidocr-onnxruntime 精确版本补进 `requirements-lock.txt`。
- 状态：`[x]`（2026-09-23 已闭环；opencv 用 headless 替代，版本已回填 lock）

### 6. 模型缺失时的优雅降级链路

- **问题**：rerank 缺失/加载失败时，是否真正降级为混合检索而不 500（09-21 遗留，仅纯函数层验证过）。
- **验证命令**：
  ```bash
  # 方式：临时关掉 Rerank 再重建 backend
  sed -i 's|^RERANK_ENABLED=.*|RERANK_ENABLED=false|' .env
  $C up -d --force-recreate backend
  $C exec backend env PYTHONUTF8=1 python -m app.cli ask '年假如何计算？' -k hr
  ```
- **通过标准**：问答正常返回（混合检索），HTTP/CLI 不 500；日志出现降级 warning 属预期。
- 状态：`[x]`（2026-09-23 线上已验证闭环）

### 7. 会话保留策略定时任务

- **问题**：`apply_retention()` 已实现，但只有被动触发（启动/接口/CLI），无周期调度。
- **方案**（二选一）：
  - 方案 A：crontab 每天凌晨执行 `docker compose -f docker-compose.deploy.yml exec backend python -m app.cli retention`
  - 方案 B：应用内 APScheduler 常驻调度（跨平台、不依赖宿主 cron，推荐）
- **验证**：`.env` 设 `CONVERSATION_ARCHIVE_DAYS` / `CONVERSATION_RETENTION_DAYS` 后跑 `retention`，确认超期会话被归档/删除且写审计。
- 状态：`[x]`（删除路径已闭环；周期调度待生产切换后挂 cron）

### 8. SSO 真实 IdP 端到端实测

- **问题**：生产登录唯一正式方案是 OIDC，目前仍是 501 预留态、未接真实 IdP。
- **方案**：`.env` 配 `SSO_PROVIDER=oidc` + `OIDC_DISCOVERY_URL/OIDC_CLIENT_ID/OIDC_CLIENT_SECRET/OIDC_REDIRECT_URI`，对接真实 IdP（Keycloak / Azure AD / 企业微信 / 钉钉等）。
- **验证**：`GET /auth/sso/login` 跳转授权页 → 回调换 token → id_token 校验 → 用户落库；前端增加 SSO 入口。
- 状态：⏸️ 暂缓（2026-09-23：待真实 IdP 就绪后再做）

### 9. CI 评测回归接入

- **问题**：`ci.yml` 已含确定性门禁，但评测回归（hit@k/MRR 非退化门禁）需 GitHub secret 后实测。
- **方案**：仓库 Secrets 配 `QWEN_API_KEY`，`workflow_dispatch` 跑 `app.eval --modes dense hybrid`，以 hr 集 1.0 基线做非退化门禁。
- 状态：⏸️ 暂缓（2026-09-23：待 GitHub secret `QWEN_API_KEY` 就绪后再做）

### 10. #28 镜像构建冒烟（已通过，待更新文档标记）

- **说明**：2026-09-23 线上 `up -d --build` 已成功，此项可闭环；仅需把 `文档入库工程化技术指标` 中 DEP-03 标记由 ⚠️ 改为 ✅。
- 状态：`[x]`（实测通过，文档标记待同步）

---

## 二、功能开发类（含解决方案，独立立项）

### 11. #6 图片 / 图表结构化 + 视觉向量检索（P3）

- **问题**：文档内图片、图表当前完全丢失（`content_type` 仅 text/table）。
- **方案**：
  - 路线 A（推荐先做）：图片/图表 → OCR + VLM（如 Qwen-VL）生成文字描述 → 描述切块走现有向量管线，引用保留页码/图号/坐标；
  - 路线 B（进阶）：CLIP 视觉向量单独建路，与文本检索 RRF 融合（适合「搜图/找图表」）。
- 状态：`[ ]`

### 12. #8 OCR 落地 + 后处理（P3）

- **问题**：扫描 PDF OCR 壳已搭（三态诊断），但 rapidocr 未装、检测框拼接乱序、无去水印。
- **方案**：安装 rapidocr 并验证兼容 → 按坐标排序还原阅读顺序 → 去水印/页眉页脚噪声。
- 状态：`[ ]`

### 13. 跨会话长期记忆（用户画像 / 偏好）

- **方案**：轻量版 `user_profiles` 表存固定槽位（部门/角色/偏好回答风格/常用知识库），登录加载、回答 prompt 末尾附一句画像提示；成熟后再上向量记忆库。
- 状态：`[ ]`

### 14. 知识沉淀（对话 → 知识库）

- **方案**：人工标记 → 管理员审核 → 写回知识库（不自动直写，防污染）。
- 状态：`[ ]`

### 15. 多轮记忆升级

- **方案**：优先「方案 B 轻量版」——改写输出 `{type, query, ref_rounds}`，让证据窗口按 `ref_rounds` 聚焦，而非无差别合并最近 N 轮。
- 状态：`[ ]`

### 16. 会话规模化演进（用量上来后按阈值触发）

- **方案**：L1（>10 万条）时间分区 + 索引优化（引用去重已做）；L2（>100 万）消息体外置 + 冷归档（Parquet/对象存储）+ 全文检索；L3（>1000 万/多租户）分库分表 + 异步落库。
- 状态：`[ ]`（当前量级无需启动）

### 17. BM25 分词升级

- **方案**：中文二元组升级为 jieba 分词 + 企业自定义词典，提升稀疏检索召回。
- 状态：`[ ]`

### 18. MCP 只读接入 + 首个 Skill

- **方案**：接入只读 MCP 工具，配套首个 Skill（后续独立立项）。
- 状态：`[ ]`

---

## 三、基建 / 量化 / 可观测待办

### 19. 未量化指标

- **项**：ACC-01 表格行列还原率、ACC-03 阅读顺序正确率、ACC-04 正文表格不重复率、PERF-01 文本解析吞吐、PERF-02 扫描页 OCR 单页耗时。
- **方案**：建标注集 + 压测脚本，把数值纳入回归。
- 状态：`[ ]`

### 20. 真实样本回归集扩充

- **问题**：examples 仅 9 篇（<10），缺扫描 PDF / 多栏 / 合并单元格真实样本。
- **方案**：补充真实文档至 ≥10 篇并纳入 `tests/test_examples_regression.py`。
- 状态：`[ ]`

### 21. 评测集扩充

- **问题**：负面样本偏少、真实样本 <10 篇。
- **方案**：替换/补充真实用户问题，增加负面样本与拒答用例。
- 状态：`[ ]`

### 22. pytest 覆盖率提升

- **问题**：基线 39%，路由层/SSO/评测/会话落库等 DB/LLM 链路覆盖率低。
- **方案**：随真实库端到端测试与功能开发逐步补足；目标核心服务 ≥70% 再上 `--cov-fail-under`。
- 状态：`[ ]`

### 23. Docker Desktop #531/#532 socket bug

- **说明**：本机开发环境跟踪项（Docker Desktop 已知 socket 问题）。
- 状态：`[ ]`

### 24. 文档标记一致性

- **问题**：`文档入库工程化技术指标` 中 ROB-01 仍写「伪造后缀未防」，但 #12/ROB-02 已实现 magic bytes 校验。
- **方案**：把 ROB-01 / DEP-03（镜像构建）等过时标记同步更新。
- 状态：`[ ]`

---

## 四、已闭环（参照，避免误当遗留）

- [x] 三大问题：会话持久化、知识库自动路由（全库聚合 + 可选指定）、文档解析第一/二层（格式扩展 + 表格结构化）
- [x] 文档入库 P0（补表头/内容嗅探/解析指标/迁移验证/reingest/rebuild）与 P1（2 列无边框表/PDF 合并单元格/跨页续接/多栏顺序/各格式细节）
- [x] P2 大部分：pytest（50+ 例）、CI 确定性门禁、老格式筛除、解析降级链、OCR 三态日志、citations 引用去重、`--cov` 接入
- [x] 2026-09-23 线上：三服务起、前端登录（admin）、ingest/授权、对外端口、dev mode 验证、nginx 探针配置

# AGENTS.md — 项目开发约定

> 本文档是给后续所有在此仓库工作的 agent（以及协作者）的工程化约束，优先级高于默认习惯。
> 适用仓库：`E:\EnterpriseKB`（后端）与 `E:\EnterpriseKBWeb`（前端）。

## 核心原则：面向「工程化、企业可生产使用」交付

这个项目最终是一个给企业真实使用的知识库问答 Agent，不是演示 Demo。每一次改动都要以
「能否安全、稳定、可维护地跑在生产环境」为准绳。具体落地为下面几条硬性要求。

## 必须遵守

1. **向后兼容**：除非明确说明并同步迁移，否则不破坏既有 API 路径、请求/响应字段、
   CLI 命令、环境变量与数据库表结构。数据库变更一律走 `init_db()` 的幂等迁移
   （`ADD COLUMN IF NOT EXISTS` / `CREATE INDEX IF NOT EXISTS`），旧库可直接升级。
2. **安全默认值**：生产必须 `AUTH_DEV_MODE=false`、`AUTH_JWT_SECRET` 随机长串；
   上传走扩展名白名单 + 大小限制 + 流式写盘；任何新增外部输入都要有校验。
3. **不硬编码**：密钥、地址、阈值、模型名等进 `.env` 或 `config/models.yaml`，
   不写死在代码里；新增环境变量要同步 `.env.example` 与 `deploy.env.example`。
4. **可观测**：关键操作（登录、上传、删除、问答、评测、复核等）接审计日志
   （`app/services/audit.py`），异常要可定位（错误信息明确、不吞掉关键异常）。
5. **可测试**：新增/修改非平凡逻辑应能通过自动化验证（pytest 优先；至少要有可重复的
   冒烟脚本），改动后跑 `py_compile`/`compileall`（后端）与 `bun run typecheck`（前端）。
6. **可部署**：改动如需新增依赖，同步 `requirements.txt`（或 `package.json`），并确认
   Docker 镜像构建路径仍可用；新能力默认不拖垮基础环境——重依赖用「延迟导入 + 可选」，
   未安装时给明确提示而不是崩溃。
7. **可追溯文档**：每次实质改动都补开发记录（`20XX-XX-XX-企业知识库助手开发记录.md`），
   并更新受影响的 `README.md` / `docs/*` 与 `docs/待解决问题与解决方案.md` 的状态标记。
8. **存量意识**：动手前先读 `README.md`、`docs/*`、根目录各份开发记录，理解既有设计；
   改动尽量贴合既有分层（core / models / schemas / services / routers）。

## 工程化程度自检（交付前过一遍）

- [ ] 有没有破坏现有接口 / CLI / 环境变量 / 表结构？
- [ ] 新代码有没有处理边界与错误（空文件、坏文件、依赖缺失、超大输入）？
- [ ] 是否加了审计、日志，生产部署是否需要新增环境变量并已同步模板？
- [ ] 是否做了编译/类型检查与可重复的功能验证？
- [ ] 是否更新了开发记录与相关文档？

## 当前已知短板（逐步补齐，不作为新债继续累积）

- 仓库尚无 `tests/` 与 CI 门禁（评测指标 hit@k/MRR 未接入回归）。
- 大量历史改动尚未 git commit（建议尽快按主题整理提交）。
- 扫描 PDF OCR、SSO 真实 IdP 实测、保留策略定时任务等仍为可选/待办。

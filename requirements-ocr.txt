# 可选重依赖：扫描 PDF OCR（RapidOCR，ONNX Runtime 推理）。版本为 2026-09-23 线上实测。
#
# 依赖说明：rapidocr-onnxruntime 声明依赖 opencv-python（完整版，含 GUI 绑定），
#   其需要 X11/GL 系统库（libxcb.so.1 / libGL.so.1 等），python:3.10-slim 不含，直接装会 ImportError。
#   实测：用 opencv-python-headless 替代完整版后，OCR 正常识别中文、且无需任何系统库。
#   安装因此分两步（见 Dockerfile 的 INSTALL_OCR 分支）：
#     1) pip install -r requirements-ocr.txt                 # 会带入完整版 opencv-python
#     2) pip uninstall -y opencv-python && pip install opencv-python-headless==5.0.0.93
# 本地安装：先跑本文件，再执行上面第 2 步。
# 未安装时扫描 PDF 会提示无法提取文字（代码层延迟导入 + 三态诊断）。
rapidocr-onnxruntime==1.4.4
onnxruntime==1.23.2
pyclipper==1.4.0
shapely==2.1.2

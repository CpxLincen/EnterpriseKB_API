# 可选重依赖：扫描 PDF OCR（RapidOCR，ONNX Runtime 推理）。
#
# 说明：rapidocr-onnxruntime 依赖 opencv-python（完整版，含 GUI 绑定），
#       opencv-python 需要 X11/GL 系统库（libxcb.so.1 / libGL.so.1 等），
#       python:3.10-slim 镜像默认不含这些库，直接安装会 ImportError。
#       Docker 部署统一由 Dockerfile 的 INSTALL_OCR 分支用 apt 补齐系统库；
#       本地裸装时需自行 apt 补齐（或临时用 opencv-python-headless 替代验证，
#       但 headless 与 rapidocr 的 opencv-python 依赖存在双 cv2 冲突，生产不走该方案）。
# 安装：Docker 构建设 INSTALL_OCR=true；本地：pip install -r requirements-ocr.txt。
# 未安装时扫描 PDF 会提示无法提取文字（代码层延迟导入 + 三态诊断）。
rapidocr-onnxruntime>=1.4.0,<2.0
